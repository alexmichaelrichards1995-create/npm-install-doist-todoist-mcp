import { ResourceTemplate as Ds, McpServer as xs } from "@modelcontextprotocol/sdk/server/mcp.js";
import { createHash as Rt, randomUUID as Pe } from "node:crypto";
import { readFileSync as As } from "node:fs";
import { dirname as Ut, join as _e } from "node:path";
import { fileURLToPath as Es } from "node:url";
import { registerAppResource as Os, RESOURCE_MIME_TYPE as Lt, registerAppTool as Ps } from "@modelcontextprotocol/ext-apps/server";
import se, { ZodError as _s, z as s } from "zod";
import { AsyncLocalStorage as Rs } from "node:async_hooks";
import { createRequire as Us } from "node:module";
import { TodoistApi as Ls, isWorkspaceProject as ke, isPersonalProject as B, colors as Re, LOCATION_TRIGGERS as Nt, REMINDER_TYPES as Ns, createCommand as V, REMINDER_DELIVERY_SERVICES as Ms, HEALTH_STATUSES as Ge, getTaskUrl as Mt, getProjectUrl as Ft, WORKSPACE_ROLES as Fs, WORKSPACE_PLANS as Hs } from "@doist/todoist-sdk";
import { addDays as Ue, formatISO as lt } from "date-fns";
const ut = Ut(Es(import.meta.url)), Ws = [
  _e(ut, "task-list", "index.html"),
  _e(ut, "mcp-apps", "index.html")
], Bs = `<!doctype html>
<html>
  <body>
    <p>Task list app is missing. Run "npm run build" to generate it.</p>
  </body>
</html>`;
function zs() {
  const e = [];
  for (const t of Ws)
    try {
      return As(t, "utf-8");
    } catch (r) {
      e.push(r);
    }
  return console.error("Failed to load task list app HTML from any known path:", e), Bs;
}
const Ht = zs(), Ks = Rt("sha256").update(Ht).digest("hex").slice(0, 12), Le = `ui://todoist/task-list@${Ks}`, qs = "ui://todoist/task-list@{hash}", Ne = "Interactive task list widget", Ys = "https://ai.todoist.net", Me = {
  ui: {
    prefersBorder: !0,
    csp: {
      // The widget is bundled into one HTML file and does not fetch or load external assets.
      connectDomains: [],
      resourceDomains: []
    }
  },
  "openai/widgetDescription": Ne,
  "openai/widgetPrefersBorder": !0,
  "openai/widgetCSP": {
    connect_domains: [],
    resource_domains: []
  },
  "openai/widgetDomain": Ys
};
function pt(e) {
  return {
    contents: [
      {
        uri: e,
        mimeType: Lt,
        text: Ht,
        _meta: Me
      }
    ]
  };
}
function Gs(e) {
  Os(
    e,
    "todoist-task-list",
    Le,
    {
      description: Ne,
      _meta: Me
    },
    async () => pt(Le)
  ), e.registerResource(
    "todoist-task-list-hash-fallback",
    new Ds(qs, { list: void 0 }),
    {
      description: `${Ne} compatibility fallback`,
      mimeType: Lt,
      _meta: Me
    },
    async (t) => pt(t.toString())
  );
}
function J(e) {
  return typeof e == "object" && e !== null;
}
function Fe(...e) {
  return e.find(J);
}
function L(...e) {
  return e.find((t) => t !== void 0);
}
function E(e) {
  if (typeof e == "number" && Number.isFinite(e))
    return e;
  if (typeof e == "string" && /^\d+$/.test(e.trim()))
    return Number(e.trim());
}
function w(e) {
  if (typeof e == "string") {
    const t = e.trim();
    return t.length > 0 ? t : void 0;
  }
  if (typeof e == "number" || typeof e == "boolean")
    return String(e);
}
function re(e) {
  const t = E(e);
  return t !== void 0 ? t : w(e);
}
function Vs(e) {
  return e.replace(/\b(Bearer)\s+[A-Za-z0-9._~+/=-]+/gi, "$1 [REDACTED]").replace(
    /\b(token|api[_-]?key|authorization)\s*[:=]\s*["']?[A-Za-z0-9._~+/=-]{6,}["']?/gi,
    "$1: [REDACTED]"
  ).replace(/([?&](?:token|api[_-]?key|authorization)=)[^&\s]+/gi, "$1[REDACTED]").replace(/\beyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\b/g, "[REDACTED]");
}
function R(e, t = 220) {
  const r = Vs(e).replace(/\s+/g, " ").trim();
  return r.length <= t ? r : `${r.slice(0, t - 3)}...`;
}
function He(e) {
  const t = e.trim();
  return /\bHTTP\s*\d{3}\b/i.test(t) || /\bstatus code \d{3}\b/i.test(t) || /^bad request$/i.test(t) || /^unauthorized$/i.test(t) || /^forbidden$/i.test(t) || /^not found$/i.test(t);
}
function xe(e) {
  if (!e)
    return;
  const t = e.match(/\b(?:HTTP|status code)\s*[:#-]?\s*(\d{3})\b/i);
  if (t?.[1])
    return E(t[1]);
}
function G(e) {
  const t = w(e);
  if (t)
    return R(t);
  if (Array.isArray(e)) {
    const n = e.map((a) => J(a) ? w(a.message) || w(a.error) : w(a)).filter((a) => !!a);
    return n.length > 0 ? R(n.slice(0, 2).join("; ")) : void 0;
  }
  if (!J(e))
    return;
  const r = L(
    w(e.detail),
    w(e.details),
    w(e.message),
    w(e.error),
    w(e.description)
  );
  if (r)
    return R(r);
  const o = [];
  for (const [n, a] of Object.entries(e)) {
    const i = w(a);
    if (i && (o.push(`${n}: ${i}`), o.length >= 2))
      break;
  }
  return o.length > 0 ? R(o.join("; ")) : void 0;
}
function Js(e) {
  if (!e)
    return [];
  const t = /* @__PURE__ */ new Set(), r = (i) => {
    i && t.add(R(i, 120));
  }, o = L(
    w(e.field),
    w(e.parameter),
    w(e.param),
    w(e.path)
  );
  r(o);
  const n = Fe(
    e.details,
    e.errorDetails,
    e.errorExtra,
    e.error_extra
  );
  if (n) {
    const i = L(
      w(n.field),
      w(n.parameter),
      w(n.param),
      w(n.path),
      w(n.argument)
    );
    r(i);
  }
  const a = e.errors;
  if (Array.isArray(a))
    for (const i of a) {
      if (!J(i)) {
        r(w(i));
        continue;
      }
      const c = L(
        w(i.field),
        w(i.parameter),
        w(i.param),
        w(i.path),
        w(i.name)
      ), d = L(
        w(i.message),
        w(i.error),
        w(i.detail),
        w(i.description)
      );
      r(c && d ? `${c}: ${d}` : c || d);
    }
  else if (J(a))
    for (const [i, c] of Object.entries(a)) {
      const d = w(c);
      if (d) {
        r(`${i}: ${d}`);
        continue;
      }
      if (Array.isArray(c)) {
        const l = c.map((u) => w(u)).filter((u) => !!u).join(", ");
        r(l ? `${i}: ${l}` : i);
      }
    }
  return Array.from(t).slice(0, 3);
}
const Xs = [
  "error",
  "error_code",
  "error_tag",
  "http_code",
  "error_extra",
  "errorCode",
  "errorTag",
  "httpCode",
  "errorExtra"
];
function Zs(e) {
  return e ? Xs.some((t) => e[t] !== void 0) : !1;
}
const We = {
  MAX_ITEMS_LIMIT_REACHED: "This project has reached the maximum number of active tasks per project (subtasks count toward it; completed tasks do not). The cap is the same on every Todoist plan — upgrading will not raise it — and it is not an authentication or permission problem. Complete, delete, or move existing tasks out of the project, or add the task to a different project, then retry.",
  USER_CANNOT_VIEW_TEMPLATE: "This template exists but belongs to another account. Personal templates are private to whoever saved them, and a link to one cannot be used by anyone else. Use a public gallery template instead, or ask the owner to export it with export-project-template and share the CSV."
};
function Qs(...e) {
  for (const t of e) {
    if (!t)
      continue;
    const r = t.toUpperCase(), o = Object.keys(We).find(
      (n) => r.includes(n)
    );
    if (o)
      return We[o];
  }
}
function er(e) {
  if (e.tag) {
    const t = We[e.tag.toUpperCase()];
    if (t)
      return t;
  }
  return Qs(...e.rawText ?? [], e.message, e.details);
}
function tr(e) {
  const t = er(e);
  if (t)
    return t;
  const { statusCode: r } = e, o = e.fieldHints.length > 0;
  return r === 401 ? "Authentication failed. Verify your API token, then retry." : r === 403 ? "Access denied. Retrying the same request will not help. If it's a scope/access problem (for example, insufficient token scope), use a token or account with the required access. Otherwise, change the request or target instead." : r === 404 ? "Confirm the referenced IDs exist and are accessible, then retry." : r === 429 ? "Rate limit reached. Wait briefly and retry." : r !== void 0 && r >= 500 ? "Todoist API may be temporarily unavailable. Retry shortly." : o ? "Fix the field hints above and retry." : r === 400 || r === 422 ? "Check parameter values and formats, then retry." : "Check the request parameters and retry.";
}
function Ve(e) {
  const t = J(e) ? e : void 0, r = e instanceof Error && J(e.cause) ? e.cause : void 0, o = Fe(t?.response, r?.response), n = Fe(
    t?.responseData,
    o?.data,
    t?.data,
    r?.responseData,
    r?.data
  ), a = L(
    E(t?.httpStatusCode),
    E(t?.statusCode),
    E(t?.status),
    E(o?.status),
    E(n?.httpStatusCode),
    E(n?.statusCode),
    E(n?.status),
    E(n?.httpCode),
    E(n?.http_code),
    xe(w(t?.message)),
    xe(w(r?.message)),
    xe(typeof e == "string" ? e : void 0)
  ), i = L(
    re(n?.errorCode),
    re(n?.error_code),
    re(n?.code),
    re(t?.errorCode),
    re(t?.error_code),
    re(t?.code)
  ), c = L(
    w(n?.errorTag),
    w(n?.error_tag),
    w(n?.tag),
    w(t?.errorTag),
    w(t?.error_tag),
    w(t?.tag)
  ), d = [
    w(n?.error),
    w(n?.message),
    G(n?.message),
    w(t?.message),
    w(r?.message),
    e instanceof Error ? e.message : w(e)
  ].filter((h) => !!h), l = d.find((h) => !He(h)) || d[0], u = L(
    G(n?.errorExtra),
    G(n?.error_extra),
    G(n?.details),
    G(n?.errorDetails),
    G(n?.errors),
    G(t?.details)
  ), p = Js(n);
  return a !== void 0 || Zs(n) || c !== void 0 || i !== void 0 || (l ? He(l) : !1) ? {
    statusCode: a,
    code: i,
    tag: c ? R(c, 80) : void 0,
    message: l ? R(l) : void 0,
    details: u ? R(u) : void 0,
    rawText: d,
    fieldHints: p
  } : null;
}
function Wt(e) {
  const t = [];
  return e.statusCode !== void 0 && t.push(`HTTP ${e.statusCode}`), e.code !== void 0 && t.push(`code ${e.code}`), e.tag && t.push(`tag ${e.tag}`), t;
}
function sr(e) {
  const t = Wt(e), r = [
    t.length > 0 ? `Todoist API request failed (${t.join(", ")}).` : "Todoist API request failed."
  ];
  return e.message && r.push(`Message: ${e.message}`), e.details && e.details !== e.message && r.push(`Details: ${e.details}`), e.fieldHints.length > 0 && r.push(`Field hints: ${e.fieldHints.join("; ")}`), r.push(`Try next: ${tr(e)}`), r.join(`
`);
}
function Bt(e) {
  return e instanceof Error ? R(e.message) : typeof e == "string" ? R(e) : "An unknown error occurred";
}
class ve extends Error {
  constructor(t) {
    super(t), this.name = "DueStringParseError";
  }
}
class zt extends Error {
  constructor(t) {
    super(t), this.name = "BatchDueStringParseError";
  }
}
function z(e) {
  if (e instanceof ve)
    return e.message;
  const t = Ve(e);
  if (!t)
    return Bt(e);
  const r = Wt(t), o = t.message && !(r.length > 0 && He(t.message)) ? t.message : "Todoist API request failed";
  return r.length > 0 ? `${o} (${r.join(", ")})` : o;
}
function rr(e, {
  taskContent: t,
  dueString: r,
  deadlineDate: o
}) {
  if (!r)
    return;
  const n = Ve(e), i = n?.tag?.toUpperCase() === "INVALID_DUE_STRING", c = !o && /^invalid date format\.?$/i.test(n?.message ?? "");
  if (!i && !c)
    return;
  const d = r.trim().replace(/^recurring(?:\s+|$)/i, "").trim();
  return d !== r.trim() && d ? `Task "${t}" wasn't created because \`dueString\` could not be parsed. Use Todoist recurrence syntax, for example \`${d}\`; don't prefix it with \`recurring\`. Change only \`dueString\` and retry.` : d !== r.trim() ? `Task "${t}" wasn't created because \`dueString\` could not be parsed. Use a complete Todoist recurrence expression; \`recurring\` alone is not valid. Change only \`dueString\` and retry.` : `Task "${t}" wasn't created because \`dueString\` could not be parsed. Change only \`dueString\` and retry.`;
}
function or(e) {
  if (e instanceof _s || e instanceof ve || e instanceof zt)
    return e.message;
  const t = Ve(e);
  return t ? sr(t) : Bt(e);
}
const nr = "12.5.3", ar = {
  version: nr
}, C = {
  /** Default limit for task listings */
  TASKS_DEFAULT: 10,
  /** Maximum limit for task search and list operations */
  TASKS_MAX: 100,
  /** Default limit for completed tasks */
  COMPLETED_TASKS_DEFAULT: 50,
  /** Maximum limit for completed tasks */
  COMPLETED_TASKS_MAX: 200,
  /** Default limit for project listings */
  PROJECTS_DEFAULT: 50,
  /** Maximum limit for project listings */
  PROJECTS_MAX: 200,
  /** Maximum limit for section listings */
  SECTIONS_MAX: 200,
  /** Batch size for fetching all tasks in a project */
  TASKS_BATCH_SIZE: 50,
  /** Maximum number of direct children returned when children are requested alongside an object */
  CHILDREN_MAX: 25,
  /** Default limit for comment listings */
  COMMENTS_DEFAULT: 10,
  /** Maximum limit for comment search and list operations */
  COMMENTS_MAX: 10,
  /** Default limit for activity log listings */
  ACTIVITY_DEFAULT: 20,
  /** Maximum limit for activity log search and list operations */
  ACTIVITY_MAX: 100,
  /** Default limit for label listings */
  LABELS_DEFAULT: 50,
  /** Maximum limit for label listings */
  LABELS_MAX: 200
}, fe = {
  /** Maximum number of failures to show in detailed error messages */
  MAX_FAILURES_SHOWN: 3
}, Kt = {
  /** Maximum tasks accepted by one task create or update operation */
  TASKS_PER_OPERATION: 25
}, Be = {
  /**
   * In-flight task-move requests per account, per process.
   *
   * Kept at 1 because the API locks the whole task tree for a move, and a tree
   * spans a task's source as well as its destination — two moves of sibling
   * subtasks contend even when they target different projects, and the loser
   * fails. Batching same-destination moves into a single request means
   * serialising costs little in practice. One per process is the smallest
   * contribution a process can make; see the note above for the global picture.
   */
  TASK_MOVES: 1,
  /** In-flight non-move write requests per account, per process. */
  WRITES: 4,
  /**
   * How long a request may wait for a slot before it is abandoned unsent.
   *
   * Matches the SDK's own 30s request timeout, which only starts once a request
   * actually goes out: a task that has already waited that long would be
   * answering a caller who has given up. Comfortably above the worst realistic
   * queue (serialised moves to 100 distinct destinations is roughly 15s).
   */
  QUEUE_WAIT_MS: 3e4
};
function mt(e, { queueTimeoutMs: t = Be.QUEUE_WAIT_MS } = {}) {
  if (!Number.isInteger(e) || e < 1)
    throw new Error(
      `maxConcurrent must be a positive integer, received ${String(e)}`
    );
  const r = [];
  let o = 0;
  async function n() {
    if (o < e) {
      o++;
      return;
    }
    await new Promise((i, c) => {
      const d = { admit: i, abandon: c };
      r.push(d), !(t <= 0) && (d.timer = setTimeout(() => {
        const l = r.indexOf(d);
        l !== -1 && r.splice(l, 1), d.abandon(
          new Error(
            `Timed out after ${t}ms waiting for an earlier request to finish; this request was not sent`
          )
        );
      }, t), d.timer.unref?.());
    });
  }
  function a() {
    const i = r.shift();
    if (i) {
      clearTimeout(i.timer), i.admit();
      return;
    }
    o--;
  }
  return async function(c) {
    await n();
    try {
      return await c();
    } finally {
      a();
    }
  };
}
function qt() {
  return {
    moves: mt(Be.TASK_MOVES),
    writes: mt(Be.WRITES)
  };
}
const ht = /* @__PURE__ */ new Map(), Yt = /* @__PURE__ */ new WeakMap();
let ft;
function ir() {
  return ft ??= qt(), ft;
}
function cr(e) {
  return Rt("sha256").update(e).digest("hex");
}
function dr(e, t) {
  const r = cr(t);
  let o = ht.get(r);
  o || (o = qt(), ht.set(r, o)), Yt.set(e, o);
}
function Gt(e) {
  return Yt.get(e) ?? ir();
}
function lr(e) {
  return Gt(e).moves;
}
function ur(e) {
  return Gt(e).writes;
}
const pr = "todoist-mcp", Je = ar.version, Vt = new Rs(), bt = Us(import.meta.url);
let Te;
const mr = async () => {
  const e = bt.resolve("@doist/todoist-sdk"), t = _e(Ut(e), "transport", "http-dispatcher.js");
  return bt(t);
};
let hr = mr;
function fr() {
  return `${pr}/${Je}`;
}
function Jt(e) {
  return e.trim().toLowerCase();
}
function br(e, t) {
  return Vt.run(Jt(e), t);
}
function gr({
  label: e,
  platform: t = "mcp",
  sessionId: r = Pe()
} = {}) {
  const o = e ? Jt(e) : Vt.getStore();
  return {
    "User-Agent": fr(),
    "doist-platform": t,
    "doist-version": Je,
    "request-id": Pe(),
    "session-id": r,
    "mcp-tool": o ?? "unknown"
  };
}
function yr(e, t) {
  const r = new Headers(e);
  for (const [o, n] of Object.entries(gr(t)))
    r.set(o, n);
  return Object.fromEntries(r.entries());
}
function gt() {
  return new DOMException("The operation was aborted due to timeout", "TimeoutError");
}
function Tr(e, t) {
  if (!e && t === void 0)
    return {
      signal: void 0,
      cleanup: () => {
      }
    };
  if (!e && t !== void 0) {
    const i = new AbortController(), c = setTimeout(() => i.abort(gt()), t);
    return {
      signal: i.signal,
      cleanup: () => clearTimeout(c)
    };
  }
  if (e && t === void 0)
    return {
      signal: e,
      cleanup: () => {
      }
    };
  const r = new AbortController(), o = [], n = (i) => {
    r.signal.aborted || r.abort(i);
  };
  if (e?.aborted)
    n(e.reason);
  else if (e) {
    const i = () => n(e.reason);
    e.addEventListener("abort", i, { once: !0 }), o.push(() => e.removeEventListener("abort", i));
  }
  const a = setTimeout(() => n(gt()), t);
  return o.push(() => clearTimeout(a)), {
    signal: r.signal,
    cleanup: () => {
      for (const i of o)
        i();
    }
  };
}
async function kr() {
  if (wr())
    return Te || (Te = hr().then((e) => e.getDefaultDispatcher()).catch((e) => {
      throw Te = void 0, e;
    })), Te;
}
function wr() {
  return typeof process < "u" && !!process.versions?.node;
}
async function vr(e) {
  const t = await kr();
  t !== void 0 && (e.dispatcher = t);
}
function Ir(e) {
  return {
    ok: e.ok,
    status: e.status,
    statusText: e.statusText,
    headers: Object.fromEntries(e.headers.entries()),
    text: () => e.text(),
    json: () => e.json(),
    arrayBuffer: () => e.arrayBuffer()
  };
}
function Sr(e = globalThis.fetch, t = {}) {
  const r = e === globalThis.fetch, { enabled: o = !0, platform: n = "mcp", sessionId: a = Pe() } = t;
  return async (i, c = {}) => {
    const { timeout: d, headers: l, signal: u, ...p } = c, { signal: m, cleanup: h } = Tr(u, d);
    try {
      const y = {
        ...p,
        signal: m,
        ...o ? { headers: yr(l, { platform: n, sessionId: a }) } : l !== void 0 ? { headers: l } : {}
      };
      r && await vr(y);
      const f = await e(i, y);
      return Ir(f);
    } finally {
      h();
    }
  };
}
function jr(e, {
  baseUrl: t,
  tracking: r
} = {}) {
  const o = new Ls(e, {
    customFetch: Sr(globalThis.fetch, r),
    ...t ? { baseUrl: t } : {}
  });
  return dr(o, e), o;
}
const Cr = 2, $r = 500, Dr = 2e3, xr = 50, Ar = /* @__PURE__ */ new Set([502, 503, 504]);
function Er(e) {
  if (e == null || typeof e != "object")
    return;
  const t = e;
  if (typeof t.httpStatusCode == "number")
    return t.httpStatusCode;
  if (typeof t.statusCode == "number")
    return t.statusCode;
  if (typeof t.status == "number")
    return t.status;
  if (e instanceof Error) {
    const r = e.message.match(/\bHTTP\s+(\d{3})\b/i);
    if (r?.[1])
      return Number(r[1]);
  }
}
function Or(e) {
  const t = Er(e);
  return t !== void 0 && Ar.has(t);
}
function Pr({
  attempt: e,
  baseDelayMs: t,
  maxDelayMs: r,
  random: o = Math.random
}) {
  const n = Math.min(t * 2 ** e, r), a = Math.round(o() * n);
  return Math.min(n, Math.max(xr, a));
}
function _r(e) {
  return new Promise((t) => setTimeout(t, e));
}
async function O(e, t = {}) {
  const r = t.maxRetries ?? Cr, o = t.baseDelayMs ?? $r, n = t.maxDelayMs ?? Dr, a = t.random;
  let i;
  for (let c = 0; c <= r; c++)
    try {
      return await e();
    } catch (d) {
      if (i = d, c < r && Or(d)) {
        const l = Pr({ attempt: c, baseDelayMs: o, maxDelayMs: n, random: a });
        await _r(l);
        continue;
      }
      throw d;
    }
  throw i;
}
function ze(e) {
  if (e == null)
    return e;
  if (Array.isArray(e))
    return e.map((t) => ze(t));
  if (typeof e == "object") {
    const t = {};
    for (const [r, o] of Object.entries(e))
      if (o !== null) {
        const n = ze(o);
        if (n !== null && typeof n == "object" && !Array.isArray(n) && Object.keys(n).length === 0)
          continue;
        t[r] = n;
      }
    return t;
  }
  return e;
}
const g = {
  // Task management tools
  ADD_TASKS: "add-tasks",
  COMPLETE_TASKS: "complete-tasks",
  UNCOMPLETE_TASKS: "uncomplete-tasks",
  UPDATE_TASKS: "update-tasks",
  FIND_TASKS: "find-tasks",
  FIND_TASKS_BY_DATE: "find-tasks-by-date",
  FIND_COMPLETED_TASKS: "find-completed-tasks",
  RESCHEDULE_TASKS: "reschedule-tasks",
  // Project management tools
  ADD_PROJECTS: "add-projects",
  UPDATE_PROJECTS: "update-projects",
  FIND_PROJECTS: "find-projects",
  PROJECT_MANAGEMENT: "project-management",
  PROJECT_MOVE: "project-move",
  // Section management tools
  ADD_SECTIONS: "add-sections",
  UPDATE_SECTIONS: "update-sections",
  FIND_SECTIONS: "find-sections",
  // Comment management tools
  ADD_COMMENTS: "add-comments",
  UPDATE_COMMENTS: "update-comments",
  FIND_COMMENTS: "find-comments",
  // Reminder management tools
  ADD_REMINDERS: "add-reminders",
  UPDATE_REMINDERS: "update-reminders",
  FIND_REMINDERS: "find-reminders",
  // Attachment tools
  VIEW_ATTACHMENT: "view-attachment",
  // Assignment and collaboration tools
  FIND_PROJECT_COLLABORATORS: "find-project-collaborators",
  MANAGE_ASSIGNMENTS: "manage-assignments",
  // Activity and audit tools
  FIND_ACTIVITY: "find-activity",
  GET_PRODUCTIVITY_STATS: "get-productivity-stats",
  // Health and insights tools
  GET_PROJECT_HEALTH: "get-project-health",
  GET_PROJECT_ACTIVITY_STATS: "get-project-activity-stats",
  ANALYZE_PROJECT_HEALTH: "analyze-project-health",
  GET_WORKSPACE_INSIGHTS: "get-workspace-insights",
  // General tools
  GET_OVERVIEW: "get-overview",
  DELETE_OBJECT: "delete-object",
  FETCH_OBJECT: "fetch-object",
  REORDER_OBJECTS: "reorder-objects",
  USER_INFO: "user-info",
  // Label management tools
  ADD_LABELS: "add-labels",
  UPDATE_LABELS: "update-labels",
  FIND_LABELS: "find-labels",
  // Filter management tools
  FIND_FILTERS: "find-filters",
  ADD_FILTERS: "add-filters",
  UPDATE_FILTERS: "update-filters",
  // Template tools
  EXPORT_PROJECT_TEMPLATE: "export-project-template",
  IMPORT_PROJECT_TEMPLATE: "import-project-template",
  // Workspace tools
  LIST_WORKSPACES: "list-workspaces",
  // OpenAI MCP tools
  SEARCH: "search",
  FETCH: "fetch"
}, Fd = {
  /**
   * Strips email addresses from tool outputs that expose user data.
   * Affects: find-project-collaborators, find-completed-tasks
   */
  STRIP_EMAILS: "strip_emails"
}, Rr = process.env.USE_STRUCTURED_CONTENT === "true" || process.env.NODE_ENV === "test";
function Ur({
  textContent: e,
  structuredContent: t,
  contentItems: r
}) {
  const o = ze(t), n = {}, a = [];
  return r && a.push(...r), !Rr && t && a.push({
    type: "text",
    text: JSON.stringify(o)
  }), e && a.push({ type: "text", text: e }), a.length > 0 && (n.content = a), t && (n.structuredContent = o), n;
}
function Lr(e) {
  return {
    content: [{ type: "text", text: e }],
    isError: !0
  };
}
const Nr = /* @__PURE__ */ new Set(["file"]);
function Mr(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.fromEntries(
    Object.entries(e).map(
      ([t, r]) => Nr.has(t) && r !== void 0 ? [t, "[redacted]"] : [t, r]
    )
  );
}
function Fr(e) {
  return { ...{
    title: `Todoist: ${Hr(e.name)}`,
    openWorldHint: !1
  }, ...e.annotations };
}
function Hr(e) {
  return e.split("-").filter(Boolean).map((t) => `${t.charAt(0).toUpperCase()}${t.slice(1)}`).join(" ");
}
function Wr(e) {
  return e ? typeof e["ui/resourceUri"] == "string" ? !0 : typeof e.ui == "object" && e.ui !== null : !1;
}
const Br = [
  g.FIND_PROJECT_COLLABORATORS,
  g.FIND_COMPLETED_TASKS
];
function Ke(e) {
  if (e == null)
    return e;
  if (Array.isArray(e))
    return e.map((t) => Ke(t));
  if (typeof e == "object") {
    const t = {};
    for (const [r, o] of Object.entries(e))
      r !== "email" && (t[r] = Ke(o));
    return t;
  }
  return e;
}
function zr(e) {
  const t = /\s*\([^)]*@[^)]+\)/g, r = /\S+@\S+\.\S+/g;
  return e.replace(t, "").replace(r, "[email hidden]");
}
function Kr({
  tool: e,
  server: t,
  client: r,
  features: o = []
}) {
  const n = o.some((d) => d.name === "strip_emails") && Br.includes(e.name), a = e.execute, i = async (d, l) => {
    try {
      let { textContent: u, structuredContent: p, contentItems: m } = await br(
        e.name,
        () => O(() => a(d, r))
      );
      return n && (u && (u = zr(u)), p && (p = Ke(p))), Ur({ textContent: u, structuredContent: p, contentItems: m });
    } catch (u) {
      return console.error(`Error executing tool ${e.name}:`, { args: Mr(d), error: u }), Lr(or(u));
    }
  }, c = {
    description: e.description,
    inputSchema: e.parameters,
    ...e.outputSchema ? { outputSchema: e.outputSchema } : {},
    annotations: Fr(e),
    ...e._meta ? { _meta: e._meta } : {}
  };
  if (Wr(e._meta)) {
    Ps(
      t,
      e.name,
      {
        ...c,
        _meta: e._meta
      },
      i
    );
    return;
  }
  t.registerTool(e.name, c, i);
}
const qr = {
  period: s.enum(["today", "7d", "14d", "30d", "this-week", "this-month"]).default("7d").describe(
    'Time period for the analysis. "today" analyzes the current day, "7d"/"14d"/"30d" analyze the last N days, "this-week" and "this-month" analyze the current week or month.'
  ),
  focus: s.enum(["overall", "goals", "projects", "trends", "recommendations"]).default("overall").describe(
    'Focus area for the analysis. "overall" provides a comprehensive report covering all areas. Other values focus on a specific aspect.'
  ),
  projectId: s.string().optional().describe("Optional project ID to scope the analysis to a specific project.")
};
function Yr(e, t = /* @__PURE__ */ new Date()) {
  const r = (n) => n.toISOString().slice(0, 10), o = r(t);
  switch (e) {
    case "today":
      return { since: o, until: o, periodDescription: "today" };
    case "7d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 6), {
        since: r(n),
        until: o,
        periodDescription: "the last 7 days"
      };
    }
    case "14d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 13), {
        since: r(n),
        until: o,
        periodDescription: "the last 14 days"
      };
    }
    case "30d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 29), {
        since: r(n),
        until: o,
        periodDescription: "the last 30 days"
      };
    }
    case "this-week": {
      const n = t.getUTCDay(), a = new Date(t);
      return a.setDate(t.getDate() - (n + 6) % 7), {
        since: r(a),
        until: o,
        periodDescription: "this week (Monday to today)"
      };
    }
    case "this-month": {
      const n = new Date(Date.UTC(t.getUTCFullYear(), t.getUTCMonth(), 1));
      return {
        since: r(n),
        until: o,
        periodDescription: "this month"
      };
    }
  }
}
function Gr(e) {
  const t = {
    goals: `### Goal Tracking
- Compare tasks completed today vs the daily goal
- Calculate progress toward the weekly goal (using daysItems to sum completions for the current week)
- Report on the current daily and weekly streaks — how long they've been going, and how they compare to the user's best streaks
- Flag if the user is at risk of breaking a streak`,
    trends: `### Completion Trends
- Show a day-by-day breakdown of tasks completed over the period
- Identify peak productivity days and low-activity days
- Note any patterns (e.g., weekday vs weekend, specific days that are consistently high or low)
- Compare weekly totals if multiple weeks are in the period`,
    projects: `### Project Distribution
- Break down completions by project (using per-project data from daysItems/weekItems)
- Identify which projects received the most and least attention
- Highlight any projects with zero completions in the period
- If a specific project was requested, focus the analysis on that project's performance`,
    recommendations: `### Karma & Momentum
- Report the current karma score and trend
- Summarize recent karma changes and their reasons

### Recommendations
- Based on all the data gathered, provide 3-5 specific, actionable suggestions
- Address any concerning patterns (declining completions, broken streaks, neglected projects)
- Suggest concrete next steps (e.g., "Focus on Project X which has had no completions this week")
- If goals seem too easy or too hard based on actual completion rates, suggest adjustments`
  };
  return e === "overall" ? Object.values(t).join(`

`) : t[e] ?? "";
}
function Vr(e) {
  const { since: t, until: r, periodDescription: o } = Yr(e.period), n = e.projectId ? `
Scope this analysis to project ID: ${e.projectId}. Pass this projectId when calling find-completed-tasks and get-overview.` : "";
  return `Analyze my Todoist productivity for ${o} (${t} to ${r}).${n}

## Instructions

Follow these steps to gather data and produce the analysis:

### Step 1: Gather Data

Call the following tools to collect the necessary data:

1. **user-info** — Get my timezone, daily/weekly goals, tasks completed today, and user ID (you'll need the user ID for the responsibleUser filter)
2. **get-productivity-stats** — Get streak data, karma score/trend, daily and weekly completion breakdowns, and karma history
3. **find-completed-tasks** — Get completed tasks for the period (since: "${t}", until: "${r}"). Set responsibleUser to my user ID (from user-info) to see only my completions${e.projectId ? `. Set projectId to "${e.projectId}"` : ""}
${e.focus === "overall" || e.focus === "projects" ? `4. **get-overview** — Get the project structure${e.projectId ? ` for project "${e.projectId}"` : ""} to provide context for the analysis` : ""}

### Step 2: Analyze

Using the collected data, produce an analysis covering the following sections:

${Gr(e.focus)}

### Step 3: Format

Present the analysis as a clear, well-structured markdown report with:
- A summary header with the period and key headline metrics
- Each analytical section with its own header
- Specific numbers and comparisons (not vague statements)
- Actionable takeaways highlighted clearly`;
}
function Jr(e) {
  return {
    messages: [
      {
        role: "user",
        content: {
          type: "text",
          text: Vr(e)
        }
      }
    ]
  };
}
const he = {
  name: "productivity-analysis",
  title: "Productivity Analysis",
  description: "Analyze your Todoist productivity with insights on completion trends, goal streaks, project distribution, and actionable recommendations. Gathers data from multiple tools and synthesizes a comprehensive report.",
  argsSchema: qr,
  callback: Jr
};
class _ extends Error {
  constructor(t, r) {
    super(`Invalid duration format "${t}": ${r}`), this.name = "DurationParseError";
  }
}
function Xt(e) {
  if (!e || typeof e != "string")
    throw new _(e, "Duration must be a non-empty string");
  const t = e.trim().toLowerCase().replace(/\s+/g, "");
  if (!t)
    throw new _(e, "Duration must be a non-empty string");
  const r = t.match(/^(?:(\d+(?:\.\d+)?)h)?(?:(\d+(?:\.\d+)?)m)?$/);
  if (!r || !r[1] && !r[2])
    throw new _(e, 'Use format like "2h", "30m", "2h30m", or "1.5h"');
  let o = 0;
  const [, n, a] = r;
  if (n) {
    const i = Number.parseFloat(n);
    if (Number.isNaN(i) || i < 0)
      throw new _(e, "Hours must be a positive number");
    o += i * 60;
  }
  if (a) {
    const i = Number.parseFloat(a);
    if (Number.isNaN(i) || i < 0)
      throw new _(e, "Minutes must be a positive number");
    if (i % 1 !== 0)
      throw new _(
        e,
        "Minutes must be a whole number (use decimal hours instead)"
      );
    o += i;
  }
  if (o = Math.round(o), o === 0)
    throw new _(e, "Duration must be greater than 0 minutes");
  if (o > 1440)
    throw new _(e, "Duration cannot exceed 24 hours (1440 minutes)");
  return { minutes: o };
}
function Xr(e) {
  if (e <= 0) return "0m";
  const t = Math.floor(e / 60), r = e % 60;
  return t === 0 ? `${r}m` : r === 0 ? `${t}h` : `${t}h${r}m`;
}
const Zr = ["p1", "p2", "p3", "p4"], Xe = 'Task priority as a string: "p1" (highest), "p2" (high), "p3" (medium), or "p4" (lowest/default). Integers like 1, 2, 3, or 4 are not accepted.', Ze = s.enum(Zr).describe(Xe);
function Zt(e) {
  return { p1: 4, p2: 3, p3: 2, p4: 1 }[e];
}
function Qr(e) {
  return { 4: "p1", 3: "p2", 2: "p3", 1: "p4" }[e];
}
const Qt = 300 * 1e3, eo = 1e4, to = 1e3;
class es {
  constructor(t, r) {
    this.maxEntries = t, this.ttl = r, this.entries = /* @__PURE__ */ new Map();
  }
  get(t) {
    const r = this.entries.get(t);
    if (r) {
      if (Date.now() - r.timestamp >= this.ttl) {
        this.entries.delete(t);
        return;
      }
      return this.entries.delete(t), this.entries.set(t, r), r.result;
    }
  }
  set(t, r) {
    if (this.entries.delete(t), this.entries.size >= this.maxEntries && this.pruneExpiredEntries(), this.entries.size >= this.maxEntries) {
      const o = this.entries.keys().next().value;
      o !== void 0 && this.entries.delete(o);
    }
    this.entries.set(t, { result: r, timestamp: Date.now() });
  }
  clear() {
    this.entries.clear();
  }
  get size() {
    return this.entries.size;
  }
  pruneExpiredEntries() {
    const t = Date.now();
    for (const [r, o] of this.entries)
      t - o.timestamp >= this.ttl && this.entries.delete(r);
  }
}
const Ae = new es(
  eo,
  Qt
), oe = new es(
  to,
  Qt
), so = "me";
class ro {
  /**
   * Resolve a user name or ID to a user ID by looking up collaborators across all shared projects.
   * Supports exact name matches, partial matches, email matches, and the "me" keyword.
   */
  async resolveUser(t, r) {
    if (!r || r.trim().length === 0)
      return null;
    const o = r.trim();
    if (o.toLowerCase() === so)
      try {
        const d = await t.getUser();
        return {
          userId: d.id,
          displayName: d.fullName,
          email: d.email
        };
      } catch {
        return null;
      }
    if (/^[0-9]+$/.test(o) || /^[a-f0-9-]{8,}$/i.test(o) && o.includes("-") || /^[a-z0-9_]{6,}$/i.test(o) && !/^[a-z]+[\s-]/.test(o) && /[0-9_]/.test(o))
      return { userId: o, displayName: o, email: o };
    const n = await this.getCacheScope(t), a = this.getCacheKey(n, `user_${o}`), i = a ? Ae.get(a) : void 0;
    if (i !== void 0) return i;
    const c = (d) => (a && Ae.set(a, d), d);
    try {
      let d = await this.getAllCollaborators(t, n);
      try {
        const p = await t.getUser();
        if (p) {
          const m = {
            id: p.id,
            name: p.fullName,
            email: p.email
          };
          d.some((h) => h.id === p.id) || (d = [m, ...d]);
        }
      } catch {
      }
      if (d.length === 0)
        return c(null);
      const l = r.toLowerCase().trim();
      let u = d.find((p) => p.id === o);
      return u ? c({
        userId: u.id,
        displayName: u.name,
        email: u.email
      }) : (u = d.find((p) => p.name.toLowerCase() === l), u ? c({
        userId: u.id,
        displayName: u.name,
        email: u.email
      }) : (u = d.find((p) => p.email.toLowerCase() === l), u ? c({
        userId: u.id,
        displayName: u.name,
        email: u.email
      }) : (u = d.find((p) => p.name.toLowerCase().includes(l)), u ? c({
        userId: u.id,
        displayName: u.name,
        email: u.email
      }) : (u = d.find((p) => p.email.toLowerCase().includes(l)), c(u ? {
        userId: u.id,
        displayName: u.name,
        email: u.email
      } : null)))));
    } catch {
      return c(null);
    }
  }
  /**
   * Validate that a user is a collaborator on a specific project
   */
  async validateProjectCollaborator(t, r, o) {
    try {
      return (await this.getProjectCollaborators(t, r)).some((a) => a.id === o);
    } catch {
      return !1;
    }
  }
  /**
   * Get collaborators for a specific project
   */
  async getProjectCollaborators(t, r, o) {
    const n = o === void 0 ? await this.getCacheScope(t) : o, a = this.getCacheKey(n, `project_${r}`), i = a ? oe.get(a) : void 0;
    if (i !== void 0) return i;
    try {
      const c = await t.getProjectCollaborators(r), l = (Array.isArray(c) ? c : c.results || []).filter((u) => u?.id && u.name && u.email);
      return a && oe.set(a, l), l;
    } catch {
      return [];
    }
  }
  /**
   * Get all collaborators from all shared projects, deduplicated by user ID.
   */
  async getAllCollaborators(t, r) {
    const o = r === void 0 ? await this.getCacheScope(t) : r, n = this.getCacheKey(o, "all_collaborators"), a = n ? oe.get(n) : void 0;
    if (a !== void 0) return a;
    try {
      const c = (await M({
        apiMethod: t.getProjects.bind(t),
        args: {}
      })).filter((m) => m.isShared);
      if (c.length === 0) {
        const m = [];
        return n && oe.set(n, m), m;
      }
      const d = [], l = /* @__PURE__ */ new Set(), u = c.map(
        (m) => this.getProjectCollaborators(t, m.id, o)
      ), p = await Promise.allSettled(u);
      for (const m of p)
        if (m.status === "fulfilled")
          for (const h of m.value)
            h && !l.has(h.id) && (d.push(h), l.add(h.id));
      return n && oe.set(n, d), d;
    } catch {
      return [];
    }
  }
  async getCacheScope(t) {
    try {
      return (await t.getUser())?.id ?? null;
    } catch {
      return null;
    }
  }
  getCacheKey(t, r) {
    return t ? `${t}:${r}` : null;
  }
  /**
   * Clear all caches - useful for testing
   */
  clearCache() {
    Ae.clear(), oe.clear();
  }
}
const W = new ro();
async function oo(e, t) {
  return W.resolveUser(e, t);
}
const ts = ["assigned", "unassignedOrMe", "all"];
async function Qe(e, t) {
  if (!t)
    return;
  const r = await oo(e, t);
  if (!r)
    throw new Error(
      `Could not find user: "${t}". Make sure the user is a collaborator on a shared project.`
    );
  return { userId: r.userId, email: r.email };
}
function ce(e, t) {
  return t.length === 0 ? e : e.length === 0 ? t : `${e} & ${t}`;
}
function ss({
  resolvedAssigneeId: e,
  assigneeEmail: t,
  responsibleUserFiltering: r = "unassignedOrMe"
}) {
  return e && t ? `assigned to: ${t}` : r === "unassignedOrMe" ? "!assigned to: others" : r === "assigned" ? "assigned to: others" : "";
}
function no({
  tasks: e,
  resolvedAssigneeId: t,
  currentUserId: r,
  responsibleUserFiltering: o = "unassignedOrMe"
}) {
  return t ? e.filter((n) => n.responsibleUid === t) : o === "unassignedOrMe" ? e.filter((n) => !n.responsibleUid || n.responsibleUid === r) : o === "assigned" ? e.filter((n) => n.responsibleUid && n.responsibleUid !== r) : e;
}
function N(e) {
  return e?.toLowerCase() === "inbox";
}
async function de(e) {
  const { projectId: t, user: r, client: o } = e;
  if (!N(t))
    return t;
  const n = r || (o ? await o.getUser() : null);
  if (!n)
    throw new Error("Either user or client must be provided when resolving inbox project ID");
  return n.inboxProjectId;
}
async function M(e) {
  const { apiMethod: t, args: r, limit: o = 100 } = e, n = [];
  let a = null;
  do {
    const i = await t({
      ...r,
      cursor: a,
      limit: o
    });
    n.push(...i.results), a = i.nextCursor ?? null;
  } while (a !== null);
  return n;
}
function et(e) {
  return /(?<!\\)(?:\\\\)*\*/.test(e) ? e : `*${e.replaceAll(/\\(?!\*)/g, "\\\\")}*`;
}
async function rs(e, t) {
  return M({
    apiMethod: e.searchProjects.bind(e),
    args: { query: et(t) },
    limit: C.PROJECTS_MAX
  });
}
async function os(e) {
  return M({
    apiMethod: e.getProjects.bind(e),
    args: {},
    limit: C.PROJECTS_MAX
  });
}
async function ao(e) {
  return M({
    apiMethod: e.getArchivedProjects.bind(e),
    args: {},
    limit: C.PROJECTS_MAX
  });
}
function io(e) {
  const t = (a) => a.replaceAll(/[.*+?^${}()|[\]\\]/g, "\\$&"), r = /(?<!\\)(?:\\\\)*\*/.test(e), o = [...e];
  let n = "";
  for (let a = 0; a < o.length; a++) {
    const i = o[a];
    i === "\\" && o[a + 1] === "\\" ? (n += "\\\\", a++) : i === "\\" && o[a + 1] === "*" ? (n += "\\*", a++) : i === "*" ? n += ".*" : n += t(i);
  }
  return new RegExp(r ? `^${n}$` : n, "iu");
}
async function co(e, t) {
  return M({
    apiMethod: e.searchLabels.bind(e),
    args: { query: et(t) },
    limit: C.LABELS_MAX
  });
}
async function lo(e) {
  return M({
    apiMethod: e.getSharedLabels.bind(e),
    args: {},
    limit: C.LABELS_MAX
  });
}
async function uo(e, t, r) {
  const o = et(t);
  return M({
    apiMethod: e.searchSections.bind(e),
    args: r ? { query: o, projectId: r } : { query: o },
    limit: C.SECTIONS_MAX
  });
}
function P(e) {
  return {
    id: e.id,
    content: e.content,
    description: e.description,
    dueDate: e.due?.date,
    recurring: e.due?.isRecurring && e.due.string ? e.due.string : !1,
    deadlineDate: e.deadline?.date,
    priority: Qr(e.priority) ?? "p4",
    projectId: e.projectId,
    sectionId: e.sectionId ?? void 0,
    parentId: e.parentId ?? void 0,
    labels: e.labels,
    duration: e.duration ? Xr(e.duration.amount) : void 0,
    responsibleUid: e.responsibleUid ?? void 0,
    assignedByUid: e.assignedByUid ?? void 0,
    checked: e.checked,
    completedAt: e.completedAt?.toISOString() ?? void 0,
    addedAt: e.addedAt?.toISOString() ?? void 0
  };
}
function Z(e) {
  return {
    id: e.id,
    name: e.name,
    description: e.description,
    color: e.color,
    isFavorite: e.isFavorite,
    isShared: e.isShared,
    parentId: B(e) ? e.parentId ?? void 0 : void 0,
    inboxProject: B(e) ? e.inboxProject ?? !1 : !1,
    viewStyle: e.viewStyle,
    workspaceId: ke(e) ? e.workspaceId : void 0,
    folderId: ke(e) ? e.folderId ?? void 0 : void 0,
    childOrder: e.childOrder,
    isArchived: e.isArchived
  };
}
function ge(e) {
  return {
    id: e.id,
    taskId: e.taskId ?? void 0,
    projectId: e.projectId ?? void 0,
    content: e.content,
    postedAt: e.postedAt.toISOString(),
    postedUid: e.postedUid ?? void 0,
    fileAttachment: e.fileAttachment ? {
      resourceType: e.fileAttachment.resourceType,
      fileName: e.fileAttachment.fileName ?? void 0,
      fileSize: e.fileAttachment.fileSize ?? void 0,
      fileType: e.fileAttachment.fileType ?? void 0,
      fileUrl: e.fileAttachment.fileUrl ?? void 0,
      fileDuration: e.fileAttachment.fileDuration ?? void 0,
      uploadState: e.fileAttachment.uploadState ?? void 0,
      url: e.fileAttachment.url ?? void 0,
      title: e.fileAttachment.title ?? void 0,
      image: e.fileAttachment.image ?? void 0,
      imageWidth: e.fileAttachment.imageWidth ?? void 0,
      imageHeight: e.fileAttachment.imageHeight ?? void 0
    } : void 0
  };
}
function po(e) {
  return {
    id: e.id ?? void 0,
    objectType: e.objectType,
    objectId: e.objectId,
    eventType: e.eventType,
    eventDate: e.eventDate.toISOString(),
    parentProjectId: e.parentProjectId ?? void 0,
    parentItemId: e.parentItemId ?? void 0,
    initiatorId: e.initiatorId ?? void 0,
    extraData: e.extraData ?? void 0
  };
}
const mo = se.object({
  httpStatusCode: se.number(),
  responseData: se.object({
    error: se.string(),
    errorCode: se.number(),
    errorTag: se.string()
  })
});
async function tt({
  client: e,
  query: t,
  limit: r,
  cursor: o
}) {
  try {
    const { results: n, nextCursor: a } = await e.getTasksByFilter({ query: t, cursor: o, limit: r });
    return { tasks: n.map(P), nextCursor: a };
  } catch (n) {
    const a = mo.safeParse(n);
    if (!a.success)
      throw n;
    const { responseData: i } = a.data;
    throw i.errorTag === "INVALID_SEARCH_QUERY" ? new Error(`Invalid filter query: ${t}`) : new Error(
      `${i.error} (tag: ${i.errorTag}, code: ${i.errorCode})`
    );
  }
}
function yt(e) {
  return {
    isRecurring: e.isRecurring,
    string: e.string,
    date: e.date,
    datetime: e.datetime ?? void 0,
    timezone: e.timezone ?? void 0
  };
}
function be(e) {
  const t = {
    id: e.id,
    taskId: e.itemId,
    type: e.type
  };
  switch (e.type) {
    case "relative":
      return {
        ...t,
        minuteOffset: e.minuteOffset,
        due: e.due ? yt(e.due) : void 0,
        isUrgent: e.isUrgent
      };
    case "absolute":
      return {
        ...t,
        due: yt(e.due),
        isUrgent: e.isUrgent
      };
    case "location":
      return {
        ...t,
        name: e.name,
        locLat: e.locLat,
        locLong: e.locLong,
        locTrigger: e.locTrigger,
        radius: e.radius
      };
  }
}
function ns(e) {
  const t = e.filter(
    (o) => o.type === "relative" || o.type === "absolute"
  ).length, r = e.filter((o) => o.type === "location").length;
  return { timeBasedCount: t, locationCount: r };
}
const Ie = Re.map((e) => e.key);
function ho(e) {
  if (typeof e != "string") return;
  const t = e.toLowerCase();
  return (Re.find((o) => o.key === t) ?? Re.find((o) => o.displayName.toLowerCase() === t))?.key;
}
const fo = `Color for the entity. Accepts a color key (e.g. "berry_red") or display name (e.g. "Berry Red"). Valid colors: ${Ie.join(", ")}. Unrecognized colors are omitted and charcoal will be used as the default.`, le = s.preprocess(ho, s.enum(Ie).optional()).describe(fo);
s.enum(Ie);
const ue = s.enum(Ie).optional().catch(void 0), F = s.object({
  id: s.string(),
  content: s.string().describe("Task title."),
  description: s.string(),
  dueDate: s.string().optional().describe("ISO 8601."),
  recurring: s.union([s.boolean(), s.string()]).describe("False when not recurring, otherwise the recurrence string."),
  deadlineDate: s.string().optional().describe("ISO 8601."),
  priority: Ze.describe("p1 is highest, p4 lowest."),
  projectId: s.string(),
  sectionId: s.string().optional(),
  parentId: s.string().optional(),
  labels: s.array(s.string()).optional(),
  duration: s.string().optional().describe('e.g. "2h30m".'),
  responsibleUid: s.string().optional(),
  isUncompletable: s.boolean().optional().describe("An organizational header, not a real task."),
  assignedByUid: s.string().optional(),
  checked: s.boolean().describe("Whether the task is completed."),
  completedAt: s.string().optional().describe("ISO 8601."),
  addedAt: s.string().optional().describe("ISO 8601.")
}), Q = s.object({
  id: s.string(),
  name: s.string(),
  description: s.string().describe("Empty string when none."),
  color: ue,
  isFavorite: s.boolean(),
  isShared: s.boolean(),
  parentId: s.string().optional(),
  inboxProject: s.boolean(),
  viewStyle: s.string().describe("list, board or calendar."),
  workspaceId: s.string().optional().describe("Undefined for personal projects."),
  folderId: s.string().optional().describe("Workspace projects only."),
  childOrder: s.number().describe("Ordering index among siblings."),
  isArchived: s.boolean()
}), X = s.object({
  id: s.string(),
  name: s.string(),
  sectionOrder: s.number().describe("Ordering index within the project."),
  description: s.string().optional().describe("Supports Markdown.")
});
function K({ id: e, name: t, sectionOrder: r, description: o }) {
  return { id: e, name: t, sectionOrder: r, description: o ?? void 0 };
}
const bo = s.object({
  resourceType: s.string().describe("file, url, image, etc."),
  fileName: s.string().optional(),
  fileSize: s.number().optional().describe("Bytes."),
  fileType: s.string().optional().describe("MIME type."),
  fileUrl: s.string().optional(),
  fileDuration: s.number().optional().describe("Milliseconds, for audio/video."),
  uploadState: s.enum(["pending", "completed"]).optional(),
  url: s.string().optional().describe("For link/url resource types."),
  title: s.string().optional().describe("For link/url resource types."),
  image: s.string().optional().describe("For image resource types."),
  imageWidth: s.number().optional().describe("Pixels."),
  imageHeight: s.number().optional().describe("Pixels.")
}), ye = s.object({
  id: s.string(),
  taskId: s.string().optional(),
  projectId: s.string().optional(),
  content: s.string(),
  postedAt: s.string().describe("ISO 8601."),
  postedUid: s.string().optional(),
  fileAttachment: bo.optional()
}), go = s.object({
  id: s.string().optional(),
  objectType: s.string().describe("task, project, etc."),
  objectId: s.string(),
  eventType: s.string().describe("added, updated, deleted, completed, etc."),
  eventDate: s.string().describe("ISO 8601."),
  parentProjectId: s.string().optional(),
  parentItemId: s.string().optional(),
  initiatorId: s.string().optional().describe("User who initiated the event."),
  extraData: s.record(s.string(), s.unknown()).optional()
}), yo = s.object({
  id: s.string(),
  name: s.string(),
  email: s.string()
}), pe = s.object({
  id: s.string(),
  name: s.string(),
  color: ue,
  order: s.number().optional().catch(void 0).describe("Display order."),
  isFavorite: s.boolean()
}), To = s.object({
  isRecurring: s.boolean(),
  string: s.string().describe("Human-readable due string."),
  date: s.string().describe("ISO 8601."),
  datetime: s.string().optional().describe("ISO 8601."),
  timezone: s.string().optional()
}), st = s.object({
  id: s.string(),
  taskId: s.string(),
  type: s.enum(Ns),
  minuteOffset: s.number().optional().describe("Minutes before due. Relative reminders only."),
  due: To.optional().describe("Absolute, and sometimes relative, reminders."),
  name: s.string().optional().describe("Location name. Location reminders only."),
  locLat: s.string().optional().describe("Location reminders only."),
  locLong: s.string().optional().describe("Location reminders only."),
  locTrigger: s.enum(Nt).optional().describe("Location reminders only."),
  radius: s.number().optional().describe("Geofence radius in metres. Location reminders only."),
  isUrgent: s.boolean().optional().describe("Relative and absolute reminders only.")
}), ee = s.object({
  item: s.string().describe("Usually the ID of the item that failed."),
  error: s.string(),
  code: s.string().optional()
}), ko = s.object({
  taskId: s.string().optional().describe("The ID of the task to comment on."),
  projectId: s.string().optional().describe(
    'The ID of the project to comment on. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  content: s.string().min(1).describe("The content of the comment.")
}), wo = {
  comments: s.array(ko).min(1).describe("The array of comments to add.")
}, vo = {
  comments: s.array(ye).describe("The created comments."),
  totalCount: s.number().describe("The total number of comments created."),
  addedCommentIds: s.array(s.string()).describe("The IDs of the added comments.")
}, Io = {
  name: g.ADD_COMMENTS,
  description: "Add multiple comments to tasks or projects. Each comment must specify either taskId or projectId.",
  parameters: wo,
  outputSchema: vo,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { comments: r } = e;
    for (const [l, u] of r.entries()) {
      if (!u.taskId && !u.projectId)
        throw new Error(
          `Comment ${l + 1}: Either taskId or projectId must be provided.`
        );
      if (u.taskId && u.projectId)
        throw new Error(
          `Comment ${l + 1}: Cannot provide both taskId and projectId. Choose one.`
        );
    }
    const n = r.some((l) => N(l.projectId)) ? await t.getUser() : void 0, a = r.map(async ({ content: l, taskId: u, projectId: p }) => {
      const m = await de({
        projectId: p,
        user: n,
        client: n ? void 0 : t
      });
      return await t.addComment({
        content: l,
        ...u ? { taskId: u } : { projectId: m }
      });
    }), c = (await Promise.all(a)).map(ge);
    return {
      textContent: So({ comments: c }),
      structuredContent: {
        comments: c,
        totalCount: c.length,
        addedCommentIds: c.map((l) => l.id)
      }
    };
  }
};
function So({ comments: e }) {
  const t = e.filter((a) => a.taskId).length, r = e.filter((a) => a.projectId).length, o = [];
  if (t > 0) {
    const a = t > 1 ? "comments" : "comment";
    o.push(`${t} task ${a}`);
  }
  if (r > 0) {
    const a = r > 1 ? "comments" : "comment";
    o.push(`${r} project ${a}`);
  }
  return o.length > 0 ? `Added ${o.join(" and ")}` : "No comments added";
}
const jo = {
  search: s.string().optional().describe(
    "Search for a filter by name (partial and case insensitive match). If omitted, all filters are returned."
  )
}, rt = s.object({
  id: s.string().describe("The unique ID of the filter."),
  name: s.string().describe("The name of the filter."),
  query: s.string().describe('The filter query string (e.g. "today & p1", "#Work & overdue").'),
  color: ue,
  isFavorite: s.boolean().describe("Whether the filter is marked as favorite."),
  itemOrder: s.number().describe("The display order of the filter.")
}), Co = {
  filters: s.array(rt).describe("The found filters."),
  totalCount: s.number().describe("The total number of filters returned.")
}, $o = {
  name: g.FIND_FILTERS,
  description: 'List all personal filters or search for filters by name. Filters are saved custom views that use query syntax to organize tasks (e.g. "today & p1", "#Work & overdue").',
  parameters: jo,
  outputSchema: Co,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    let o = ((await t.sync({ resourceTypes: ["filters"], syncToken: "*" })).filters ?? []).filter((i) => !i.isDeleted);
    if (e.search) {
      const i = e.search.toLowerCase();
      o = o.filter((c) => c.name.toLowerCase().includes(i));
    }
    o.sort((i, c) => i.itemOrder - c.itemOrder);
    const n = o.map((i) => ({
      id: i.id,
      name: i.name,
      query: i.query,
      color: ue.parse(i.color),
      isFavorite: i.isFavorite,
      itemOrder: i.itemOrder
    }));
    return {
      textContent: Do({ filters: n, search: e.search }),
      structuredContent: {
        filters: n,
        totalCount: n.length
      }
    };
  }
};
function Do({
  filters: e,
  search: t
}) {
  const r = t ? `Filters matching "${t}"` : "Filters";
  if (e.length === 0) {
    const n = t ? ["Try broader search terms", "Check spelling", "Remove search to see all filters"] : ["No filters created yet", `Use ${g.ADD_FILTERS} to create a filter`];
    return `${r}: 0 found

Suggestions:
${n.map((a) => `- ${a}`).join(`
`)}`;
  }
  const o = [`${r}: ${e.length} found`, ""];
  for (const n of e) {
    const a = n.isFavorite ? " ★" : "";
    o.push(`• ${n.name}${a} (id=${n.id})`), o.push(`  Query: ${n.query}`);
  }
  return o.join(`
`);
}
const xo = s.object({
  name: s.string().min(1).describe("The name of the filter."),
  query: s.string().min(1).describe(
    'The filter query string. Examples: "today & p1", "#Work & overdue", "@email & today", "(p1 | p2) & !assigned". Operators: | (OR), & (AND), ! (NOT), () grouping, , (multiple queries).'
  ),
  color: le,
  isFavorite: s.boolean().optional().describe("Whether to mark the filter as a favorite. Defaults to false.")
}), Ao = {
  filters: s.array(xo).min(1).describe("The array of filters to add.")
}, Eo = {
  filters: s.array(rt).describe("The created filters."),
  totalCount: s.number().describe("The total number of filters created.")
}, Oo = {
  name: g.ADD_FILTERS,
  description: "Add one or more new personal filters. Filters are saved custom views using query syntax to organize tasks.",
  parameters: Ao,
  outputSchema: Eo,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ filters: e }, t) {
    const r = Date.now(), o = e.map((p, m) => `tempFilterAdd${m}${r}`), n = e.map((p, m) => V(
      "filter_add",
      {
        name: p.name,
        query: p.query,
        ...p.color !== void 0 ? { color: p.color } : {},
        ...p.isFavorite !== void 0 ? { isFavorite: p.isFavorite } : {}
      },
      o[m]
    )), i = (await t.sync({ commands: n })).tempIdMapping ?? {}, c = e.map((p, m) => {
      const h = o[m], y = h !== void 0 ? i[h] : void 0;
      if (!y) return null;
      const f = p.color !== void 0 ? ue.parse(p.color) : void 0;
      return {
        id: y,
        name: p.name,
        query: p.query,
        color: f,
        isFavorite: p.isFavorite ?? !1,
        itemOrder: 0
      };
    }).filter((p) => p !== null), d = c.length, l = c.map((p) => `• ${p.name} (id=${p.id})`).join(`
`);
    return {
      textContent: `Added ${d} filter${d === 1 ? "" : "s"}:
${l}`,
      structuredContent: {
        filters: c,
        totalCount: c.length
      }
    };
  }
}, Po = s.object({
  name: s.string().min(1).max(128).describe("The name of the label."),
  color: le,
  order: s.number().int().optional().describe("The position of the label in the label list."),
  isFavorite: s.boolean().optional().describe("Whether the label is a favorite. Defaults to false.")
}), _o = {
  labels: s.array(Po).min(1).describe("The array of labels to add.")
}, Ro = {
  labels: s.array(pe).describe("The created labels."),
  totalCount: s.number().describe("The total number of labels created.")
}, Uo = {
  name: g.ADD_LABELS,
  description: "Add one or more new personal labels.",
  parameters: _o,
  outputSchema: Ro,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ labels: e }, t) {
    const r = await Promise.all(e.map((n) => t.addLabel(n)));
    return {
      textContent: Lo({ labels: r }),
      structuredContent: {
        labels: r.map((n) => pe.parse(n)),
        totalCount: r.length
      }
    };
  }
};
function Lo({ labels: e }) {
  const t = e.length, r = e.map((o) => `• ${o.name} (id=${o.id})`).join(`
`);
  return `Added ${t} label${t === 1 ? "" : "s"}:
${r}`;
}
function ie(e = /* @__PURE__ */ new Date()) {
  const t = e.getFullYear(), r = String(e.getMonth() + 1).padStart(2, "0"), o = String(e.getDate()).padStart(2, "0");
  return `${t}-${r}-${o}`;
}
function ot(e, t, r = {}) {
  const { context: o, showDetails: n = !1 } = r, a = t.length, i = [], d = `${e} ${a} ${a === 1 ? "task" : "tasks"}${o ? ` ${o}` : ""}.`;
  i.push(d);
  const l = 5;
  if (n || a <= l) {
    const u = Se(t, l);
    if (u.length > 0) {
      const p = a > l ? `, +${a - l} more` : "";
      i.push(`Tasks:
${u}${p}.`);
    }
  }
  return i.join(`
`);
}
function nt(e) {
  const { action: t, success: r, total: o, successItems: n, successLabel: a = "Completed", failures: i } = e, c = [], d = `${t}: ${r}/${o} successful.`;
  if (c.push(d), n?.length && n.length <= 5 && c.push(`${a}:
${n.map((l) => `    ${l}`).join(`
`)}.`), i?.length) {
    const l = i.length, u = `Failed (${l}):
${i.slice(0, fe.MAX_FAILURES_SHOWN).map((p) => `    ${p.item} (Error: ${p.error}${p.code ? ` [${p.code}]` : ""})`).join(
      `
`
    )}${l > fe.MAX_FAILURES_SHOWN ? `, +${l - fe.MAX_FAILURES_SHOWN} more` : ""}.`;
    c.push(u);
  }
  return c.join(`
`);
}
function at(e, t) {
  if (t.length === 0)
    return e;
  const r = t.slice(0, fe.MAX_FAILURES_SHOWN), o = t.length - r.length, n = r.map((i) => `    ${i.item}: ${i.error}`).join(`
`), a = o > 0 ? `
    +${o} more` : "";
  return `${e}
Failed (${t.length}) - address or drop these items:
${n}${a}`;
}
function No(e) {
  const t = e.content || e.title || "Untitled", r = e.dueDate ? ` • due ${e.dueDate}` : "", o = e.priority ? ` • ${e.priority.toUpperCase()}` : "", n = e.projectName ? ` • ${e.projectName}` : "", a = e.id ? ` • id=${e.id}` : "";
  return `    ${t}${r}${o}${n}${a}`;
}
function Mo(e) {
  const t = ` (${e.color})`, r = e.isFavorite ? " • ⭐" : "", o = ` • id=${e.id}`;
  return `    ${e.name}${t}${r}${o}`;
}
function Fo(e) {
  const t = e.inboxProject ? " • Inbox" : "", r = e.isFavorite ? " • ⭐" : "", o = e.isShared ? " • Shared" : "", n = e.isArchived ? " • Archived" : "", a = e.viewStyle && e.viewStyle !== "list" ? ` • ${e.viewStyle}` : "", i = ` • id=${e.id}`;
  return `    ${e.name}${t}${r}${o}${n}${a}${i}`;
}
function Se(e, t = 5) {
  const o = e.slice(0, t).map(No).join(`
`);
  if (e.length > t) {
    const n = e.length - t;
    return `${o}
    ... and ${n} more task${n === 1 ? "" : "s"}`;
  }
  return o;
}
function q({
  subject: e,
  count: t,
  limit: r,
  nextCursor: o,
  filterHints: n,
  previewLines: a,
  zeroReasonHints: i,
  nextSteps: c
}) {
  const d = [], l = `${e}: ${t}${typeof r == "number" ? ` (limit ${r})` : ""}${o ? ", more available" : ""}.`;
  return d.push(l), n?.length && d.push(`Filter: ${n.join("; ")}.`), a?.length && d.push(`Preview:
${a}`), !t && i?.length && d.push(`No results. ${i.join("; ")}.`), (c?.length || o) && d.push(as(c || [], o)), d.join(`
`);
}
function as(e, t) {
  const r = [...e];
  return t && r.push(`Pass cursor '${t}' to fetch more results.`), `${r.length === 1 ? "Possible suggested next step:" : "Possible suggested next steps:"}
${r.map((n) => `- ${n}`).join(`
`)}`;
}
const Ho = 300 * 1e3;
function Wo(e) {
  return /^\d+$/.test(e);
}
class Bo {
  constructor() {
    this.cache = null, this.inFlightWorkspaces = null;
  }
  async getWorkspaces(t) {
    if (this.cache && Date.now() - this.cache.timestamp < Ho)
      return this.cache.workspaces;
    if (this.inFlightWorkspaces)
      return this.inFlightWorkspaces;
    const r = t.getWorkspaces().then((o) => (this.cache = { workspaces: o, timestamp: Date.now() }, o)).finally(() => {
      this.inFlightWorkspaces === r && (this.inFlightWorkspaces = null);
    });
    return this.inFlightWorkspaces = r, r;
  }
  /**
   * Resolve a workspace name or ID to a workspace ID and name.
   *
   * Resolution order:
   * 1. Exact ID match (always tried first for any input)
   * 2. If input looks like an ID (numeric) but wasn't found, pass through as-is (API validates)
   * 3. Exact case-insensitive name match
   * 4. Unique partial case-insensitive name match
   * 5. Multiple partial matches → throw ambiguous error
   * 6. No match → throw not-found error
   */
  async resolveWorkspace(t, r) {
    const o = r.trim();
    if (!o)
      throw new Error("Workspace reference cannot be empty");
    const n = await this.getWorkspaces(t), a = n.find((u) => u.id === o);
    if (a)
      return { workspaceId: a.id, workspaceName: a.name };
    if (Wo(o))
      return { workspaceId: o, workspaceName: o };
    const i = o.toLowerCase(), c = n.find((u) => u.name.toLowerCase() === i);
    if (c)
      return { workspaceId: c.id, workspaceName: c.name };
    const d = n.filter((u) => u.name.toLowerCase().includes(i)), l = d.length === 1 ? d[0] : void 0;
    if (l)
      return { workspaceId: l.id, workspaceName: l.name };
    if (d.length > 1) {
      const u = d.slice(0, 5).map((p) => `  - "${p.name}" (id: ${p.id})`).join(`
`);
      throw new Error(
        `Ambiguous workspace reference "${o}". Multiple workspaces match:
${u}` + (d.length > 5 ? `
  ... and ${d.length - 5} more` : "")
      );
    }
    throw new Error(
      `Workspace "${o}" not found. Use list-workspaces to see available workspaces.`
    );
  }
  /**
   * Clear the workspace cache — useful for testing.
   */
  clearCache() {
    this.cache = null;
  }
}
const is = new Bo(), zo = s.object({
  name: s.string().min(1).describe("The name of the project."),
  parentId: s.string().optional().describe("The ID of the parent project. If provided, creates this as a sub-project."),
  isFavorite: s.boolean().optional().describe("Whether the project is a favorite. Defaults to false."),
  viewStyle: s.enum(["list", "board", "calendar"]).optional().describe('The project view style. Defaults to "list".'),
  description: s.string().optional().describe("The description of the project. Supports Markdown."),
  color: le,
  workspace: s.string().trim().min(1).optional().describe(
    "The workspace to create the project in. Accepts a workspace name or workspace ID. If not provided, creates a personal project. Use list-workspaces to see available workspaces."
  )
}), Ko = {
  projects: s.array(zo).min(1).describe("The array of projects to add.")
}, qo = {
  projects: s.array(Q).describe("The created projects."),
  totalCount: s.number().describe("The total number of projects created."),
  failures: s.array(ee).describe(
    "Projects that could not be created, with the reason for each. A failure here does not affect the other projects in the batch — do not retry the whole batch; address or drop the failed items."
  ),
  totalRequested: s.number().describe("The total number of projects requested."),
  successCount: s.number().describe("The number of successfully created projects."),
  failureCount: s.number().describe("The number of failed project creations.")
}, Yo = {
  name: g.ADD_PROJECTS,
  description: "Add one or more new projects.",
  parameters: Ko,
  outputSchema: qo,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ projects: e }, t) {
    const r = /* @__PURE__ */ new Map(), o = (l) => {
      if (!l)
        return Promise.resolve(void 0);
      let u = r.get(l);
      return u || (u = O(
        () => is.resolveWorkspace(t, l)
      ).then(({ workspaceId: p }) => p), r.set(l, u)), u;
    }, n = await Promise.allSettled(
      e.map(async ({ workspace: l, ...u }) => {
        const p = await o(l);
        return await O(
          () => t.addProject({ ...u, ...p ? { workspaceId: p } : {} })
        );
      })
    ), a = [], i = [];
    n.forEach((l, u) => {
      l.status === "fulfilled" ? a.push(l.value) : i.push({
        item: e[u]?.name ?? `Project ${u + 1}`,
        error: z(l.reason)
      });
    });
    const c = a.map(Z);
    return {
      textContent: Go({ projects: a, failures: i }),
      structuredContent: {
        projects: c,
        totalCount: c.length,
        failures: i,
        totalRequested: e.length,
        successCount: c.length,
        failureCount: i.length
      }
    };
  }
};
function Go({
  projects: e,
  failures: t
}) {
  const r = e.length, o = e.map((a) => `• ${a.name} (id=${a.id})`).join(`
`), n = r > 0 ? `Added ${r} project${r === 1 ? "" : "s"}:
${o}` : "Added 0 projects";
  return at(n, t);
}
const we = 25, je = s.enum(Ms), cs = s.object({
  date: s.string().optional().describe("Due date in YYYY-MM-DD format."),
  string: s.string().optional().describe('Natural language due string, e.g. "tomorrow at 3pm".'),
  timezone: s.string().optional().describe('Timezone for the reminder, e.g. "America/New_York".'),
  lang: s.string().optional().describe('Language for parsing the due string, e.g. "en".')
}), ds = s.enum(Nt), Ce = s.boolean().optional().describe("Whether this is an urgent reminder. Applies to relative and absolute reminders."), Vo = s.object({
  type: s.literal("relative"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  minuteOffset: s.number().int().min(0).describe(
    "Minutes before the task due time to trigger the reminder. E.g., 30 for 30 minutes before, 60 for 1 hour before, 1440 for 1 day before."
  ),
  service: je.optional().describe(
    'Delivery method: "email" or "push" notification. Defaults to push.'
  ),
  isUrgent: Ce
}), Jo = s.object({
  type: s.literal("absolute"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  due: cs.describe("The specific date/time for the reminder."),
  service: je.optional().describe(
    'Delivery method: "email" or "push" notification. Defaults to push.'
  ),
  isUrgent: Ce
}), Xo = s.object({
  type: s.literal("location"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  name: s.string().min(1).describe('Name of the location, e.g. "Office", "Home".'),
  locLat: s.string().describe('Latitude of the location as a string, e.g. "37.7749".'),
  locLong: s.string().describe('Longitude of the location as a string, e.g. "-122.4194".'),
  locTrigger: ds.describe(
    'When to trigger: "on_enter" (arriving) or "on_leave" (departing).'
  ),
  radius: s.number().int().optional().describe("Radius in meters for the geofence. Defaults to server default.")
}), Zo = s.discriminatedUnion("type", [
  Vo,
  Jo,
  Xo
]), Qo = {
  reminders: s.array(Zo).min(1).max(we).describe(
    `Array of reminders to create (max ${we}). Each reminder must specify a type: "relative" (minutes before due), "absolute" (specific date/time), or "location" (geofence trigger).`
  )
}, en = {
  reminders: s.array(st).describe("The created reminders."),
  totalCount: s.number().describe("Total number of reminders created."),
  addedReminderIds: s.array(s.string()).describe("IDs of the created reminders.")
}, tn = {
  name: g.ADD_REMINDERS,
  description: 'Add reminders to tasks. Supports three types: "relative" (minutes before due), "absolute" (specific date/time), or "location" (geofence-triggered). Each reminder must specify a taskId.',
  parameters: Qo,
  outputSchema: en,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { reminders: r } = e, o = r.map(async (c) => {
      switch (c.type) {
        case "relative":
          return await t.addReminder({
            taskId: c.taskId,
            reminderType: "relative",
            minuteOffset: c.minuteOffset,
            service: c.service,
            isUrgent: c.isUrgent
          });
        case "absolute":
          return await t.addReminder({
            taskId: c.taskId,
            reminderType: "absolute",
            due: c.due,
            service: c.service,
            isUrgent: c.isUrgent
          });
        case "location":
          return await t.addLocationReminder({
            taskId: c.taskId,
            name: c.name,
            locLat: c.locLat,
            locLong: c.locLong,
            locTrigger: c.locTrigger,
            radius: c.radius
          });
      }
    }), a = (await Promise.all(o)).map(be);
    return {
      textContent: sn(a),
      structuredContent: {
        reminders: a,
        totalCount: a.length,
        addedReminderIds: a.map((c) => c.id)
      }
    };
  }
};
function sn(e) {
  const { timeBasedCount: t, locationCount: r } = ns(e), o = [];
  if (t > 0) {
    const n = t > 1 ? "reminders" : "reminder";
    o.push(`${t} time-based ${n}`);
  }
  if (r > 0) {
    const n = r > 1 ? "reminders" : "reminder";
    o.push(`${r} location ${n}`);
  }
  return o.length > 0 ? `Added ${o.join(" and ")}` : "No reminders added";
}
const rn = s.object({
  name: s.string().min(1).describe("The name of the section."),
  projectId: s.string().min(1).describe(
    'The ID of the project to add the section to. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  description: s.string().optional().describe("The description of the section. Supports Markdown.")
}), on = {
  sections: s.array(rn).min(1).describe("The array of sections to add.")
}, nn = {
  sections: s.array(X).describe("The created sections."),
  totalCount: s.number().describe("The total number of sections created."),
  failures: s.array(ee).describe(
    "Sections that could not be created, with the reason for each. A failure here does not affect the other sections in the batch — do not retry the whole batch; address or drop the failed items."
  ),
  totalRequested: s.number().describe("The total number of sections requested."),
  successCount: s.number().describe("The number of successfully created sections."),
  failureCount: s.number().describe("The number of failed section creations.")
}, an = {
  name: g.ADD_SECTIONS,
  description: "Add one or more new sections to projects.",
  parameters: on,
  outputSchema: nn,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ sections: e }, t) {
    const r = e.some((c) => N(c.projectId)) ? O(() => t.getUser()) : void 0, o = await Promise.allSettled(
      e.map(async (c) => {
        const d = N(c.projectId) ? await r : void 0, l = await de({
          projectId: c.projectId,
          user: d,
          client: void 0
        }) ?? c.projectId;
        return O(() => t.addSection({ ...c, projectId: l }));
      })
    ), n = [], a = [];
    return o.forEach((c, d) => {
      c.status === "fulfilled" ? n.push(c.value) : a.push({
        item: e[d]?.name ?? `Section ${d + 1}`,
        error: z(c.reason)
      });
    }), {
      textContent: cn({ sections: n, failures: a }),
      structuredContent: {
        sections: n.map(K),
        totalCount: n.length,
        failures: a,
        totalRequested: e.length,
        successCount: n.length,
        failureCount: a.length
      }
    };
  }
};
function cn({
  sections: e,
  failures: t
}) {
  const r = e.length, o = e.map((a) => `• ${a.name} (id=${a.id}, projectId=${a.projectId})`).join(`
`), n = r > 0 ? `Added ${r} section${r === 1 ? "" : "s"}:
${o}` : "Added 0 sections";
  return at(n, t);
}
const ne = {
  USER_NOT_FOUND: "USER_NOT_FOUND",
  USER_NOT_COLLABORATOR: "USER_NOT_COLLABORATOR",
  PROJECT_NOT_SHARED: "PROJECT_NOT_SHARED",
  TASK_NOT_ACCESSIBLE: "TASK_NOT_ACCESSIBLE",
  PERMISSION_DENIED: "PERMISSION_DENIED",
  PROJECT_NOT_FOUND: "PROJECT_NOT_FOUND",
  TASK_NOT_FOUND: "TASK_NOT_FOUND"
};
class dn {
  /**
   * Validate a single assignment operation
   */
  async validateAssignment(t, r) {
    const { taskId: o, projectId: n, responsibleUid: a } = r, i = await W.resolveUser(t, a);
    if (!i)
      return {
        isValid: !1,
        taskId: o,
        projectId: n,
        error: {
          type: ne.USER_NOT_FOUND,
          message: `User "${a}" not found`,
          suggestions: [
            "Check the spelling of the user name or email",
            "Ensure the user is a collaborator on at least one shared project",
            "Try using the user's full email address"
          ]
        }
      };
    try {
      const c = await t.getProject(n);
      if (!c.isShared)
        return {
          isValid: !1,
          taskId: o,
          projectId: n,
          resolvedUser: i,
          error: {
            type: ne.PROJECT_NOT_SHARED,
            message: `Project "${c.name}" is not shared. Task assignments require a shared project with collaborators. Share the project with at least one other person to enable assignments.`,
            suggestions: [
              "Share the project with at least one collaborator to enable assignments",
              "Use find-project-collaborators to check project sharing status"
            ]
          }
        };
      if (!await W.validateProjectCollaborator(
        t,
        n,
        i.userId
      ))
        return {
          isValid: !1,
          taskId: o,
          projectId: n,
          resolvedUser: i,
          error: {
            type: ne.USER_NOT_COLLABORATOR,
            message: `User "${i.displayName}" is not a collaborator on project "${c.name}"`,
            suggestions: [
              "Invite the user to collaborate on this project first",
              "Check if the user has been removed from the project",
              "Verify you have permission to see project collaborators"
            ]
          }
        };
      if (o)
        try {
          await t.getTask(o);
        } catch {
          return {
            isValid: !1,
            taskId: o,
            projectId: n,
            resolvedUser: i,
            error: {
              type: ne.TASK_NOT_FOUND,
              message: `Task "${o}" not found or not accessible`,
              suggestions: [
                "Verify the task ID is correct",
                "Check if the task has been deleted",
                "Ensure you have access to the task"
              ]
            }
          };
        }
      return {
        isValid: !0,
        taskId: o,
        projectId: n,
        resolvedUser: i
      };
    } catch {
      return {
        isValid: !1,
        taskId: o,
        projectId: n,
        resolvedUser: i,
        error: {
          type: ne.PERMISSION_DENIED,
          message: "Permission denied or API error occurred",
          suggestions: [
            "Check your API permissions",
            "Verify you have access to the project",
            "Try again later if this is a temporary API issue"
          ]
        }
      };
    }
  }
  /**
   * Validate multiple assignment operations in bulk
   */
  async validateBulkAssignment(t, r) {
    const o = r.map(
      (n) => this.validateAssignment(t, n)
    );
    return Promise.all(o);
  }
  /**
   * Validate assignment for task creation (no taskId required)
   */
  async validateTaskCreationAssignment(t, r, o) {
    return this.validateAssignment(t, {
      projectId: r,
      responsibleUid: o
    });
  }
  /**
   * Validate assignment for task update
   */
  async validateTaskUpdateAssignment(t, r, o) {
    if (o === null)
      return {
        isValid: !0,
        taskId: r
      };
    try {
      const n = await t.getTask(r);
      return this.validateAssignment(t, {
        taskId: r,
        projectId: n.projectId,
        responsibleUid: o
      });
    } catch {
      return {
        isValid: !1,
        taskId: r,
        error: {
          type: ne.TASK_NOT_FOUND,
          message: `Task "${r}" not found or not accessible`,
          suggestions: [
            "Verify the task ID is correct",
            "Check if the task has been deleted",
            "Ensure you have access to the task"
          ]
        }
      };
    }
  }
  /**
   * Get detailed assignment eligibility information for troubleshooting
   */
  async getAssignmentEligibility(t, r, o, n) {
    const a = [];
    let i = !1, c;
    try {
      c = await t.getProject(r);
    } catch {
      return {
        canAssign: !1,
        projectInfo: {
          name: "Unknown",
          isShared: !1,
          collaboratorCount: 0
        },
        recommendations: ["Project not found or not accessible"]
      };
    }
    const d = await W.getProjectCollaborators(t, r), l = {
      name: c.name,
      isShared: c.isShared,
      collaboratorCount: d.length
    };
    if (!c.isShared)
      return a.push("Share this project to enable task assignments"), { canAssign: !1, projectInfo: l, recommendations: a };
    const u = await W.resolveUser(t, o);
    if (!u)
      return a.push("User not found - check spelling or invite to a shared project"), { canAssign: !1, projectInfo: l, recommendations: a };
    const p = d.some((y) => y.id === u.userId), m = {
      resolvedName: u.displayName,
      isCollaborator: p
    };
    if (!p)
      return a.push(
        `Invite ${u.displayName} to collaborate on project "${c.name}"`
      ), { canAssign: !1, projectInfo: l, userInfo: m, recommendations: a };
    let h;
    if (n && n.length > 0) {
      let y = 0, f = 0;
      for (const v of n)
        try {
          await t.getTask(v), y++;
        } catch {
          f++;
        }
      h = { accessibleTasks: y, inaccessibleTasks: f }, f > 0 && a.push(`${f} task(s) are not accessible`);
    }
    return i = !0, a.push(`Ready to assign tasks to ${u.displayName}`), {
      canAssign: i,
      projectInfo: l,
      userInfo: m,
      taskInfo: h,
      recommendations: a
    };
  }
}
const it = new dn();
function U(e) {
  return s.string().transform((t) => t === "" ? void 0 : t).optional().describe(e);
}
const Tt = Kt.TASKS_PER_OPERATION, ln = s.object({
  content: s.string().min(1).describe(
    'The task name/title. Should be concise and actionable (e.g., "Review PR #123", "Call dentist"). For longer content, use the description field instead. Supports Markdown.'
  ),
  description: U(
    "Additional details, notes, or context for the task. Use this for longer content rather than putting it in the task name. Supports Markdown."
  ),
  priority: Ze.optional().describe(Xe),
  dueString: U(
    'The due date for the task, in natural language. Also use natural language for recurrences; do not prefix it with "recurring".'
  ),
  deadlineDate: U(
    'The deadline date for the task in ISO 8601 format (YYYY-MM-DD, e.g., "2025-12-31"). Deadlines are immovable constraints shown with a different indicator than due dates.'
  ),
  duration: U(
    'The duration of the task. Use format: "2h" (hours), "90m" (minutes), "2h30m" (combined), or "1.5h" (decimal hours). Max 24h.'
  ),
  labels: s.array(s.string()).optional().describe("The labels to attach to the task."),
  projectId: U(
    'The project ID to add this task to. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: U("The section ID to add this task to."),
  parentId: U("The parent task ID (for subtasks)."),
  order: s.number().optional().describe("Position of the task among sibling tasks under the same parent/section."),
  responsibleUser: U(
    'Assign task to this user. Can be "me" (assigns to current user), a user ID, name, or email address. User must be a collaborator on the target project.'
  ),
  isUncompletable: s.boolean().optional().describe(
    "Whether this task should be uncompletable (organizational header). Tasks with isUncompletable: true appear as organizational headers and cannot be completed."
  )
}), kt = {
  tasks: s.array(ln).min(1).max(Tt).describe(`The array of tasks to add (max ${Tt}).`)
}, un = {
  tasks: s.array(F).describe("The created tasks."),
  totalCount: s.number().describe("The total number of tasks created."),
  failures: s.array(ee).describe("Failed task creations with error details."),
  totalRequested: s.number().describe("The total number of tasks requested."),
  successCount: s.number().describe("The number of successfully created tasks."),
  failureCount: s.number().describe("The number of failed task creations.")
}, pn = {
  name: g.ADD_TASKS,
  description: "Add one or more tasks to a project, section, or parent. Supports assignment to project collaborators.",
  parameters: kt,
  outputSchema: un,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: r } = s.object(kt).parse(e), o = /* @__PURE__ */ new Map();
    r.forEach((m, h) => {
      const y = mn(m), f = o.get(y);
      f ? f.push({ task: m, index: h }) : o.set(y, [{ task: m, index: h }]);
    });
    const n = /* @__PURE__ */ new Map(), i = (await Promise.all(
      [...o.values()].map(async (m) => {
        const h = [];
        for (const { task: y, index: f } of m)
          try {
            const v = await hn(y, t, n);
            h.push({ index: f, result: { status: "fulfilled", value: v } });
          } catch (v) {
            h.push({
              index: f,
              result: { status: "rejected", reason: v }
            });
          }
        return h;
      })
    )).flat().sort((m, h) => m.index - h.index), c = [], d = [];
    let l = !1;
    for (const { index: m, result: h } of i)
      h.status === "fulfilled" ? c.push(h.value) : (l ||= h.reason instanceof ve, d.push({
        item: r[m]?.content ?? `Task ${m + 1}`,
        // Keep API error signals (status/code/tag) — `error.message`
        // alone collapses e.g. a full-project rejection into a bare
        // "HTTP 403: Forbidden"
        error: z(h.reason)
      }));
    if (c.length === 0 && d.length > 0) {
      const m = d.map((y) => `"${y.item}": ${y.error}`).join("; "), h = `All ${d.length} task(s) failed to create: ${m}`;
      throw l ? new zt(h) : new Error(h);
    }
    const u = c.map(P);
    return {
      textContent: fn({
        tasks: u,
        failures: d,
        args: { tasks: r }
      }),
      structuredContent: {
        tasks: u,
        totalCount: u.length,
        failures: d,
        totalRequested: r.length,
        successCount: c.length,
        failureCount: d.length
      }
    };
  }
};
function mn(e) {
  return `${e.projectId ?? ""}|${e.sectionId ?? ""}|${e.parentId ?? ""}`;
}
async function hn(e, t, r) {
  const {
    duration: o,
    projectId: n,
    sectionId: a,
    parentId: i,
    order: c,
    responsibleUser: d,
    priority: l,
    labels: u,
    deadlineDate: p,
    ...m
  } = e, h = N(n) ? void 0 : n;
  if (h) {
    const f = await t.getProject(h);
    if (f.isArchived)
      throw new Error(
        `Task "${e.content}": Cannot create task in archived project "${f.name}"`
      );
  }
  let y = {
    ...m,
    projectId: h,
    sectionId: a,
    parentId: i,
    order: c,
    labels: u,
    deadlineDate: p
  };
  if (l && (y.priority = Zt(l)), d && !h && !a && !i)
    throw new Error(
      `Task "${e.content}": Cannot assign tasks without specifying project context. Please specify a projectId, sectionId, or parentId.`
    );
  if (o)
    try {
      const { minutes: f } = Xt(o);
      y = {
        ...y,
        duration: f,
        durationUnit: "minute"
      };
    } catch (f) {
      throw f instanceof _ ? new Error(`Task "${e.content}": ${f.message}`) : f;
    }
  if (d) {
    let f = h;
    if (!f && i)
      try {
        f = (await t.getTask(i)).projectId;
      } catch {
        throw new Error(`Task "${e.content}": Parent task "${i}" not found`);
      }
    else if (!f && a && (f = r.get(a), !f)) {
      const j = await t.getSection(a);
      if (!j)
        throw new Error(`Task "${e.content}": Section "${a}" not found`);
      f = j.projectId, r.set(a, j.projectId);
    }
    if (!f)
      throw new Error(
        `Task "${e.content}": Cannot determine target project for assignment validation`
      );
    const v = await it.validateTaskCreationAssignment(
      t,
      f,
      d
    );
    if (!v.isValid) {
      const j = v.error?.message || "Assignment validation failed", A = v.error?.suggestions?.join(". ") || "";
      throw new Error(
        `Task "${e.content}": ${j}${A ? `. ${A}` : ""}`
      );
    }
    y.assigneeId = v.resolvedUser?.userId;
  }
  try {
    return await t.addTask(y);
  } catch (f) {
    const v = rr(f, {
      taskContent: e.content,
      dueString: e.dueString,
      deadlineDate: e.deadlineDate
    });
    throw v ? new ve(v) : f;
  }
}
function fn({
  tasks: e,
  failures: t,
  args: r
}) {
  const o = /* @__PURE__ */ new Set();
  for (const a of r.tasks)
    a.projectId && !N(a.projectId) ? o.add("projects") : a.sectionId ? o.add("sections") : a.parentId ? o.add("subtasks") : o.add("inbox");
  let n = "";
  if (o.size === 1) {
    const a = Array.from(o)[0];
    n = a === "inbox" ? "" : `to ${a}`;
  } else o.size > 1 && (n = "to multiple contexts");
  return t.length > 0 ? nt({
    action: `Added tasks${n ? ` ${n}` : ""}`,
    success: e.length,
    total: r.tasks.length,
    successItems: e.map((a) => a.content ?? "Untitled"),
    successLabel: "Created",
    failures: t
  }) : ot("Added", e, {
    context: n,
    showDetails: !0
  });
}
const bn = {
  projectId: s.string().min(1).describe(
    "The ID of the project to analyze. This triggers a new health analysis which may take some time to complete."
  )
}, gn = {
  projectId: s.string().describe("The project ID."),
  health: s.object({
    status: s.enum(Ge).describe("The health status after triggering analysis."),
    isStale: s.boolean().describe("Whether the health data is still stale after the request."),
    updateInProgress: s.boolean().describe("Whether an analysis update is currently in progress.")
  }).describe("The health response returned after triggering analysis."),
  message: s.string().describe("A human-readable message about the analysis status.")
}, yn = {
  name: g.ANALYZE_PROJECT_HEALTH,
  description: "Trigger a new health analysis for a project. Use this when the health data is stale or you want a fresh assessment. The analysis may take time to complete — use get-project-health afterward to see updated results.",
  parameters: bn,
  outputSchema: gn,
  annotations: {
    readOnlyHint: !1,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: r } = e, o = await t.analyzeProjectHealth(r), n = o.updateInProgress ? "Health analysis is in progress. Use get-project-health shortly to see updated results." : `Health analysis complete. Current status: ${o.status}.`;
    return {
      textContent: `# Analyze Project Health

${n}`,
      structuredContent: {
        projectId: r,
        health: {
          status: o.status,
          isStale: o.isStale,
          updateInProgress: o.updateInProgress
        },
        message: n
      }
    };
  }
}, Tn = {
  ids: s.array(s.string().min(1)).min(1).describe("The IDs of the tasks to complete.")
}, kn = {
  completed: s.array(s.string()).describe("The IDs of successfully completed tasks."),
  failures: s.array(ee).describe("Failed task completions with error details."),
  totalRequested: s.number().describe("The total number of tasks requested to complete."),
  successCount: s.number().describe("The number of successfully completed tasks."),
  failureCount: s.number().describe("The number of failed task completions.")
}, wn = {
  name: g.COMPLETE_TASKS,
  description: "Complete one or more tasks by their IDs.",
  parameters: Tn,
  outputSchema: kn,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const r = [], o = [];
    for (const a of e.ids)
      try {
        await t.closeTask(a), r.push(a);
      } catch (i) {
        const c = i instanceof Error ? i.message : "Unknown error";
        o.push({
          item: a,
          error: c
        });
      }
    return {
      textContent: vn({
        completed: r,
        failures: o,
        args: e
      }),
      structuredContent: {
        completed: r,
        failures: o,
        totalRequested: e.ids.length,
        successCount: r.length,
        failureCount: o.length
      }
    };
  }
};
function vn({
  completed: e,
  failures: t,
  args: r
}) {
  return nt({
    action: "Completed tasks",
    success: e.length,
    total: r.ids.length,
    successItems: e,
    failures: t
  });
}
const ls = [
  "project",
  "section",
  "task",
  "comment",
  "label",
  "filter",
  "reminder",
  "location_reminder"
], In = {
  type: s.enum(ls).describe("The type of entity to delete."),
  id: s.string().min(1).describe("The ID of the entity to delete.")
}, Sn = {
  deletedEntity: s.object({
    type: s.enum(ls).describe("The type of deleted entity."),
    id: s.string().describe("The ID of the deleted entity.")
  }).describe("Information about the deleted entity."),
  success: s.boolean().describe("Whether the deletion was successful.")
}, jn = {
  name: g.DELETE_OBJECT,
  description: "Delete a project, section, task, comment, label, filter, reminder, or location_reminder by its ID. Projects can be deleted whether active or archived (find archived ones via find-projects with archivedStatus); note a workspace project must be archived before it can be deleted, while personal projects can be deleted regardless.",
  parameters: In,
  outputSchema: Sn,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !0 },
  async execute(e, t) {
    switch (e.type) {
      case "project": {
        const r = await t.getProject(e.id);
        if (ke(r) && !r.isArchived)
          throw new Error(
            `Workspace project "${r.name}" must be archived before it can be deleted. Archive the project first, then delete it.`
          );
        await t.deleteProject(e.id);
        break;
      }
      case "section":
        await t.deleteSection(e.id);
        break;
      case "task":
        await t.deleteTask(e.id);
        break;
      case "comment":
        await t.deleteComment(e.id);
        break;
      case "label":
        await t.deleteLabel(e.id);
        break;
      case "filter":
        await t.sync({
          commands: [V("filter_delete", { id: e.id })]
        });
        break;
      case "reminder":
        await t.deleteReminder(e.id);
        break;
      case "location_reminder":
        await t.deleteLocationReminder(e.id);
        break;
    }
    return {
      textContent: `Deleted ${e.type}: id=${e.id}`,
      structuredContent: {
        deletedEntity: { type: e.type, id: e.id },
        success: !0
      }
    };
  }
}, wt = 5, us = ["file", "url"], Cn = {
  projectId: s.string().min(1).describe("The ID of the project to export as a template."),
  format: s.enum(us).default("file").describe(
    'How to return the template. "file" returns the CSV content, which can be passed straight back to import-project-template. "url" returns a shareable download link instead, and is the better choice for large projects because it does not return the whole file.'
  ),
  useRelativeDates: s.boolean().optional().describe(
    'Export due dates relative to the import date (e.g. "day 3") instead of absolute dates. Defaults to false.'
  )
}, $n = {
  format: s.enum(us).describe("The format the template was exported in."),
  content: s.string().optional().describe('The template as CSV content. Only present when format is "file".'),
  lineCount: s.number().optional().describe('Number of rows in the exported CSV. Only present when format is "file".'),
  fileName: s.string().optional().describe('The generated template file name. Only present when format is "url".'),
  fileUrl: s.string().optional().describe('The shareable download URL. Only present when format is "url".')
}, Dn = {
  name: g.EXPORT_PROJECT_TEMPLATE,
  description: "Export an existing project as a Todoist template, either as CSV content or as a shareable URL. Use it to duplicate a project, share its structure, or hand the CSV to import-project-template. To read a project rather than export it, use find-tasks instead — it returns structured tasks rather than raw CSV. Nothing is modified.",
  parameters: Cn,
  outputSchema: $n,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  // `format` is defaulted here as well as in the schema: scripts/run-tool.ts calls
  // execute() with raw JSON, so schema defaults are not applied on that path.
  async execute({ projectId: e, format: t = "file", useRelativeDates: r }, o) {
    if (t === "url") {
      const { fileName: i, fileUrl: c } = await o.exportTemplateAsUrl({
        projectId: e,
        useRelativeDates: r
      });
      return {
        textContent: `Exported project (id=${e}) as template URL: ${i}
${c}`,
        structuredContent: { format: t, fileName: i, fileUrl: c }
      };
    }
    const n = await o.exportTemplateAsFile({ projectId: e, useRelativeDates: r }), a = n.split(`
`).filter((i) => i.trim().length > 0);
    return {
      // The full CSV already ships in structuredContent; a preview here keeps a large
      // project from being spelled out twice in the response.
      textContent: xn({ projectId: e, lines: a }),
      structuredContent: { format: t, content: n, lineCount: a.length }
    };
  }
};
function xn({ projectId: e, lines: t }) {
  const r = `Exported project (id=${e}) as a template file (${t.length} row${t.length === 1 ? "" : "s"}).`, o = t.slice(0, wt).join(`
`), n = t.length - wt;
  return n > 0 ? `${r}

${o}
… ${n} more row${n === 1 ? "" : "s"} in the full content.` : `${r}

${o}`;
}
const An = F.pick({
  id: !0,
  content: !0,
  dueDate: !0,
  checked: !0
}).extend({
  hasChildren: s.boolean().describe(
    "Whether this subtask has subtasks of its own. Fetch it with includeChildren to expand."
  )
}), En = Q.pick({ id: !0, name: !0 }).extend({
  hasChildren: s.boolean().describe(
    "Whether this sub-project has sub-projects of its own. Fetch it with includeChildren to expand."
  )
}), On = {
  childCount: s.number().optional().describe(
    "The number of direct children listed in children. Only present when children were requested and the type supports them. 0 means the object definitively has none."
  ),
  children: s.array(s.union([An, En])).optional().describe(
    "Direct children only: subtasks for a task, sub-projects for a project. Completed subtasks and archived sub-projects are excluded."
  ),
  hasMoreChildren: s.boolean().optional().describe(
    `Present when the object has more than ${C.CHILDREN_MAX} direct children and the list was truncated. Page through the rest with find-tasks by parentId for subtasks, or find-projects for sub-projects.`
  ),
  childrenError: s.string().optional().describe(
    'Present when the children lookup failed or returned incomplete information. When no children are listed alongside it, childCount is unknown - do not read its absence as "no children".'
  )
};
async function Pn(e, t) {
  const { results: r, nextCursor: o } = await e.getTasks({
    parentId: t,
    limit: C.CHILDREN_MAX
  }), n = await Promise.allSettled(
    r.map(({ id: i }) => e.getTasks({ parentId: i, limit: 1 }))
  ), a = n.filter((i) => i.status === "rejected").length;
  return {
    childCount: r.length,
    children: r.map((i, c) => {
      const d = n[c];
      return {
        id: i.id,
        content: i.content,
        dueDate: i.due?.date,
        checked: i.checked,
        hasChildren: d?.status === "fulfilled" && d.value.results.length > 0
      };
    }),
    hasMoreChildren: o ? !0 : void 0,
    childrenError: a ? `Could not check ${a} of ${r.length} subtasks for subtasks of their own; those are reported as hasChildren=false.` : void 0
  };
}
async function _n(e, t) {
  if (!B(t))
    return { childCount: 0, children: [] };
  const r = await os(e), o = /* @__PURE__ */ new Map();
  for (const i of r) {
    if (!B(i) || !i.parentId) continue;
    const c = o.get(i.parentId) ?? [];
    c.push(i), o.set(i.parentId, c);
  }
  const n = [...o.get(t.id) ?? []].sort((i, c) => i.childOrder - c.childOrder), a = n.slice(0, C.CHILDREN_MAX);
  return {
    childCount: a.length,
    children: a.map((i) => ({
      id: i.id,
      name: i.name,
      hasChildren: (o.get(i.id)?.length ?? 0) > 0
    })),
    hasMoreChildren: n.length > a.length ? !0 : void 0
  };
}
async function vt(e) {
  try {
    return await e();
  } catch (t) {
    return { childrenError: t instanceof Error ? t.message : String(t) };
  }
}
function It(e, t) {
  if (t.childCount === void 0)
    return t.childrenError ? ` • ${e}=unavailable` : "";
  const r = t.hasMoreChildren ? "+" : "", o = t.childrenError ? " (partial)" : "";
  return ` • ${e}=${t.childCount}${r}${o}`;
}
const ps = ["task", "project", "comment", "section"], Rn = {
  type: s.enum(ps).describe("The type of object to fetch."),
  id: s.string().min(1).describe("The unique ID of the object to fetch."),
  includeChildren: s.boolean().optional().describe(
    "Also return the direct children of the object: subtasks for a task, sub-projects for a project. Returns childCount plus a compact list, flagging each child that has children of its own. Use this to check whether a task hides subtasks instead of a speculative find-tasks lookup. Ignored for comments and sections."
  )
}, Un = {
  type: s.enum(ps).describe("The type of object fetched."),
  id: s.string().describe("The ID of the fetched object."),
  object: s.union([F, Q, ye, X]).describe("The fetched object data."),
  ...On
}, Ln = {
  name: g.FETCH_OBJECT,
  description: "Fetch a single task, project, comment, or section by its ID. Use this when you have a specific object ID and want to retrieve its full details. Set includeChildren to also get its direct subtasks or sub-projects.",
  parameters: Rn,
  outputSchema: Un,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { type: r, id: o, includeChildren: n } = e;
    try {
      switch (r) {
        case "task": {
          const [a, i] = await Promise.all([
            t.getTask(o),
            n ? vt(() => Pn(t, o)) : Promise.resolve({})
          ]), c = P(a);
          return {
            textContent: `Found task: ${c.content} • id=${c.id} • priority=${c.priority} • project=${c.projectId}${It("subtasks", i)}`,
            structuredContent: {
              type: r,
              id: o,
              object: c,
              ...i
            }
          };
        }
        case "project": {
          const a = await t.getProject(o), i = Z(a), c = n ? await vt(() => _n(t, a)) : {};
          return {
            textContent: `Found project: ${i.name} • id=${i.id} • color=${i.color} • viewStyle=${i.viewStyle}${It("subProjects", c)}`,
            structuredContent: {
              type: r,
              id: o,
              object: i,
              ...c
            }
          };
        }
        case "comment": {
          const a = await t.getComment(o), i = ge(a), c = i.content.length > 50 ? `${i.content.substring(0, 50)}...` : i.content;
          return {
            textContent: `Found comment • id=${i.id} • content="${c}" • posted=${i.postedAt}`,
            structuredContent: {
              type: r,
              id: o,
              object: i
            }
          };
        }
        case "section": {
          const a = await t.getSection(o);
          if (!a)
            throw new Error(`Section ${o} not found.`);
          const i = K(a);
          return {
            textContent: `Found section: ${i.name} • id=${i.id}`,
            structuredContent: {
              type: r,
              id: o,
              object: i
            }
          };
        }
      }
    } catch (a) {
      throw new Error(
        `Failed to fetch ${r} with id ${o}: ${a instanceof Error ? a.message : String(a)}`
      );
    }
  }
}, Nn = {
  id: s.string().min(1).describe(
    'A unique identifier for the document in the format "task:{id}" or "project:{id}".'
  )
}, Mn = {
  id: s.string().describe("The ID of the fetched document."),
  title: s.string().describe("The title of the document."),
  text: s.string().describe("The text content of the document."),
  url: s.string().describe("The URL of the document."),
  metadata: s.record(s.string(), s.unknown()).optional().describe("Additional metadata about the document.")
}, Fn = {
  name: g.FETCH,
  description: 'Fetch the full contents of a task or project by its ID. The ID should be in the format "task:{id}" or "project:{id}".',
  parameters: Nn,
  outputSchema: Mn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { id: r } = e, [o, n] = r.split(":", 2);
    if (!n || o !== "task" && o !== "project")
      throw new Error(
        'Invalid ID format. Expected "task:{id}" or "project:{id}". Example: "task:8485093748" or "project:6cfCcrrCFg2xP94Q"'
      );
    let a;
    if (o === "task") {
      const i = await t.getTask(n), c = P(i), d = [c.content];
      c.description && d.push(`

Description: ${c.description}`), c.dueDate && d.push(`
Due: ${c.dueDate}`), c.labels.length > 0 && d.push(`
Labels: ${c.labels.join(", ")}`), a = {
        id: `task:${c.id}`,
        title: c.content,
        text: d.join(""),
        url: Mt(c.id),
        metadata: {
          priority: c.priority,
          projectId: c.projectId,
          sectionId: c.sectionId,
          parentId: c.parentId,
          recurring: c.recurring,
          duration: c.duration,
          responsibleUid: c.responsibleUid,
          assignedByUid: c.assignedByUid,
          checked: c.checked,
          completedAt: c.completedAt
        }
      };
    } else {
      const i = await t.getProject(n), c = Z(i), d = [c.name];
      c.description && d.push(`

Description: ${c.description}`), c.isShared && d.push(`

Shared project`), c.isFavorite && d.push(`
Favorite: Yes`), a = {
        id: `project:${c.id}`,
        title: c.name,
        text: d.join(""),
        url: Ft(c.id),
        metadata: {
          color: c.color,
          isFavorite: c.isFavorite,
          isShared: c.isShared,
          parentId: c.parentId,
          inboxProject: c.inboxProject,
          viewStyle: c.viewStyle
        }
      };
    }
    return {
      textContent: JSON.stringify(a),
      structuredContent: a
    };
  }
}, Hn = s.union([s.iso.date(), s.iso.datetime({ offset: !0 })]);
function St(e) {
  return U(e).pipe(Hn.optional());
}
const Wn = {
  objectType: s.enum(["task", "project", "comment"]).optional().describe("Type of object to filter by."),
  objectId: s.string().optional().describe("Filter by specific object ID (task, project, or comment)."),
  eventType: s.enum([
    "added",
    "updated",
    "deleted",
    "completed",
    "uncompleted",
    "archived",
    "unarchived",
    "shared",
    "left"
  ]).optional().describe("Type of event to filter by."),
  projectId: s.string().optional().describe("Filter events by parent project ID."),
  taskId: s.string().optional().describe("Filter events by parent task ID (for subtask events)."),
  initiatorId: s.string().optional().describe("Filter by the user ID who initiated the event."),
  dateFrom: St(
    `Inclusive start of the activity range, as an ISO 8601 date or date-time. For all events on one local calendar day, use that day's start, for example "2026-08-02T00:00:00-04:00". Natural-language dates such as "tomorrow" are not supported.`
  ),
  dateTo: St(
    'Exclusive end of the activity range, as an ISO 8601 date or date-time. For all events on 2026-08-02, use "2026-08-03T00:00:00-04:00". Natural-language dates such as "tomorrow" are not supported.'
  ),
  limit: s.number().int().min(1).max(C.ACTIVITY_MAX).default(C.ACTIVITY_DEFAULT).describe("Maximum number of activity events to return."),
  cursor: s.string().optional().describe("Pagination cursor for retrieving the next page of results.")
}, Bn = {
  events: s.array(go).describe("The activity events."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of events in this page."),
  hasMore: s.boolean(),
  appliedFilters: s.record(s.string(), s.unknown())
}, zn = {
  name: g.FIND_ACTIVITY,
  description: 'Retrieve activity logs to monitor and audit changes in Todoist. Shows events from all users by default (use initiatorId to filter by specific user). To answer what someone completed in a period, including recurring task occurrences, use objectType "task", eventType "completed", and dateFrom/dateTo. Track task completions, updates, deletions, project changes, and more with flexible filtering. Activity history availability and retention depend on the user plan.',
  parameters: Wn,
  outputSchema: Bn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const {
      objectType: r,
      objectId: o,
      eventType: n,
      projectId: a,
      taskId: i,
      initiatorId: c,
      dateFrom: d,
      dateTo: l,
      limit: u,
      cursor: p
    } = e, m = {
      limit: u,
      cursor: p ?? null
    };
    r && n ? m.objectEventTypes = `${r}:${n}` : r ? m.objectEventTypes = `${r}:` : n && (m.objectEventTypes = `:${n}`), o && o !== "remove" && (m.objectId = o), a && (m.parentProjectId = a), i && (m.parentItemId = i), c && (m.initiatorId = c), d && (m.dateFrom = d), l && (m.dateTo = l);
    const { results: h, nextCursor: y } = await t.getActivityLogs(m), f = h.map(po);
    return {
      textContent: Kn({ events: f, args: e, nextCursor: y }),
      structuredContent: {
        events: f,
        nextCursor: y ?? void 0,
        totalCount: f.length,
        hasMore: !!y,
        appliedFilters: e
      }
    };
  }
};
function Kn({
  events: e,
  args: t,
  nextCursor: r
}) {
  let o = "Activity events";
  const n = [];
  if (t.eventType && n.push(`${t.eventType}`), t.objectType) {
    const c = t.objectType === "task" ? "tasks" : `${t.objectType}s`;
    n.push(c);
  }
  n.length > 0 && (o = `Activity: ${n.join(" ")}`);
  const a = [];
  t.objectId && a.push(`object ID: ${t.objectId}`), t.projectId && a.push(`project: ${t.projectId}`), t.taskId && a.push(`task: ${t.taskId}`), t.initiatorId && a.push(`initiator: ${t.initiatorId}`), t.dateFrom && a.push(`from: ${t.dateFrom}`), t.dateTo && a.push(`until (exclusive): ${t.dateTo}`);
  const i = [];
  return e.length === 0 && (i.push("No activity events match the specified filters"), i.push("Note: Activity logs only show recent events"), t.eventType && i.push(`Try removing the eventType filter (${t.eventType})`), t.objectType && i.push(`Try removing the objectType filter (${t.objectType})`), (t.objectId || t.projectId || t.taskId) && i.push("Verify the object ID is correct")), q({
    subject: o,
    count: e.length,
    limit: t.limit,
    nextCursor: r ?? void 0,
    filterHints: a,
    previewLines: qn(e, Math.min(e.length, t.limit)),
    zeroReasonHints: i
  });
}
function qn(e, t = 10) {
  const o = e.slice(0, t).map(Yn).join(`
`);
  if (e.length > t) {
    const n = e.length - t;
    return `${o}
    ... and ${n} more event${n === 1 ? "" : "s"}`;
  }
  return o;
}
function Yn(e) {
  const t = Gn(e.eventDate), r = `${e.eventType} ${e.objectType}`;
  let o = "";
  if (e.extraData) {
    const c = e.extraData.content || e.extraData.name || e.extraData.last_content;
    c && typeof c == "string" && (o = ` • "${c.length > 50 ? `${c.substring(0, 47)}...` : c}"`);
  }
  const n = e.objectId ? ` • id=${e.objectId}` : "", a = e.initiatorId ? ` • by=${e.initiatorId}` : " • system", i = e.parentProjectId ? ` • project=${e.parentProjectId}` : "";
  return `    [${t}] ${r}${o}${n}${a}${i}`;
}
function Gn(e) {
  try {
    const t = new Date(e), r = t.toLocaleDateString("en-US", { month: "short", timeZone: "UTC" }), o = t.toLocaleDateString("en-US", { day: "numeric", timeZone: "UTC" }), n = t.toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      hour12: !1,
      timeZone: "UTC"
    });
    return `${r} ${o}, ${n}`;
  } catch {
    return e;
  }
}
const Vn = {
  taskId: s.string().optional().describe("Find comments for a specific task."),
  projectId: s.string().optional().describe(
    'Find comments for a specific project. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  commentId: s.string().optional().describe("Get a specific comment by ID."),
  cursor: s.string().optional().describe("Pagination cursor for retrieving more results."),
  limit: s.number().int().min(1).max(C.COMMENTS_MAX).optional().describe("Maximum number of comments to return")
}, Jn = {
  comments: s.array(ye).describe("The found comments."),
  searchType: s.string().describe(
    'The type of search performed: "single" (comment ID), "task" (task ID), or "project" (project ID).'
  ),
  searchId: s.string().describe("The ID that was searched for (comment, task, or project ID)."),
  hasMore: s.boolean(),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of comments in this page.")
}, Xn = {
  name: g.FIND_COMMENTS,
  description: "Find comments by task, project, or get a specific comment by ID. Exactly one of taskId, projectId, or commentId must be provided.",
  parameters: Vn,
  outputSchema: Jn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = [e.taskId, e.projectId, e.commentId].filter(Boolean);
    if (r.length === 0)
      throw new Error("Must provide exactly one of: taskId, projectId, or commentId.");
    if (r.length > 1)
      throw new Error(
        "Cannot provide multiple search parameters. Choose one of: taskId, projectId, or commentId."
      );
    const o = await de({
      projectId: e.projectId,
      client: t
    });
    let n = !1, a = null, i;
    if (e.commentId)
      i = [await t.getComment(e.commentId)];
    else if (e.taskId) {
      const l = await t.getComments({
        taskId: e.taskId,
        cursor: e.cursor || null,
        limit: e.limit || C.COMMENTS_DEFAULT
      });
      i = l.results, n = l.nextCursor !== null, a = l.nextCursor;
    } else if (o) {
      const l = await t.getComments({
        projectId: o,
        cursor: e.cursor || null,
        limit: e.limit || C.COMMENTS_DEFAULT
      });
      i = l.results, n = l.nextCursor !== null, a = l.nextCursor;
    } else
      throw new Error("Invalid state: no search parameter provided");
    const c = i.map(ge);
    return {
      textContent: Zn({
        comments: c,
        searchType: e.commentId ? "single" : e.taskId ? "task" : "project",
        searchId: e.commentId || e.taskId || e.projectId || "",
        hasMore: n,
        nextCursor: a
      }),
      structuredContent: {
        comments: c,
        searchType: e.commentId ? "single" : e.taskId ? "task" : "project",
        searchId: e.commentId || e.taskId || e.projectId || "",
        hasMore: n,
        nextCursor: a ?? void 0,
        totalCount: c.length
      }
    };
  }
};
function Zn({
  comments: e,
  searchType: t,
  searchId: r,
  hasMore: o,
  nextCursor: n
}) {
  if (e.length === 0)
    return `No comments found for ${t}${t !== "single" ? ` ${r}` : ""}`;
  let a;
  if (t === "single") {
    const i = e[0];
    if (!i)
      return "Comment not found";
    a = `Found comment${i.fileAttachment !== void 0 ? ` • Has attachment: ${i.fileAttachment?.fileName || "file"}` : ""} • id=${i.id}`;
  } else {
    const i = e.filter((l) => l.fileAttachment !== void 0).length, c = i > 0 ? ` (${i} with attachments)` : "", d = e.length === 1 ? "comment" : "comments";
    a = `Found ${e.length} ${d} for ${t} ${r}${c}`, o && (a += " • More available");
  }
  if (n) {
    const i = as([], n);
    return `${a}
${i}`;
  }
  return a;
}
function ms(e) {
  const t = e.getUTCFullYear(), r = String(e.getUTCMonth() + 1).padStart(2, "0"), o = String(e.getUTCDate()).padStart(2, "0");
  return `${t}-${r}-${o}`;
}
function Qn(e) {
  const t = /^([+-])(\d{2}):(\d{2})$/.exec(e);
  if (!t)
    return 0;
  const [, r, o, n] = t, a = Number(o) * 60 + Number(n);
  return r === "-" ? -a : a;
}
function ea(e, t) {
  const r = new Date(e.getTime() + t * 60 * 1e3);
  return ms(r);
}
function jt(e, t) {
  const [r, o, n] = e.split("-"), a = Number(r), i = Number(o), c = Number(n);
  if (!Number.isFinite(a) || !Number.isFinite(i) || !Number.isFinite(c))
    throw new Error(`Invalid date format: ${e}. Expected YYYY-MM-DD.`);
  const d = new Date(Date.UTC(a, i - 1, c));
  return d.setUTCDate(d.getUTCDate() + t), ms(d);
}
const ta = ["and", "or"], ct = {
  labels: s.string().array().optional().describe("The labels to filter the tasks by"),
  labelsOperator: s.enum(ta).optional().describe(
    'The operator to use when filtering by labels. This will dictate whether a task has all labels, or some of them. Default is "or".'
  )
};
function dt(e = [], t = "or") {
  if (e.length === 0) return "";
  const r = t === "and" ? " & " : " | ";
  return `(${e.map((a) => a.startsWith("@") ? a : `@${a}`).join(` ${r} `)})`;
}
const sa = 7;
function ra({
  since: e,
  until: t,
  userGmtOffset: r,
  now: o = /* @__PURE__ */ new Date()
}) {
  const n = Qn(r), a = ea(o, n), i = sa - 1, c = t ?? (e ? jt(e, i) : a);
  return {
    since: e ?? jt(c, -i),
    until: c
  };
}
const oa = {
  getBy: s.enum(["completion", "due"]).default("completion").describe(
    'The method to use to get the tasks: "completion" to get tasks by completion date (ie, when the task was actually completed), "due" to get tasks by due date (ie, when the task was due to be completed by).'
  ),
  since: s.string().date().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe(
    "Optional start date for completed tasks. Format: YYYY-MM-DD. Defaults to 6 days before until (or today if until is omitted), resulting in a 7-day window."
  ),
  until: s.string().date().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe(
    "Optional end date for completed tasks. Format: YYYY-MM-DD. Defaults to 6 days after since (or today if since is omitted), resulting in a 7-day window."
  ),
  workspaceId: s.string().optional().describe("The ID of the workspace to get the tasks for."),
  projectId: s.string().optional().describe(
    'The ID of the project to get the tasks for. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: s.string().optional().describe("The ID of the section to get the tasks for."),
  parentId: s.string().optional().describe("The ID of the parent task to get the tasks for."),
  responsibleUser: s.string().optional().describe(
    "Filter completed tasks assigned to this user. User ID, name, or email. For personal queries (summaries, plans, reports), set to current user from user-info to exclude collaborators. Defaults to all collaborators."
  ),
  limit: s.number().int().min(1).max(C.COMPLETED_TASKS_MAX).default(C.COMPLETED_TASKS_DEFAULT).describe("The maximum number of tasks to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  ...ct
}, na = {
  tasks: s.array(F).describe("The found completed tasks."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean(),
  appliedFilters: s.record(s.string(), s.unknown())
}, aa = {
  name: g.FIND_COMPLETED_TASKS,
  description: "Get completed tasks. since/until are optional and default to a 7-day window when omitted. Includes all collaborators by default. Person-specific queries (summaries, plans, reports) require responsibleUser.",
  parameters: oa,
  outputSchema: na,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { getBy: r, labels: o, labelsOperator: n, since: a, until: i, responsibleUser: c, projectId: d, ...l } = e;
    if (e.cursor && (!a || !i))
      throw new Error(
        "Cursor pagination requires explicit since and until. Reuse structuredContent.appliedFilters.since and structuredContent.appliedFilters.until from the previous page."
      );
    const u = await Qe(t, c), p = u?.email;
    let h = dt(o, n);
    u && p && (h = ce(h, `assigned to: ${p}`));
    const y = await t.getUser(), f = y.tzInfo?.gmtString || "+00:00", v = await de({
      projectId: d,
      user: y
    }), j = ra({
      since: a,
      until: i,
      userGmtOffset: f
    }), A = `${j.since}T00:00:00${f}`, k = `${j.until}T23:59:59${f}`, I = new Date(A).toISOString(), x = new Date(k).toISOString(), T = {
      ...e,
      since: j.since,
      until: j.until
    }, { items: b, nextCursor: S } = r === "completion" ? await t.getCompletedTasksByCompletionDate({
      ...l,
      projectId: v,
      since: I,
      until: x,
      ...h ? { filterQuery: h, filterLang: "en" } : {}
    }) : await t.getCompletedTasksByDueDate({
      ...l,
      projectId: v,
      since: I,
      until: x,
      ...h ? { filterQuery: h, filterLang: "en" } : {}
    }), $ = b.map(P);
    return {
      textContent: ia({
        tasks: $,
        args: T,
        nextCursor: S,
        assigneeEmail: p
      }),
      structuredContent: {
        tasks: $,
        nextCursor: S ?? void 0,
        totalCount: $.length,
        hasMore: !!S,
        appliedFilters: T
      }
    };
  }
};
function ia({
  tasks: e,
  args: t,
  nextCursor: r,
  assigneeEmail: o
}) {
  const n = t.getBy === "completion" ? "completed" : "due", a = `Completed tasks (by ${n} date)`, i = [];
  if (i.push(`${n} date: ${t.since} to ${t.until}`), t.projectId && i.push(`project: ${t.projectId}`), t.sectionId && i.push(`section: ${t.sectionId}`), t.parentId && i.push(`parent: ${t.parentId}`), t.workspaceId && i.push(`workspace: ${t.workspaceId}`), t.labels && t.labels.length > 0) {
    const d = t.labels.map((l) => `@${l}`).join(t.labelsOperator === "and" ? " & " : " | ");
    i.push(`labels: ${d}`);
  }
  if (t.responsibleUser) {
    const d = o || t.responsibleUser;
    i.push(`assigned to: ${d}`);
  }
  const c = [];
  return e.length === 0 && (c.push("No tasks completed in this date range"), c.push("Try expanding the date range"), (t.projectId || t.sectionId || t.parentId) && c.push("Try removing project/section/parent filters"), t.getBy === "due" && c.push('Try switching to "completion" date instead')), q({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: r ?? void 0,
    filterHints: i,
    previewLines: Se(e, Math.min(e.length, t.limit)),
    zeroReasonHints: c
  });
}
const ca = {
  searchText: s.string().optional().describe(
    'Search for a label by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all labels are returned.'
  ),
  limit: s.number().int().min(1).max(C.LABELS_MAX).default(C.LABELS_DEFAULT).describe("The maximum number of labels to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of labels (cursor is obtained from the previous call to this tool, with the same parameters). Ignored when searchText is provided."
  )
}, da = {
  labels: s.array(pe).describe("The found personal labels."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of labels in this page."),
  hasMore: s.boolean(),
  sharedLabels: s.array(s.string()).describe(
    "Names of all shared labels visible to you. These have no IDs or metadata — use their names directly when filtering tasks."
  ),
  appliedFilters: s.record(s.string(), s.unknown())
}, la = {
  name: g.FIND_LABELS,
  description: "List personal labels and shared labels. Personal labels have full metadata (id, name, color, order, isFavorite) and support pagination and name search (partial, case insensitive). Shared labels are labels used on tasks shared with you — they are returned as names only (no IDs or metadata). When searching, all matching personal labels are fetched across all pages and returned as a single result set (limit and cursor are ignored). When not searching, personal labels are returned with pagination.",
  parameters: ca,
  outputSchema: da,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const [r, o] = await Promise.all([
      e.searchText ? co(t, e.searchText).then((c) => ({
        results: c,
        nextCursor: null
      })) : t.getLabels({ limit: e.limit, cursor: e.cursor ?? null }),
      lo(t)
    ]), { results: n, nextCursor: a } = r, i = e.searchText ? { searchText: e.searchText } : { limit: e.limit, cursor: e.cursor };
    return {
      textContent: ua({ labels: n, args: e, nextCursor: a, sharedLabels: o }),
      structuredContent: {
        labels: n.map((c) => pe.parse(c)),
        nextCursor: a ?? void 0,
        totalCount: n.length,
        hasMore: !!a,
        sharedLabels: o,
        appliedFilters: i
      }
    };
  }
};
function ua({
  labels: e,
  args: t,
  nextCursor: r,
  sharedLabels: o
}) {
  const n = t.searchText ? `All labels matching "${t.searchText}"` : "Labels", a = [];
  t.searchText && a.push(`searchText: "${t.searchText}"`);
  const i = 10, d = e.slice(0, i).map(Mo).join(`
`), l = e.length - i, u = l > 0 ? `${d}
    …and ${l} more` : d, p = [];
  e.length === 0 && (t.searchText ? (p.push("Try broader search terms"), p.push("Check spelling"), p.push("Remove searchText to see all labels")) : p.push("No personal labels created yet"));
  const m = o.length > 0 ? `
Shared labels (${o.length}): ${o.join(", ")}` : `
No shared labels.`;
  return q({
    subject: n,
    count: e.length,
    limit: t.searchText ? void 0 : t.limit,
    nextCursor: r ?? void 0,
    filterHints: a,
    previewLines: u,
    zeroReasonHints: p
  }) + m;
}
const { FIND_PROJECTS: hs, ADD_TASKS: fs, UPDATE_TASKS: bs } = g, pa = {
  projectId: s.string().min(1).optional().describe(
    'Optional. If provided, searches only collaborators of this project. If omitted, searches across the collaborators of all shared projects the authenticated user can access (plus the authenticated user themselves) — use this for general "find a user" / "who is X" lookups.'
  ),
  searchTerm: s.string().optional().describe(
    "Search for a user by name or email (partial and case insensitive match). If omitted, all users are returned."
  )
}, ma = {
  collaborators: s.array(yo).describe("The found users."),
  projectInfo: s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name."),
    isShared: s.boolean().describe("Whether the project is shared.")
  }).optional().describe("Information about the project (only present when projectId was provided)."),
  totalCount: s.number().describe("The total number of users found."),
  totalAvailable: s.number().optional().describe("The total number of available users before the search filter was applied."),
  appliedFilters: s.record(s.string(), s.unknown())
}, ha = {
  name: g.FIND_PROJECT_COLLABORATORS,
  description: `Find Todoist users (collaborators, teammates) by name or email to look up their user ID. Use this whenever the user asks to find, look up, or identify a person — e.g. "find Carrie's user ID", "who is Ernesto", "look up a user". When projectId is omitted, searches across the collaborators of every shared project the authenticated user has access to, plus the authenticated user themselves — an empty result means the person is not a collaborator on any project you share with them, not necessarily that they do not exist in Todoist. When projectId is provided, searches only that project. Partial, case-insensitive match on name and email.`,
  parameters: pa,
  outputSchema: ma,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { projectId: r, searchTerm: o } = e;
    if (!r)
      return fa({ searchTerm: o, client: t, appliedFilters: e });
    let n = r, a;
    try {
      if (a = await t.getProject(r), !a)
        throw new Error(`Project with ID "${r}" not found or not accessible`);
      if (n = a.name, !a.isShared)
        return {
          textContent: `Project "${n}" is not shared and has no collaborators.

**Next steps:**
• Share the project to enable collaboration
• Use ${fs} and ${bs} for assignment features once shared`,
          structuredContent: {
            collaborators: [],
            projectInfo: {
              id: r,
              name: n,
              isShared: !1
            },
            totalCount: 0,
            appliedFilters: e
          }
        };
    } catch (l) {
      throw new Error(
        `Failed to access project "${r}": ${l instanceof Error ? l.message : "Unknown error"}`
      );
    }
    const i = await W.getProjectCollaborators(t, r);
    if (i.length === 0)
      return {
        textContent: `Project "${n}" has no collaborators or collaborator data is not accessible.

**Next steps:**
• Check project sharing settings
• Ensure you have permission to view collaborators
• Try refreshing or re-sharing the project`,
        structuredContent: {
          collaborators: [],
          projectInfo: {
            id: r,
            name: n,
            isShared: !0
          },
          totalCount: 0,
          appliedFilters: e
        }
      };
    const c = gs(i, o);
    return {
      textContent: ys({
        collaborators: c,
        projectName: n,
        searchTerm: o,
        totalAvailable: i.length
      }),
      structuredContent: {
        collaborators: c,
        projectInfo: {
          id: r,
          name: n,
          isShared: !0
        },
        totalCount: c.length,
        totalAvailable: i.length,
        appliedFilters: e
      }
    };
  }
};
async function fa({
  searchTerm: e,
  client: t,
  appliedFilters: r
}) {
  const o = await W.getAllCollaborators(t), n = await ba(t, o);
  if (n.length === 0)
    return {
      textContent: `No users found. You may have no shared projects, or collaborator data is not accessible.

**Next steps:**
• Use ${hs} to find shared projects
• Share a project to add collaborators
• Ensure you have permission to view collaborators`,
      structuredContent: {
        collaborators: [],
        projectInfo: void 0,
        totalCount: 0,
        totalAvailable: 0,
        appliedFilters: r
      }
    };
  const a = gs(n, e);
  return {
    textContent: ys({
      collaborators: a,
      projectName: void 0,
      searchTerm: e,
      totalAvailable: n.length
    }),
    structuredContent: {
      collaborators: a,
      projectInfo: void 0,
      totalCount: a.length,
      totalAvailable: n.length,
      appliedFilters: r
    }
  };
}
async function ba(e, t) {
  try {
    const r = await e.getUser();
    return !r || t.some((o) => o.id === r.id) ? t : [
      { id: r.id, name: r.fullName, email: r.email },
      ...t
    ];
  } catch {
    return t;
  }
}
function gs(e, t) {
  if (!t)
    return e;
  const r = t.toLowerCase().trim();
  return e.filter(
    (o) => o.name.toLowerCase().includes(r) || o.email.toLowerCase().includes(r)
  );
}
function ys({
  collaborators: e,
  projectName: t,
  searchTerm: r,
  totalAvailable: o
}) {
  const n = t ? `project "${t}"` : "workspace", a = t ? "Project collaborators" : "Workspace users", i = r ? `${a} matching "${r}"` : a, c = [];
  r && c.push(`matching "${r}"`), c.push(`in ${n}`);
  let d = [];
  e.length > 0 && (d = e.slice(0, 10).map((p) => {
    const m = p.name || "Unknown Name", h = p.email || "No email";
    return `• ${m} (${h}) - ID: ${p.id}`;
  }), e.length > 10 && d.push(`... and ${e.length - 10} more`));
  const l = [];
  e.length === 0 && r && (l.push(`No users match "${r}"`), l.push("Try a broader search term or check spelling"), o > 0 && l.push(`${o} users available without filter`));
  const u = [];
  return e.length > 0 ? (u.push(`Use ${fs} with responsibleUser to assign new tasks`), u.push(`Use ${bs} with responsibleUser to reassign existing tasks`), u.push("Use user names, emails, or IDs for assignments")) : (u.push(`Use ${hs} to find other projects`), r && o > 0 && u.push("Try searching without filters to see all users")), q({
    subject: i,
    count: e.length,
    filterHints: c,
    previewLines: d.join(`
`),
    zeroReasonHints: l,
    nextSteps: u
  });
}
const { ADD_PROJECTS: Ct } = g, ga = {
  active: "projects",
  archived: "archived projects",
  all: "projects (active and archived)"
}, ya = {
  searchText: s.string().optional().describe(
    'Search for a project by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all projects are returned.'
  ),
  limit: s.number().int().min(1).max(C.PROJECTS_MAX).default(C.PROJECTS_DEFAULT).describe("The maximum number of projects to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of projects (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  archivedStatus: s.enum(["active", "archived", "all"]).optional().describe(
    "Which projects to return by archive status: 'active' (default, non-archived only), 'archived' (archived only), or 'all' (both active and archived). Each project includes an isArchived field. Archived projects can be deleted via the delete-object tool (type: 'project')."
  )
}, Ta = {
  projects: s.array(Q).describe("The found projects."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of projects in this page."),
  hasMore: s.boolean(),
  appliedFilters: s.record(s.string(), s.unknown())
}, ka = {
  name: g.FIND_PROJECTS,
  description: "List all projects or search for projects by name. By default only active projects are returned; use archivedStatus ('archived' or 'all') to include archived projects. When searching or when archivedStatus is 'all', all matching projects are returned (pagination is ignored). Otherwise projects are returned with pagination.",
  parameters: ya,
  outputSchema: Ta,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { searchText: r } = e, o = e.archivedStatus ?? "active";
    let n, a = null;
    if (r || o === "all") {
      const c = Promise.resolve([]), [d, l] = await Promise.all([
        o === "archived" ? c : r ? rs(t, r) : os(t),
        o === "active" ? c : ao(t)
      ]), u = r ? io(r) : null, p = u ? l.filter((m) => u.test(m.name)) : l;
      n = [...d, ...p];
    } else {
      const c = o === "archived" ? await t.getArchivedProjects({
        limit: e.limit,
        cursor: e.cursor ?? null
      }) : await t.getProjects({ limit: e.limit, cursor: e.cursor ?? null });
      n = c.results, a = c.nextCursor;
    }
    const i = n.map(Z);
    return {
      textContent: wa({ projects: i, args: e, nextCursor: a }),
      structuredContent: {
        projects: i,
        nextCursor: a ?? void 0,
        totalCount: i.length,
        hasMore: !!a,
        appliedFilters: e
      }
    };
  }
};
function wa({
  projects: e,
  args: t,
  nextCursor: r
}) {
  const o = ga[t.archivedStatus ?? "active"], n = t.searchText ? `All ${o} matching "${t.searchText}"` : `${o.charAt(0).toUpperCase()}${o.slice(1)}`, a = [];
  t.searchText && a.push(`searchText: "${t.searchText}"`), t.archivedStatus && t.archivedStatus !== "active" && a.push(`archivedStatus: "${t.archivedStatus}"`);
  const i = 10, d = e.slice(0, i).map(Fo).join(`
`), l = e.length - i, u = l > 0 ? `${d}
    …and ${l} more` : d, p = [];
  return e.length === 0 && (t.searchText ? (p.push("Try broader search terms"), p.push("Check spelling"), p.push("Remove searchText to see all projects")) : t.archivedStatus === "archived" ? (p.push("No archived projects"), p.push("Use archivedStatus: 'all' to also see active projects")) : t.archivedStatus === "all" ? (p.push("No projects created yet"), p.push(`Use ${Ct} to create a project`)) : (p.push("No active projects"), p.push("Use archivedStatus: 'all' to include archived projects"), p.push(`Use ${Ct} to create a project`))), q({
    subject: n,
    count: e.length,
    limit: t.searchText || t.archivedStatus === "all" ? void 0 : t.limit,
    nextCursor: r ?? void 0,
    filterHints: a,
    previewLines: u,
    zeroReasonHints: p
  });
}
const va = {
  taskId: s.string().optional().describe(
    "Find all reminders for a specific task. Returns both time-based and location reminders."
  ),
  reminderId: s.string().optional().describe("Get a specific time-based reminder (relative or absolute) by its ID."),
  locationReminderId: s.string().optional().describe("Get a specific location reminder by its ID.")
}, Ia = {
  reminders: s.array(st).describe("The found reminders (time-based and location)."),
  searchType: s.string().describe('The search type used: "task", "reminder", or "location_reminder".'),
  searchId: s.string().describe("The ID used for the search."),
  totalCount: s.number().describe("Total reminders in this response.")
}, Sa = {
  name: g.FIND_REMINDERS,
  description: "Find reminders by task ID (returns all reminder types), or get a specific reminder by its ID. Use reminderId for time-based reminders and locationReminderId for location reminders.",
  parameters: va,
  outputSchema: Ia,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { taskId: r, reminderId: o, locationReminderId: n } = e, a = [r, o, n].filter(Boolean);
    if (a.length === 0)
      throw new Error("One of taskId, reminderId, or locationReminderId must be provided.");
    if (a.length > 1)
      throw new Error(
        "Only one of taskId, reminderId, or locationReminderId can be provided at a time."
      );
    if (o) {
      const i = await t.getReminder(o), c = be(i);
      return {
        textContent: `Found ${i.type} reminder (id=${o})`,
        structuredContent: {
          reminders: [c],
          searchType: "reminder",
          searchId: o,
          totalCount: 1
        }
      };
    }
    if (n) {
      const i = await t.getLocationReminder(n), c = be(i);
      return {
        textContent: `Found location reminder (id=${n})`,
        structuredContent: {
          reminders: [c],
          searchType: "location_reminder",
          searchId: n,
          totalCount: 1
        }
      };
    }
    if (r) {
      const [i, c] = await Promise.all([
        M({
          apiMethod: (p) => t.getReminders(p),
          args: { taskId: r }
        }),
        M({
          apiMethod: (p) => t.getLocationReminders(p),
          args: { taskId: r }
        })
      ]), l = [...i, ...c].map(be);
      return {
        textContent: ja(l, r),
        structuredContent: {
          reminders: l,
          searchType: "task",
          searchId: r,
          totalCount: l.length
        }
      };
    }
    throw new Error("One of taskId, reminderId, or locationReminderId must be provided.");
  }
};
function ja(e, t) {
  if (e.length === 0)
    return `No reminders found for task ${t}`;
  const { timeBasedCount: r, locationCount: o } = ns(e), n = [];
  if (r > 0) {
    const a = r > 1 ? "time-based reminders" : "time-based reminder";
    n.push(`${r} ${a}`);
  }
  if (o > 0) {
    const a = o > 1 ? "location reminders" : "location reminder";
    n.push(`${o} ${a}`);
  }
  return `Found ${n.join(" and ")} for task ${t}`;
}
const { ADD_SECTIONS: Ca } = g, $a = {
  projectId: s.string().min(1).describe(
    'The ID of the project to search sections in. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  searchText: s.string().optional().describe(
    'Search for a section by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all sections in the project are returned.'
  )
}, Da = {
  sections: s.array(X).describe("The found sections."),
  totalCount: s.number().describe("The total number of sections found."),
  appliedFilters: s.record(s.string(), s.unknown())
}, xa = {
  name: g.FIND_SECTIONS,
  description: "Search for sections by name or other criteria in a project. When searching, uses server-side search to avoid fetching all sections.",
  parameters: $a,
  outputSchema: Da,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = await de({
      projectId: e.projectId,
      client: t
    });
    let o;
    e.searchText ? o = await uo(t, e.searchText, r) : o = (await t.getSections({
      projectId: r
    })).results;
    const n = o.map(K);
    return {
      textContent: Aa({
        sections: n,
        projectId: e.projectId,
        searchText: e.searchText
      }),
      structuredContent: {
        sections: n,
        totalCount: n.length,
        appliedFilters: e
      }
    };
  }
};
function Aa({
  sections: e,
  projectId: t,
  searchText: r
}) {
  const o = [];
  r ? (o.push("Try broader search terms"), o.push("Check spelling"), o.push("Remove searchText to see all sections")) : (o.push("Project has no sections yet"), o.push(`Use ${Ca} to create sections`));
  const n = r ? `Sections in project ${t} matching "${r}"` : `Sections in project ${t}`, a = e.length > 0 ? e.map((i) => `    ${i.name} • id=${i.id}`).join(`
`) : void 0;
  return q({
    subject: n,
    count: e.length,
    previewLines: a,
    zeroReasonHints: o
  });
}
function Ts(e) {
  return /* @__PURE__ */ new Date(`${e}T00:00:00`);
}
const Ea = {
  startDate: s.string().regex(/^(\d{4}-\d{2}-\d{2}|today)$/).optional().describe(
    "The start date to get the tasks for. Format: YYYY-MM-DD, or 'today', which by default also includes overdue tasks."
  ),
  overdueOption: s.enum(["overdue-only", "include-overdue", "exclude-overdue"]).optional().describe(
    "How to handle overdue tasks. 'overdue-only' to get only overdue tasks, 'include-overdue' to include overdue tasks along with tasks for the specified date(s), and 'exclude-overdue' to exclude overdue tasks. Default is 'include-overdue'."
  ),
  daysCount: s.number().int().min(1).max(30).default(1).describe(
    "The number of days to get the tasks for, starting from the start date. Default is 1 which means only tasks for the start date."
  ),
  limit: s.number().int().min(1).max(C.TASKS_MAX).default(C.TASKS_DEFAULT).describe("The maximum number of tasks to return. Default is 10."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  responsibleUser: s.string().optional().describe(
    "Filter tasks assigned to this user. User ID, name, or email. For personal queries (summaries, plans, reports), set to current user from user-info to exclude collaborators."
  ),
  responsibleUserFiltering: s.enum(ts).optional().describe(
    "Filter when responsibleUser is omitted. 'assigned'=assigned to others; 'unassignedOrMe'=unassigned+mine; 'all'=everyone. Default: 'unassignedOrMe'."
  ),
  ...ct
}, Oa = {
  tasks: s.array(F).describe("The found tasks."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean(),
  appliedFilters: s.record(s.string(), s.unknown())
}, Pa = {
  name: g.FIND_TASKS_BY_DATE,
  description: "Get tasks by date range. startDate='today' includes overdue items. Default responsibleUserFiltering='unassignedOrMe' excludes others' tasks. Person-specific queries (summaries, plans, reports) require responsibleUser.",
  parameters: Ea,
  outputSchema: Oa,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    if (!e.startDate && e.overdueOption !== "overdue-only")
      throw new Error(
        "Either startDate must be provided or overdueOption must be set to overdue-only"
      );
    const r = await Qe(t, e.responsibleUser), o = r?.userId, n = r?.email;
    let a = "";
    if (e.overdueOption === "overdue-only")
      a = "overdue";
    else if (e.startDate === "today")
      if (e.daysCount > 1) {
        const p = /* @__PURE__ */ new Date(), m = ie(p), h = Ue(p, e.daysCount), y = lt(h, { representation: "date" }), f = `(due after: ${m} | due: ${m}) & due before: ${y}`;
        a = e.overdueOption === "exclude-overdue" ? f : `(${f} | overdue)`;
      } else
        a = e.overdueOption === "exclude-overdue" ? "today" : "(today | overdue)";
    else if (e.startDate) {
      const p = e.startDate, m = Ue(Ts(p), e.daysCount), h = lt(m, { representation: "date" });
      a = `(due after: ${p} | due: ${p}) & due before: ${h}`;
    }
    const i = dt(e.labels, e.labelsOperator);
    i.length > 0 && (a = ce(a, `(${i})`));
    const c = ss({
      resolvedAssigneeId: o,
      assigneeEmail: n,
      responsibleUserFiltering: e.responsibleUserFiltering
    });
    a = ce(a, c);
    const { tasks: d, nextCursor: l } = await tt({
      client: t,
      query: a,
      cursor: e.cursor,
      limit: e.limit
    });
    return {
      textContent: _a({ tasks: d, args: e, nextCursor: l, assigneeEmail: n }),
      structuredContent: {
        tasks: d,
        nextCursor: l ?? void 0,
        totalCount: d.length,
        hasMore: !!l,
        appliedFilters: e
      }
    };
  }
};
function _a({
  tasks: e,
  args: t,
  nextCursor: r,
  assigneeEmail: o
}) {
  const n = [];
  if (t.overdueOption === "overdue-only")
    n.push("overdue tasks only");
  else if (t.startDate === "today") {
    const c = t.overdueOption === "exclude-overdue" ? "" : " + overdue tasks";
    n.push(
      `today${c}${t.daysCount > 1 ? ` + ${t.daysCount - 1} more days` : ""}`
    );
  } else if (t.startDate) {
    const c = t.daysCount > 1 ? ` to ${ie(Ue(Ts(t.startDate), t.daysCount))}` : "";
    n.push(`${t.startDate}${c}`);
  }
  if (t.labels && t.labels.length > 0) {
    const c = t.labels.map((d) => `@${d}`).join(t.labelsOperator === "and" ? " & " : " | ");
    n.push(`labels: ${c}`);
  }
  if (t.responsibleUser) {
    const c = o || t.responsibleUser;
    n.push(`assigned to: ${c}`);
  }
  let a = "";
  if (t.overdueOption === "overdue-only" ? a = "Overdue tasks" : t.startDate === "today" ? a = t.overdueOption === "exclude-overdue" ? "Today's tasks" : "Today's tasks + overdue" : t.startDate ? a = `Tasks for ${t.startDate}` : a = "Tasks", t.responsibleUser) {
    const c = o || t.responsibleUser;
    a += ` assigned to ${c}`;
  }
  const i = [];
  if (e.length === 0)
    if (t.overdueOption === "overdue-only")
      i.push("Great job! No overdue tasks");
    else if (t.startDate === "today") {
      const c = t.overdueOption === "exclude-overdue" ? "" : " or overdue";
      i.push(`Great job! No tasks for today${c}`);
    } else
      i.push("Expand date range with larger 'daysCount'"), i.push("Check today's tasks with startDate='today'");
  return q({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: r ?? void 0,
    filterHints: n,
    previewLines: Se(e, Math.min(e.length, t.limit)),
    zeroReasonHints: i
  });
}
const Ra = 300 * 1e3;
class Ua {
  constructor() {
    this.cache = null;
  }
  async getFilters(t) {
    if (this.cache && Date.now() - this.cache.timestamp < Ra)
      return this.cache.filters;
    const o = ((await t.sync({
      resourceTypes: ["filters"],
      syncToken: "*"
    })).filters ?? []).filter((n) => !n.isDeleted);
    return this.cache = { filters: o, timestamp: Date.now() }, o;
  }
  /**
   * Resolve a filter name or ID to a filter ID, name, and query.
   *
   * Resolution order:
   * 1. Exact ID match
   * 2. Exact case-insensitive name match
   * 3. Unique partial case-insensitive name match
   * 4. Multiple partial matches → throw ambiguous error
   * 5. No match → throw not-found error
   */
  async resolveFilter(t, r) {
    const o = r.trim();
    if (!o)
      throw new Error("Filter reference cannot be empty");
    const n = await this.getFilters(t), a = n.find((u) => u.id === o);
    if (a)
      return { filterId: a.id, filterName: a.name, filterQuery: a.query };
    const i = o.toLowerCase(), c = n.find((u) => u.name.toLowerCase() === i);
    if (c)
      return {
        filterId: c.id,
        filterName: c.name,
        filterQuery: c.query
      };
    const d = n.filter((u) => u.name.toLowerCase().includes(i)), l = d.length === 1 ? d[0] : void 0;
    if (l)
      return {
        filterId: l.id,
        filterName: l.name,
        filterQuery: l.query
      };
    if (d.length > 1) {
      const u = d.slice(0, 5).map((p) => `  - "${p.name}" (id: ${p.id})`).join(`
`);
      throw new Error(
        `Ambiguous filter reference "${o}". Multiple filters match:
${u}` + (d.length > 5 ? `
  ... and ${d.length - 5} more` : "")
      );
    }
    throw new Error(`Filter "${o}" not found.`);
  }
  /**
   * Clear the filter cache — useful for testing.
   */
  clearCache() {
    this.cache = null;
  }
}
const La = new Ua(), { FIND_COMPLETED_TASKS: $t, ADD_TASKS: Dt } = g, Na = {
  searchText: s.string().optional().describe("The text to search for in tasks."),
  projectId: s.string().optional().describe(
    'Find tasks in this project. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: s.string().optional().describe("Find tasks in this section."),
  parentId: s.string().optional().describe("Find subtasks of this parent task."),
  responsibleUser: s.string().optional().describe("Find tasks assigned to this user. Can be a user ID, name, or email address."),
  responsibleUserFiltering: s.enum(ts).optional().describe(
    'How to filter by responsible user when responsibleUser is not provided. "assigned" = only tasks assigned to others; "unassignedOrMe" = only unassigned tasks or tasks assigned to me; "all" = all tasks regardless of assignment. Default value will be `unassignedOrMe`.'
  ),
  limit: s.number().int().min(1).max(C.TASKS_MAX).default(C.TASKS_DEFAULT).describe("The maximum number of tasks to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  filter: s.string().optional().describe(
    'A raw Todoist filter query string (e.g. "today", "p1", "##Work", "(today | overdue) & p1"). Combined with other filters using AND. Cannot be used with projectId, sectionId, parentId, or filterIdOrName.'
  ),
  filterIdOrName: s.string().optional().describe(
    "The ID or name of a saved Todoist filter. The filter's query will be fetched and used to find tasks. Cannot be used with the `filter` parameter, projectId, sectionId, or parentId."
  ),
  ...ct
}, Ma = {
  tasks: s.array(F).describe("The found tasks."),
  nextCursor: s.string().optional(),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean(),
  appliedFilters: s.record(s.string(), s.unknown())
}, Fa = {
  name: g.FIND_TASKS,
  description: "Find tasks by text search, project/section/parent container, responsible user, labels, a raw Todoist filter string, or a saved filter by ID or name (filterIdOrName). At least one filter must be provided.",
  parameters: Na,
  outputSchema: Ma,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const {
      searchText: r,
      projectId: o,
      sectionId: n,
      parentId: a,
      responsibleUser: i,
      responsibleUserFiltering: c,
      limit: d,
      cursor: l,
      labels: u,
      labelsOperator: p,
      filter: m,
      filterIdOrName: h
    } = e, y = await t.getUser(), f = u && u.length > 0;
    if (!r && !o && !n && !a && !i && !f && !m && !h)
      throw new Error(
        "At least one filter must be provided: searchText, projectId, sectionId, parentId, responsibleUser, labels, filter, or filterIdOrName"
      );
    if (m && h)
      throw new Error(
        "The `filter` and `filterIdOrName` parameters cannot be used together. Provide only one."
      );
    if ((m || h) && (o || n || a))
      throw new Error(
        'The `filter`/`filterIdOrName` parameter cannot be combined with projectId, sectionId, or parentId. Use filter syntax instead (e.g. "##ProjectName").'
      );
    let v = m;
    h && (v = (await La.resolveFilter(t, h)).filterQuery);
    const j = await Qe(t, i), A = j?.userId, k = j?.email;
    if (o || n || a) {
      const D = {
        limit: d,
        cursor: l ?? null
      };
      o && (D.projectId = await de({ projectId: o, user: y })), n && (D.sectionId = n), a && (D.parentId = a);
      const { results: me, nextCursor: Y } = await t.getTasks(D), $e = me.map(P);
      let H = r ? $e.filter(
        (te) => te.content.toLowerCase().includes(r.toLowerCase()) || te.description?.toLowerCase().includes(r.toLowerCase())
      ) : $e;
      return H = no({
        tasks: H,
        resolvedAssigneeId: A,
        currentUserId: y.id,
        responsibleUserFiltering: c
      }), u && u.length > 0 && (H = p === "and" ? H.filter(
        (te) => u.every((De) => te.labels.includes(De))
      ) : H.filter(
        (te) => u.some((De) => te.labels.includes(De))
      )), {
        textContent: Ee({
          tasks: H,
          args: e,
          nextCursor: Y,
          isContainerSearch: !0,
          assigneeEmail: k
        }),
        structuredContent: {
          tasks: H,
          nextCursor: Y ?? void 0,
          totalCount: H.length,
          hasMore: !!Y,
          appliedFilters: e
        }
      };
    }
    if (A && !r && !f && !v) {
      const { results: D, nextCursor: me } = await t.getTasksByFilter({
        query: `assigned to: ${k}`,
        lang: "en",
        limit: d,
        cursor: l ?? null
      }), Y = D.map(P);
      return {
        textContent: Ee({
          tasks: Y,
          args: e,
          nextCursor: me,
          isContainerSearch: !1,
          assigneeEmail: k
        }),
        structuredContent: {
          tasks: Y,
          nextCursor: me ?? void 0,
          totalCount: Y.length,
          hasMore: !!me,
          appliedFilters: e
        }
      };
    }
    let I = v ? `(${v})` : "";
    r && (I = ce(I, `search: ${r}`));
    const x = dt(u, p);
    if (I = ce(I, x), !(h && !i && !c)) {
      const D = ss({
        resolvedAssigneeId: A,
        assigneeEmail: k,
        responsibleUserFiltering: c
      });
      I = ce(I, D);
    }
    const { tasks: b, nextCursor: S } = await tt({
      client: t,
      query: I,
      cursor: e.cursor,
      limit: e.limit
    });
    return {
      textContent: Ee({
        tasks: b,
        args: e,
        nextCursor: S,
        isContainerSearch: !1,
        assigneeEmail: k
      }),
      structuredContent: {
        tasks: b,
        nextCursor: S ?? void 0,
        totalCount: b.length,
        hasMore: !!S,
        appliedFilters: e
      }
    };
  }
};
function Ha(e) {
  if (e.projectId) {
    const t = [
      e.searchText ? "No tasks in project match search" : "Project has no tasks yet"
    ];
    return e.searchText || t.push(`Use ${Dt} to create tasks`), t;
  }
  if (e.sectionId) {
    const t = [e.searchText ? "No tasks in section match search" : "Section is empty"];
    return e.searchText || t.push("Tasks may be in other sections of the project"), t;
  }
  if (e.parentId) {
    const t = [e.searchText ? "No subtasks match search" : "No subtasks created yet"];
    return e.searchText || t.push(`Use ${Dt} with parentId to add subtasks`), t;
  }
  return [];
}
function Ee({
  tasks: e,
  args: t,
  nextCursor: r,
  isContainerSearch: o,
  assigneeEmail: n
}) {
  let a = "Tasks";
  const i = [], c = [];
  if (o) {
    if (t.projectId ? (a = "Tasks in project", i.push(`in project ${t.projectId}`)) : t.sectionId ? (a = "Tasks in section", i.push(`in section ${t.sectionId}`)) : t.parentId ? (a = "Subtasks", i.push(`subtasks of ${t.parentId}`)) : a = "Tasks", t.searchText && (a += ` matching "${t.searchText}"`, i.push(`containing "${t.searchText}"`)), t.responsibleUser) {
      const d = n || t.responsibleUser;
      a += ` assigned to ${d}`, i.push(`assigned to ${d}`);
    }
    if (t.labels && t.labels.length > 0) {
      const d = t.labels.map((l) => `@${l}`).join(t.labelsOperator === "and" ? " & " : " | ");
      i.push(`labels: ${d}`);
    }
    e.length === 0 && c.push(...Ha(t));
  } else {
    const d = n || t.responsibleUser, l = [];
    if (t.filter && l.push(`filter: ${t.filter}`), t.searchText && l.push(`"${t.searchText}"`), t.responsibleUser && l.push(`assigned to ${d}`), t.labels && t.labels.length > 0) {
      const u = t.labels.map((p) => `@${p}`).join(t.labelsOperator === "and" ? " & " : " | ");
      l.push(`with labels: ${u}`);
    }
    if (t.filter && !t.searchText && !t.responsibleUser && !t.labels?.length ? (a = `Tasks matching filter: ${t.filter}`, i.push(`filter: ${t.filter}`)) : t.searchText ? (a = `Search results for ${l.join(" ")}`, i.push(`matching "${t.searchText}"`), t.filter && i.push(`filter: ${t.filter}`)) : t.responsibleUser && (!t.labels || t.labels.length === 0) ? (a = `Tasks assigned to ${d}`, t.filter && i.push(`filter: ${t.filter}`)) : t.labels && t.labels.length > 0 && !t.responsibleUser ? (a = `Tasks with labels: ${t.labels.map((p) => `@${p}`).join(t.labelsOperator === "and" ? " & " : " | ")}`, t.filter && i.push(`filter: ${t.filter}`)) : a = `Tasks ${l.join(" ")}`, t.responsibleUser && i.push(`assigned to ${d}`), t.labels && t.labels.length > 0) {
      const u = t.labels.map((p) => `@${p}`).join(t.labelsOperator === "and" ? " & " : " | ");
      i.push(`labels: ${u}`);
    }
    if (e.length === 0) {
      if (t.responsibleUser) {
        const u = n || t.responsibleUser;
        c.push(`No tasks assigned to ${u}`), c.push("Check if the user name is correct"), c.push(`Check completed tasks with ${$t}`);
      }
      t.searchText && (c.push("Try broader search terms"), c.push("Verify spelling and try partial words"), t.responsibleUser || c.push(`Check completed tasks with ${$t}`));
    }
  }
  return q({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: r ?? void 0,
    filterHints: i,
    previewLines: Se(e, Math.min(e.length, t.limit)),
    zeroReasonHints: c
  });
}
const Wa = {
  projectId: s.string().min(1).optional().describe(
    "Optional project ID. If provided, shows detailed overview of that project. If omitted, shows overview of all projects."
  )
}, ks = s.lazy(
  () => s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name."),
    parentId: s.string().optional().describe("The parent project ID (for sub-projects, personal projects only)."),
    folderId: s.string().optional().describe("The folder ID this project belongs to (workspace projects only)."),
    childOrder: s.number().describe("The ordering index among siblings."),
    sections: s.array(
      s.object({
        id: s.string(),
        name: s.string()
      })
    ),
    children: s.array(ks).describe("Nested child projects.")
  })
), Ba = {
  type: s.enum(["account_overview", "project_overview"]).describe("The type of overview returned."),
  totalProjects: s.number().optional().describe("Total number of projects (account overview only)."),
  totalTasks: s.number().optional().describe("Total number of tasks."),
  totalSections: s.number().optional().describe("Total number of sections (project overview only)."),
  tasksWithoutSection: s.number().optional().describe("Number of tasks not in any section (project overview only)."),
  projectInfo: s.object({
    id: s.string(),
    name: s.string(),
    isShared: s.boolean(),
    isFavorite: s.boolean()
  }).optional().describe("Project information (project overview only)."),
  // Additional fields that exist in structured outputs
  hasNestedProjects: s.boolean().optional().describe("Whether account has nested projects (account overview only)."),
  inbox: s.object({
    id: s.string().describe("The inbox project ID."),
    name: s.string().describe("The inbox project name."),
    sections: s.array(X).describe("Sections in the inbox project.")
  }).nullable().optional().describe("Inbox information (account overview only)."),
  projects: s.array(s.lazy(() => ks)).optional().describe(
    "List of projects with hierarchy, folders, and ordering (account overview only)."
  ),
  project: s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name.")
  }).optional().describe("Project details (project overview only)."),
  sections: s.array(X).optional().describe("List of sections (project overview only)."),
  tasks: s.array(s.any()).optional().describe("List of tasks (project overview only)."),
  stats: s.object({
    totalTasks: s.number(),
    totalSections: s.number(),
    tasksWithoutSection: s.number()
  }).optional().describe("Statistics object (project overview only).")
};
function za(e) {
  const t = {};
  for (const n of e)
    t[n.id] = {
      ...n,
      children: [],
      childOrder: n.childOrder ?? 0
    };
  const r = [];
  for (const n of e) {
    const a = t[n.id];
    if (a)
      if (B(n) && n.parentId) {
        const i = t[n.parentId];
        i ? i.children.push(a) : r.push(a);
      } else
        r.push(a);
  }
  function o(n) {
    n.sort((a, i) => a.childOrder - i.childOrder);
    for (const a of n)
      o(a.children);
  }
  return o(r), r;
}
async function Ka(e, t) {
  const r = {};
  return await Promise.all(
    t.map(async (o) => {
      const { results: n } = await e.getSections({ projectId: o });
      r[o] = n;
    })
  ), r;
}
function ws(e, t, r = "") {
  const o = [];
  o.push(`${r}- Project: ${e.name} (id=${e.id})`);
  const n = t[e.id] || [];
  for (const a of n)
    o.push(`${r}  - Section: ${a.name} (id=${a.id})`);
  for (const a of e.children)
    o.push(...ws(a, t, `${r}  `));
  return o;
}
function xt(e) {
  const t = {};
  for (const o of e)
    t[o.id] = { ...o, children: [] };
  const r = [];
  for (const o of e) {
    const n = t[o.id];
    if (!n) continue;
    if (!o.parentId) {
      r.push(n);
      continue;
    }
    const a = t[o.parentId];
    a ? a.children.push(n) : r.push(n);
  }
  return r;
}
function qe(e, t = "") {
  const r = [];
  for (const o of e) {
    const n = `id=${o.id}`, a = o.dueDate ? `; due=${o.dueDate}` : "", i = `; content=${o.content}`;
    r.push(`${t}- ${n}${a}${i}`), o.children.length > 0 && r.push(...qe(o.children, `${t}  `));
  }
  return r;
}
function vs(e, t) {
  return {
    id: e.id,
    name: e.name,
    parentId: B(e) ? e.parentId ?? void 0 : void 0,
    folderId: ke(e) ? e.folderId ?? void 0 : void 0,
    childOrder: e.childOrder,
    sections: (t[e.id] || []).map(K),
    children: e.children.map(
      (r) => vs(r, t)
    )
  };
}
async function qa(e, t) {
  let r = [], o;
  do {
    const { results: n, nextCursor: a } = await e.getTasks({
      projectId: t,
      limit: C.TASKS_BATCH_SIZE,
      cursor: o ?? void 0
    });
    r = r.concat(n.map(P)), o = a ?? void 0;
  } while (o);
  return r;
}
async function Ya(e, t) {
  const { results: r } = await e.getSections({ projectId: t });
  return r;
}
async function Ga(e) {
  const { results: t } = await e.getProjects({}), r = t.find((p) => B(p) && p.inboxProject === !0), o = t.filter((p) => !B(p) || p.inboxProject !== !0), n = za(o), a = t.map((p) => p.id), i = await Ka(e, a), c = ["# Personal Projects", ""];
  if (r) {
    c.push(`- Inbox Project: ${r.name} (id=${r.id})`);
    for (const p of i[r.id] || [])
      c.push(`  - Section: ${p.name} (id=${p.id})`);
  }
  if (n.length)
    for (const p of n)
      c.push(...ws(p, i));
  else
    c.push("_No projects found._");
  c.push("");
  const d = n.some((p) => p.children.length > 0);
  d && c.push(
    "_Note: Indentation indicates that a project is a sub-project of the one above it. This allows for organizing projects hierarchically, with parent projects containing related sub-projects._",
    ""
  );
  const l = c.join(`
`), u = {
    type: "account_overview",
    inbox: r ? {
      id: r.id,
      name: r.name,
      sections: (i[r.id] || []).map(K)
    } : null,
    projects: n.map(
      (p) => vs(p, i)
    ),
    totalProjects: t.length,
    totalSections: a.reduce(
      (p, m) => p + (i[m]?.length || 0),
      0
    ),
    hasNestedProjects: d
  };
  return { textContent: l, structuredContent: u };
}
async function Va(e, t) {
  const [r, o, n] = await Promise.all([
    e.getProject(t),
    Ya(e, t),
    qa(e, t)
  ]), a = {};
  for (const u of o)
    a[u.id] = [];
  const i = [];
  for (const u of n)
    (u.sectionId ? a[u.sectionId] ?? i : i).push(u);
  const c = [`# ${r.name}`];
  if (i.length > 0) {
    c.push("");
    const u = xt(i);
    c.push(...qe(u));
  }
  for (const u of o) {
    c.push(""), c.push(`## ${u.name}`);
    const p = a[u.id];
    if (!p?.length)
      continue;
    const m = xt(p);
    c.push(...qe(m));
  }
  const d = c.join(`
`), l = {
    type: "project_overview",
    project: {
      id: r.id,
      name: r.name
    },
    sections: o.map(K),
    tasks: n.map((u) => ({
      ...u,
      children: []
      // Tasks already include hierarchical info via parentId
    })),
    stats: {
      totalTasks: n.length,
      totalSections: o.length,
      tasksWithoutSection: i.length
    }
  };
  return { textContent: d, structuredContent: l };
}
const Ja = {
  name: g.GET_OVERVIEW,
  description: "Get a Markdown overview. If no projectId is provided, shows all projects with hierarchy and sections (useful for navigation). If projectId is provided, shows detailed overview of that specific project including all tasks grouped by sections.",
  parameters: Wa,
  outputSchema: Ba,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = e.projectId ? await Va(t, e.projectId) : await Ga(t);
    return {
      textContent: r.textContent,
      structuredContent: r.structuredContent
    };
  }
}, Xa = {}, ae = s.object({
  count: s.number().describe("Number of consecutive periods in this streak."),
  start: s.string().describe("Start date of the streak."),
  end: s.string().describe("End date of the streak.")
}), At = s.object({
  id: s.string().describe("Project ID."),
  completed: s.number().describe("Number of tasks completed in this project.")
}), Za = {
  completedCount: s.number().describe("Total number of completed tasks (all-time)."),
  daysItems: s.array(
    s.object({
      date: s.string().describe("Date string (YYYY-MM-DD)."),
      totalCompleted: s.number().describe("Total tasks completed on this day."),
      items: s.array(At).describe("Per-project completion breakdown for this day.")
    })
  ).describe("Daily completion breakdown (most recent days)."),
  weekItems: s.array(
    s.object({
      from: s.string().describe("Start date of the week."),
      to: s.string().describe("End date of the week."),
      totalCompleted: s.number().describe("Total tasks completed in this week."),
      items: s.array(At).describe("Per-project completion breakdown for this week.")
    })
  ).describe("Weekly completion breakdown (most recent weeks)."),
  goals: s.object({
    dailyGoal: s.number().describe("Daily task completion goal."),
    weeklyGoal: s.number().describe("Weekly task completion goal."),
    currentDailyStreak: ae.describe("Current daily goal streak."),
    currentWeeklyStreak: ae.describe("Current weekly goal streak."),
    lastDailyStreak: ae.describe("Previous daily goal streak."),
    lastWeeklyStreak: ae.describe("Previous weekly goal streak."),
    maxDailyStreak: ae.describe("Best daily goal streak ever."),
    maxWeeklyStreak: ae.describe("Best weekly goal streak ever."),
    vacationMode: s.boolean().describe("Whether vacation mode is enabled."),
    karmaDisabled: s.boolean().describe("Whether karma tracking is disabled.")
  }).describe("Goal and streak information."),
  karma: s.number().describe("Current karma score."),
  karmaTrend: s.string().describe('Karma trend direction (e.g., "up" or "down").'),
  karmaLastUpdate: s.number().describe("Timestamp of the last karma update."),
  karmaGraphData: s.array(
    s.object({
      date: s.string().describe("Date of the karma data point."),
      karmaAvg: s.number().describe("Average karma for this date.")
    })
  ).describe("Historical karma data points for graphing."),
  karmaUpdateReasons: s.array(
    s.object({
      time: s.string().describe("Timestamp of the karma change."),
      newKarma: s.number().describe("New karma value after the change."),
      positiveKarma: s.number().describe("Positive karma earned."),
      negativeKarma: s.number().describe("Negative karma incurred."),
      positiveKarmaReasons: s.array(s.any()).describe("Reasons for positive karma."),
      negativeKarmaReasons: s.array(s.any()).describe("Reasons for negative karma.")
    })
  ).describe("Recent karma change events with reasons."),
  projectColors: s.record(s.string(), s.string()).describe("Map of project ID to color key.")
}, Qa = {
  name: g.GET_PRODUCTIVITY_STATS,
  description: "Get comprehensive productivity statistics including daily/weekly completion breakdowns, goal streaks (current, last, max), karma score and trends, and historical karma data. Useful for productivity analysis and tracking goal progress.",
  parameters: Xa,
  outputSchema: Za,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const r = await t.getProductivityStats();
    return {
      textContent: ei(r),
      structuredContent: {
        completedCount: r.completedCount,
        daysItems: r.daysItems,
        weekItems: r.weekItems,
        goals: {
          dailyGoal: r.goals.dailyGoal,
          weeklyGoal: r.goals.weeklyGoal,
          currentDailyStreak: r.goals.currentDailyStreak,
          currentWeeklyStreak: r.goals.currentWeeklyStreak,
          lastDailyStreak: r.goals.lastDailyStreak,
          lastWeeklyStreak: r.goals.lastWeeklyStreak,
          maxDailyStreak: r.goals.maxDailyStreak,
          maxWeeklyStreak: r.goals.maxWeeklyStreak,
          vacationMode: r.goals.vacationMode,
          karmaDisabled: r.goals.karmaDisabled
        },
        karma: r.karma,
        karmaTrend: r.karmaTrend,
        karmaLastUpdate: r.karmaLastUpdate,
        karmaGraphData: r.karmaGraphData,
        karmaUpdateReasons: r.karmaUpdateReasons,
        projectColors: r.projectColors
      }
    };
  }
};
function ei(e) {
  const t = [
    "# Productivity Statistics",
    "",
    `**Total Completed Tasks:** ${e.completedCount.toLocaleString()}`,
    `**Karma:** ${e.karma.toLocaleString()} (${e.karmaTrend})`,
    "",
    "## Goals & Streaks",
    `**Daily Goal:** ${e.goals.dailyGoal} tasks/day`,
    `**Weekly Goal:** ${e.goals.weeklyGoal} tasks/week`,
    `**Current Daily Streak:** ${e.goals.currentDailyStreak.count} days (${e.goals.currentDailyStreak.start} to ${e.goals.currentDailyStreak.end})`,
    `**Current Weekly Streak:** ${e.goals.currentWeeklyStreak.count} weeks (${e.goals.currentWeeklyStreak.start} to ${e.goals.currentWeeklyStreak.end})`,
    `**Best Daily Streak:** ${e.goals.maxDailyStreak.count} days`,
    `**Best Weekly Streak:** ${e.goals.maxWeeklyStreak.count} weeks`
  ];
  if (e.daysItems.length > 0) {
    t.push("", "## Recent Daily Completions");
    const r = e.daysItems.slice(0, 7);
    for (const o of r)
      t.push(`  ${o.date}: ${o.totalCompleted} tasks`);
  }
  if (e.weekItems.length > 0) {
    t.push("", "## Recent Weekly Completions");
    const r = e.weekItems.slice(0, 4);
    for (const o of r)
      t.push(`  ${o.from} to ${o.to}: ${o.totalCompleted} tasks`);
  }
  return t.join(`
`);
}
const ti = {
  projectId: s.string().min(1).describe("The ID of the project to get activity stats for."),
  weeks: s.number().int().min(1).max(12).optional().describe("Number of weeks of activity data to retrieve (1-12, default 2)."),
  includeWeeklyCounts: s.boolean().optional().describe("Include weekly rollup counts alongside daily counts.")
}, si = {
  projectId: s.string().describe("The project ID."),
  dayItems: s.array(
    s.object({
      date: s.string().describe("Date in ISO format."),
      totalCount: s.number().describe("Number of tasks completed on this day.")
    })
  ).describe("Daily task completion counts."),
  weekItems: s.array(
    s.object({
      fromDate: s.string().describe("Start date of the week."),
      toDate: s.string().describe("End date of the week."),
      totalCount: s.number().describe("Number of tasks completed in this week.")
    })
  ).nullable().describe("Weekly completion rollups. Only included when includeWeeklyCounts is true.")
};
function ri(e, t) {
  const r = [`# Activity Stats: Project ${e}`, ""];
  if (t.dayItems.length > 0) {
    r.push("## Daily Activity");
    for (const o of t.dayItems)
      r.push(`- ${o.date}: ${o.totalCount} completed`);
  } else
    r.push("No daily activity data available.");
  if (t.weekItems && t.weekItems.length > 0) {
    r.push(""), r.push("## Weekly Activity");
    for (const o of t.weekItems)
      r.push(`- ${o.fromDate} to ${o.toDate}: ${o.totalCount} completed`);
  }
  return r.join(`
`);
}
const oi = {
  name: g.GET_PROJECT_ACTIVITY_STATS,
  description: "Get daily and optional weekly task completion counts for a project over a configurable time window (1-12 weeks). Useful for identifying completion trends and patterns.",
  parameters: ti,
  outputSchema: si,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: r, weeks: o, includeWeeklyCounts: n } = e, a = await t.getProjectActivityStats(r, {
      weeks: o,
      includeWeeklyCounts: n
    });
    return {
      textContent: ri(r, a),
      structuredContent: {
        projectId: r,
        dayItems: a.dayItems,
        weekItems: a.weekItems ?? null
      }
    };
  }
}, ni = {
  projectId: s.string().min(1).describe("The ID of the project to check health for."),
  includeContext: s.boolean().default(!1).describe(
    "Include detailed health context with project metrics and task-level data. May produce large output for projects with many tasks."
  )
}, ai = s.object({
  taskId: s.string().describe("The ID of the task this recommendation is for."),
  recommendation: s.string().describe("The recommendation for this task.")
}), ii = s.object({
  id: s.string().describe("The task ID."),
  content: s.string().describe("The task content/title."),
  priority: s.string().describe("The task priority (1-4)."),
  due: s.string().nullable().optional().describe("The due date string, if set."),
  deadline: s.string().nullable().optional().describe("The deadline date string, if set."),
  isCompleted: s.boolean().describe("Whether the task is completed."),
  labels: s.array(s.string()).describe("Labels applied to this task.")
}), ci = {
  projectId: s.string().describe("The project ID."),
  projectName: s.string().describe("The project name."),
  progress: s.object({
    completedCount: s.number().describe("Number of completed tasks."),
    activeCount: s.number().describe("Number of active (incomplete) tasks."),
    progressPercent: s.number().describe("Completion percentage (0-100).")
  }).describe("Project completion progress."),
  health: s.object({
    status: s.enum(Ge).describe("The overall health status of the project."),
    description: s.string().nullable().optional().describe("Detailed description of the health assessment."),
    descriptionSummary: s.string().nullable().optional().describe("Brief summary of the health assessment."),
    taskRecommendations: s.array(ai).nullable().optional().describe("Specific recommendations for individual tasks."),
    isStale: s.boolean().describe("Whether the health data is stale and may need refreshing."),
    updateInProgress: s.boolean().describe("Whether a health analysis update is currently in progress."),
    updatedAt: s.string().nullable().optional().describe("When the health assessment was last updated.")
  }).describe("Project health assessment."),
  context: s.object({
    projectDescription: s.string().nullable().describe("The project description, if any."),
    projectMetrics: s.object({
      totalTasks: s.number().describe("Total number of tasks in the project."),
      completedTasks: s.number().describe("Number of completed tasks."),
      overdueTasks: s.number().describe("Number of overdue tasks."),
      tasksCreatedThisWeek: s.number().describe("Tasks created in the current week."),
      tasksCompletedThisWeek: s.number().describe("Tasks completed in the current week."),
      averageCompletionTime: s.number().nullable().describe("Average task completion time in days, if available.")
    }).describe("Aggregated project metrics."),
    tasks: s.array(ii).describe("Tasks in the project.")
  }).optional().describe(
    "Detailed project context with metrics and task data. Only included when includeContext is true."
  )
};
async function di(e, t, r) {
  if (r) {
    const [a, i, c] = await Promise.all([
      e.getProjectProgress(t),
      e.getProjectHealth(t),
      e.getProjectHealthContext(t)
    ]);
    return { progress: a, health: i, context: c };
  }
  const [o, n] = await Promise.all([
    e.getProjectProgress(t),
    e.getProjectHealth(t)
  ]);
  return { progress: o, health: n };
}
function li(e, { progress: t, health: r, context: o }) {
  const n = [`# Project Health: ${e}`, ""];
  if (n.push(`**Status:** ${r.status}`), r.isStale && n.push("**Note:** Health data is stale and may not reflect recent changes."), r.updateInProgress && n.push("**Note:** A health analysis update is currently in progress."), r.updatedAt && n.push(`**Last Updated:** ${r.updatedAt.toISOString()}`), n.push(""), n.push("## Progress"), n.push(
    `**Completed:** ${t.completedCount} | **Active:** ${t.activeCount} | **Progress:** ${t.progressPercent}%`
  ), r.description && (n.push(""), n.push("## Health Description"), n.push(r.description)), r.taskRecommendations && r.taskRecommendations.length > 0) {
    n.push(""), n.push("## Task Recommendations");
    for (const a of r.taskRecommendations)
      n.push(`- **Task ${a.taskId}**: ${a.recommendation}`);
  }
  if (o) {
    n.push(""), n.push("## Project Metrics");
    const a = o.projectMetrics;
    if (n.push(`- **Total Tasks:** ${a.totalTasks}`), n.push(`- **Completed:** ${a.completedTasks}`), n.push(`- **Overdue:** ${a.overdueTasks}`), n.push(`- **Created This Week:** ${a.tasksCreatedThisWeek}`), n.push(`- **Completed This Week:** ${a.tasksCompletedThisWeek}`), a.averageCompletionTime !== null && n.push(`- **Avg Completion Time:** ${a.averageCompletionTime} days`), o.tasks.length > 0) {
      n.push(""), n.push(`## Tasks (${o.tasks.length})`);
      for (const i of o.tasks) {
        const c = [`id=${i.id}`, i.content];
        i.due && c.push(`due=${i.due}`), i.deadline && c.push(`deadline=${i.deadline}`), i.isCompleted && c.push("(completed)"), n.push(`- ${c.join("; ")}`);
      }
    }
  }
  return n.join(`
`);
}
const ui = {
  name: g.GET_PROJECT_HEALTH,
  description: "Get a comprehensive health assessment for a project including completion progress, health status (EXCELLENT, ON_TRACK, AT_RISK, CRITICAL), and optional detailed context with project metrics and task-level recommendations. Use includeContext=true for full detail including task data.",
  parameters: ni,
  outputSchema: ci,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: r, includeContext: o } = e, n = await di(t, r, o), a = n.context?.projectName ?? `Project ${r}`, i = li(a, n), c = n.context ? {
      projectDescription: n.context.projectDescription,
      projectMetrics: n.context.projectMetrics,
      tasks: n.context.tasks.map((d) => ({
        id: d.id,
        content: d.content,
        priority: d.priority,
        due: d.due ?? null,
        deadline: d.deadline ?? null,
        isCompleted: d.isCompleted,
        labels: d.labels
      }))
    } : void 0;
    return {
      textContent: i,
      structuredContent: {
        projectId: r,
        projectName: a,
        progress: {
          completedCount: n.progress.completedCount,
          activeCount: n.progress.activeCount,
          progressPercent: n.progress.progressPercent
        },
        health: {
          status: n.health.status,
          description: n.health.description ?? null,
          descriptionSummary: n.health.descriptionSummary ?? null,
          taskRecommendations: n.health.taskRecommendations ?? null,
          isStale: n.health.isStale,
          updateInProgress: n.health.updateInProgress,
          updatedAt: n.health.updatedAt?.toISOString() ?? null
        },
        context: c
      }
    };
  }
}, pi = {
  workspaceIdOrName: s.string().min(1).describe(
    "The workspace ID or name. Supports exact ID, exact name match (case-insensitive), or unique partial name match."
  ),
  projectIds: s.array(s.string().min(1)).min(1).optional().describe("Optional list of project IDs to scope insights to specific projects.")
}, mi = s.object({
  projectId: s.string().describe("The project ID."),
  health: s.object({
    status: s.enum(Ge).describe("The health status of the project."),
    isStale: s.boolean().describe("Whether the health data is stale."),
    updateInProgress: s.boolean().describe("Whether a health analysis update is in progress.")
  }).nullable().describe("Health data for this project, if available."),
  progress: s.object({
    completedCount: s.number().describe("Number of completed tasks."),
    activeCount: s.number().describe("Number of active tasks."),
    progressPercent: s.number().describe("Completion percentage (0-100).")
  }).nullable().describe("Progress data for this project, if available.")
}), hi = {
  workspaceId: s.string().describe("The resolved workspace ID."),
  workspaceName: s.string().describe("The resolved workspace name."),
  folderId: s.string().nullable().describe("The folder ID, if applicable."),
  projectInsights: s.array(mi).describe("Health and progress insights for each project in the workspace.")
}, fi = {
  name: g.GET_WORKSPACE_INSIGHTS,
  description: "Get aggregated health and progress insights across all projects in a workspace. Accepts workspace name or ID, with optional project ID filtering. Useful for a cross-project health overview.",
  parameters: pi,
  outputSchema: hi,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { workspaceIdOrName: r, projectIds: o } = e, n = await is.resolveWorkspace(t, r), a = await t.getWorkspaceInsights(n.workspaceId, { projectIds: o }), i = a.projectInsights.map((d) => ({
      projectId: d.projectId,
      health: d.health ? {
        status: d.health.status,
        isStale: d.health.isStale,
        updateInProgress: d.health.updateInProgress
      } : null,
      progress: d.progress ? {
        completedCount: d.progress.completedCount,
        activeCount: d.progress.activeCount,
        progressPercent: d.progress.progressPercent
      } : null
    })), c = [
      `# Workspace Insights: ${n.workspaceName}`,
      "",
      `**Projects:** ${i.length}`,
      ""
    ];
    for (const d of i) {
      const l = d.health?.status ?? "N/A", u = d.progress ? `${d.progress.progressPercent}%` : "N/A";
      c.push(`- Project ${d.projectId}: status=${l}, progress=${u}`);
    }
    return {
      textContent: c.join(`
`),
      structuredContent: {
        workspaceId: n.workspaceId,
        workspaceName: n.workspaceName,
        folderId: a.folderId,
        projectInsights: i
      }
    };
  }
};
function bi(e) {
  const t = e.trim();
  if (!/^https?:\/\//i.test(t))
    return t;
  let r;
  try {
    r = new URL(t);
  } catch {
    throw new Error(`Could not read a template ID from "${t}".`);
  }
  if (!gi(r.hostname))
    throw new Error(
      `"${r.hostname}" is not a Todoist domain. Pass a todoist.com template URL or the template ID itself.`
    );
  const o = r.pathname.split("/").filter(Boolean);
  if (!o.includes("templates"))
    throw new Error(`"${t}" is not a Todoist template URL.`);
  const n = o.at(-1), a = n === "view" ? o.at(-2) : n;
  if (!a)
    throw new Error(`Could not read a template ID from "${t}".`);
  return a;
}
function gi(e) {
  const t = e.toLowerCase();
  return t === "todoist.com" || t.endsWith(".todoist.com");
}
const yi = {
  projectId: s.string().min(1).describe("The ID of the existing project to import the template into."),
  templateId: s.string().optional().describe(
    'The template to import — either a gallery slug ("product-launch"), a personal template ID ("UT_28Ex..."), or a full Todoist template URL, which is reduced to the ID automatically. Gallery templates work for anyone; personal templates only for the account that owns them. There is no way to list templates through this server, so only use the ID or URL the user supplied. Provide either this or `csvFileContent`, not both.'
  ),
  csvFileContent: s.string().optional().describe(
    "Raw CSV template content, as produced by export-project-template. Provide either this or `templateId`, not both."
  ),
  locale: s.string().optional().describe('Locale for the imported content when using `templateId`. Defaults to "en".')
}, Ti = {
  sections: s.array(X).describe("Sections created by the import."),
  tasks: s.array(F).describe("Tasks created by the import."),
  comments: s.array(ye).describe("Comments created by the import."),
  totalCount: s.number().describe("The total number of objects created by the import.")
}, ki = {
  name: g.IMPORT_PROJECT_TEMPLATE,
  description: "Import a template into an existing project, adding its tasks, sections and comments to whatever is already there. Source it by template ID/URL or by passing CSV content from export-project-template. To start a new project from a template, create the project with add-projects first, then import into it. This writes immediately and cannot be undone, so only run it against a project the user named.",
  parameters: yi,
  outputSchema: Ti,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ projectId: e, templateId: t, csvFileContent: r, locale: o }, n) {
    if (!!t == !!r)
      throw new Error(
        "Provide exactly one template source: either `templateId` or `csvFileContent`. Templates cannot be listed through this server — ask the user for an ID or template URL."
      );
    const a = t ? await n.importTemplateFromId({
      projectId: e,
      templateId: bi(t),
      locale: o
    }) : await n.importTemplateIntoProject({
      projectId: e,
      file: r ?? "",
      fileName: "template.csv"
    }), i = a.sections.map(K), c = a.tasks.map(P), d = a.comments.map(ge), l = a.projects.length + i.length + c.length + d.length;
    return {
      textContent: wi({
        projectId: e,
        counts: {
          projects: a.projects.length,
          sections: i.length,
          tasks: c.length,
          comments: d.length
        },
        totalCount: l
      }),
      structuredContent: { sections: i, tasks: c, comments: d, totalCount: l }
    };
  }
};
function wi({
  projectId: e,
  counts: t,
  totalCount: r
}) {
  if (r === 0)
    return `Imported template into project (id=${e}), but it created nothing.`;
  const o = Object.entries(t).filter(([, n]) => n > 0).map(([n, a]) => `${a} ${a === 1 ? n.replace(/s$/, "") : n}`);
  return `Imported template into project (id=${e}): ${o.join(", ")}.`;
}
const vi = {}, Ii = {
  id: s.string().describe("The unique identifier for the workspace."),
  name: s.string().describe("The name of the workspace."),
  plan: s.enum(Hs).describe("The workspace plan type."),
  role: s.enum(Fs).optional().describe("The user's role in the workspace, if available."),
  isLinkSharingEnabled: s.boolean().describe("Whether link sharing is enabled for the workspace."),
  isGuestAllowed: s.boolean().describe("Whether guests are allowed in the workspace."),
  createdAt: s.string().optional().describe("The ISO 8601 timestamp when the workspace was created."),
  creatorId: s.string().describe("The ID of the user who created the workspace.")
}, Si = {
  type: s.literal("workspaces").describe("The type of the response."),
  workspaces: s.array(s.object(Ii)).describe("List of workspaces."),
  count: s.number().describe("The total number of workspaces.")
};
async function ji(e) {
  const r = (await e.getWorkspaces()).map((i) => ({
    id: i.id,
    name: i.name,
    plan: i.plan,
    role: i.role,
    isLinkSharingEnabled: i.isLinkSharingEnabled,
    isGuestAllowed: i.isGuestAllowed,
    createdAt: i.createdAt?.toISOString(),
    creatorId: i.creatorId
  })), o = ["# Workspaces", ""];
  if (r.length === 0)
    o.push("No workspaces found.");
  else {
    o.push(
      `Found ${r.length} workspace${r.length === 1 ? "" : "s"}:`,
      ""
    );
    for (const i of r)
      o.push(`## ${i.name}`), o.push(`- **ID:** ${i.id}`), o.push(`- **Plan:** ${i.plan}`), i.role && o.push(`- **Your Role:** ${i.role}`), o.push(
        `- **Link Sharing:** ${i.isLinkSharingEnabled ? "Enabled" : "Disabled"}`
      ), o.push(`- **Guests Allowed:** ${i.isGuestAllowed ? "Yes" : "No"}`), i.createdAt && o.push(`- **Created:** ${i.createdAt}`), o.push(`- **Creator ID:** ${i.creatorId}`), o.push("");
  }
  const n = o.join(`
`), a = {
    type: "workspaces",
    workspaces: r,
    count: r.length
  };
  return { textContent: n, structuredContent: a };
}
const Ci = {
  name: g.LIST_WORKSPACES,
  description: "Get all workspaces for the authenticated user. Returns workspace details including ID, name, plan type (STARTER/BUSINESS), user role (ADMIN/MEMBER/GUEST), link sharing settings, guest permissions, creation date, and creator ID.",
  parameters: vi,
  outputSchema: Si,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = await ji(t);
    return {
      textContent: r.textContent,
      structuredContent: r.structuredContent
    };
  }
}, { FIND_TASKS: $i, FIND_PROJECT_COLLABORATORS: Et, UPDATE_TASKS: Di } = g, xi = 50, Ai = {
  operation: s.enum(["assign", "unassign", "reassign"]).describe("The assignment operation to perform."),
  taskIds: s.array(s.string()).min(1).max(xi).describe("The IDs of the tasks to operate on (max 50)."),
  responsibleUser: s.string().optional().describe(
    'The user to assign tasks to. Can be "me" (assigns to current user), a user ID, name, or email. Required for assign and reassign operations.'
  ),
  fromAssigneeUser: s.string().optional().describe(
    "For reassign operations: the current assignee to reassign from. Can be user ID, name, or email. Optional - if not provided, reassigns from any current assignee."
  ),
  dryRun: s.boolean().optional().default(!1).describe("If true, validates operations without executing them.")
}, Ei = {
  results: s.array(
    s.object({
      taskId: s.string().describe("The ID of the task."),
      success: s.boolean().describe("Whether the operation was successful."),
      error: s.string().optional().describe("Error message if the operation failed."),
      originalAssigneeId: s.string().optional().describe("The original assignee ID before the operation."),
      newAssigneeId: s.string().optional().describe("The new assignee ID after the operation.")
    })
  ).describe("Results of the assignment operations."),
  summary: s.object({
    total: s.number().describe("Total number of tasks processed."),
    succeeded: s.number().describe("Number of successful operations."),
    failed: s.number().describe("Number of failed operations."),
    dryRun: s.boolean().describe("Whether this was a dry run.")
  }).optional().describe("Summary of the operation.")
}, Oi = {
  name: g.MANAGE_ASSIGNMENTS,
  description: "Bulk assignment operations for multiple tasks. Supports assign, unassign, and reassign operations with atomic rollback on failures.",
  parameters: Ai,
  outputSchema: Ei,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { operation: r, taskIds: o, responsibleUser: n, fromAssigneeUser: a, dryRun: i } = e;
    if ((r === "assign" || r === "reassign") && !n)
      throw new Error(`${r} operation requires responsibleUser parameter`);
    const c = await Promise.allSettled(
      o.map(async (k) => {
        try {
          return await t.getTask(k);
        } catch {
          throw new Error(`Task ${k} not found or not accessible`);
        }
      })
    ), d = [], l = [];
    for (let k = 0; k < c.length; k++) {
      const I = c[k];
      I && I.status === "fulfilled" ? d.push(I.value) : I && I.status === "rejected" ? l.push({
        taskId: o[k] || "invalid-task-id",
        success: !1,
        error: I.reason?.message || "Task not accessible"
      }) : l.push({
        taskId: o[k] || "invalid-task-id",
        success: !1,
        error: "Task not accessible"
      });
    }
    if (d.length === 0) {
      const k = l.map((I) => `"${I.taskId}": ${I.error}`).join("; ");
      throw new Error(`All ${l.length} task(s) failed: ${k}`);
    }
    let u;
    r === "reassign" && a && (u = (await W.resolveUser(t, a))?.userId || a);
    const p = [];
    for (const k of d)
      r === "reassign" && u && k.responsibleUid !== u || p.push({
        taskId: k.id,
        projectId: k.projectId,
        responsibleUid: n || ""
        // Will be validated appropriately
      });
    if (r === "unassign") {
      if (i) {
        const b = d.map(($) => ({
          taskId: $.id,
          success: !0,
          originalAssigneeId: $.responsibleUid ?? void 0,
          newAssigneeId: void 0
        }));
        return {
          textContent: Oe({
            operation: r,
            results: b,
            dryRun: !0
          }),
          structuredContent: {
            operation: r,
            results: b,
            totalRequested: o.length,
            successful: b.length,
            failed: l.length,
            dryRun: !0
          }
        };
      }
      const k = d.map(async (b) => {
        try {
          return await t.updateTask(b.id, { assigneeId: null }), {
            taskId: b.id,
            success: !0,
            originalAssigneeId: b.responsibleUid ?? void 0,
            newAssigneeId: void 0
          };
        } catch (S) {
          return {
            taskId: b.id,
            success: !1,
            error: S instanceof Error ? S.message : "Update failed",
            originalAssigneeId: b.responsibleUid ?? void 0
          };
        }
      }), I = await Promise.all(k), x = [...I, ...l];
      if (x.length > 0 && x.every((b) => !b.success)) {
        const b = x.map((S) => `"${S.taskId}": ${S.error}`).join("; ");
        throw new Error(`All ${x.length} unassign operation(s) failed: ${b}`);
      }
      return {
        textContent: Oe({
          operation: r,
          results: x,
          dryRun: !1
        }),
        structuredContent: {
          operation: r,
          results: x,
          totalRequested: o.length,
          successful: I.filter((b) => b.success).length,
          failed: x.filter((b) => !b.success).length,
          dryRun: !1
        }
      };
    }
    const m = await it.validateBulkAssignment(
      t,
      p
    ), h = [], y = [];
    for (let k = 0; k < p.length; k++) {
      const I = p[k], x = m[k];
      I && x?.isValid ? h.push({ assignment: I, validation: x }) : I?.taskId && y.push({
        taskId: I.taskId,
        success: !1,
        error: x?.error?.message || "Validation failed"
      });
    }
    async function f(k, I) {
      const x = k.filter(
        (b) => b.assignment != null && b.validation != null
      );
      if (!I)
        return x.map(({ assignment: b, validation: S }) => {
          const $ = d.find((D) => D.id === b.taskId);
          if (!b.taskId || !S.resolvedUser?.userId)
            throw new Error(
              "Invalid assignment or validation data - this should not happen"
            );
          return {
            taskId: b.taskId,
            success: !0,
            originalAssigneeId: $?.responsibleUid ?? void 0,
            newAssigneeId: S.resolvedUser.userId
          };
        });
      const T = x.map(
        async ({ assignment: b, validation: S }) => {
          const $ = d.find((D) => D.id === b.taskId);
          if (!b.taskId || !S.resolvedUser?.userId)
            return {
              taskId: b.taskId || "unknown-task",
              success: !1,
              error: "Invalid assignment data - missing task ID or resolved user",
              originalAssigneeId: $?.responsibleUid ?? void 0
            };
          try {
            return await t.updateTask(b.taskId, {
              assigneeId: S.resolvedUser.userId
            }), {
              taskId: b.taskId,
              success: !0,
              originalAssigneeId: $?.responsibleUid ?? void 0,
              newAssigneeId: S.resolvedUser.userId
            };
          } catch (D) {
            return {
              taskId: b.taskId,
              success: !1,
              error: D instanceof Error ? D.message : "Update failed",
              originalAssigneeId: $?.responsibleUid ?? void 0
            };
          }
        }
      );
      return Promise.all(T);
    }
    const v = await f(h, !i), j = [...v, ...y, ...l];
    if (j.length > 0 && j.every((k) => !k.success)) {
      const k = j.map((I) => `"${I.taskId}": ${I.error}`).join("; ");
      throw new Error(`All ${j.length} ${r} operation(s) failed: ${k}`);
    }
    return {
      textContent: Oe({
        operation: r,
        results: j,
        dryRun: i
      }),
      structuredContent: {
        operation: r,
        results: j,
        totalRequested: o.length,
        successful: v.filter((k) => k.success).length,
        failed: j.filter((k) => !k.success).length,
        dryRun: i
      }
    };
  }
};
function Oe({
  operation: e,
  results: t,
  dryRun: r
}) {
  const o = t.filter((d) => d.success), n = t.filter((d) => !d.success), a = r ? "would be" : "were", i = {
    assign: "assigned",
    unassign: "unassigned",
    reassign: "reassigned"
  }[e];
  let c = `**${r ? "Dry Run: " : ""}Bulk ${e} operation**

`;
  if (o.length > 0) {
    c += `**${o.length} task${o.length === 1 ? "" : "s"} ${a} successfully ${i}**
`;
    const d = o.slice(0, 5);
    for (const l of d) {
      let u = "";
      e === "unassign" ? u = " (unassigned from previous assignee)" : l.newAssigneeId && (u = ` → ${l.newAssigneeId}`), c += `  • Task ${l.taskId}${u}
`;
    }
    o.length > 5 && (c += `  • ... and ${o.length - 5} more
`), c += `
`;
  }
  if (n.length > 0) {
    c += `**${n.length} task${n.length === 1 ? "" : "s"} failed**
`;
    const d = n.slice(0, 5);
    for (const l of d)
      c += `  • Task ${l.taskId}: ${l.error}
`;
    n.length > 5 && (c += `  • ... and ${n.length - 5} more failures
`), c += `
`;
  }
  return !r && o.length > 0 ? (c += `**Next steps:**
`, c += `• Use ${$i} with responsibleUser to see ${e === "unassign" ? "unassigned" : "newly assigned"} tasks
`, c += `• Use ${Di} for individual assignment changes
`, n.length > 0 && (c += `• Check failed tasks and use ${Et} to verify collaborator access
`)) : r ? (c += `**To execute:**
`, c += `• Remove dryRun parameter and run again to execute changes
`, o.length > 0 && (c += `• ${o.length} task${o.length === 1 ? "" : "s"} ready for ${e} operation
`), n.length > 0 && (c += `• Fix ${n.length} validation error${n.length === 1 ? "" : "s"} before executing
`)) : o.length === 0 && (c += `**Suggestions:**
`, c += `• Use ${Et} to find valid assignees
`, c += `• Check task IDs and assignee permissions
`, c += `• Use dryRun=true to validate before executing
`), c;
}
const Pi = {
  action: s.enum(["archive", "unarchive"]).describe("The action to perform on the project."),
  projectId: s.string().min(1).describe("The ID of the project.")
}, _i = {
  project: Q.describe("The updated project."),
  success: s.boolean().describe("Whether the action was successful.")
}, Ri = {
  name: g.PROJECT_MANAGEMENT,
  description: "Archive or unarchive a project by its ID.",
  parameters: Pi,
  outputSchema: _i,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = e.action === "archive" ? await t.archiveProject(e.projectId) : await t.unarchiveProject(e.projectId), o = Z(r);
    return {
      textContent: `${e.action === "archive" ? "Archived" : "Unarchived"} project: ${o.name} (id=${o.id})`,
      structuredContent: {
        project: o,
        success: !0
      }
    };
  }
}, Ui = {
  action: s.enum(["move-to-workspace", "move-to-personal"]).describe("The action to perform on the project."),
  projectId: s.string().min(1).describe("The ID of the project to move."),
  workspaceId: s.string().min(1).optional().describe("The target workspace ID. Required when action is move-to-workspace."),
  folderId: s.string().min(1).optional().describe("Optional target folder ID within the workspace."),
  visibility: s.enum(["restricted", "team", "public"]).optional().describe(
    "Optional access visibility for the project in the workspace (restricted, team, or public)."
  )
}, Li = {
  project: Q.describe("The moved project."),
  success: s.boolean().describe("Whether the move was successful.")
}, Ni = {
  name: g.PROJECT_MOVE,
  description: "Move a project between personal and workspace contexts.",
  parameters: Ui,
  outputSchema: Li,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    let r;
    if (e.action === "move-to-workspace") {
      if (!e.workspaceId)
        throw new Error("workspaceId is required when action is move-to-workspace");
      const a = {
        projectId: e.projectId,
        workspaceId: e.workspaceId
      };
      e.folderId && (a.folderId = e.folderId), e.visibility && (a.access = { visibility: e.visibility }), r = await t.moveProjectToWorkspace(a);
    } else
      r = await t.moveProjectToPersonal({ projectId: e.projectId });
    const o = Z(r);
    return {
      textContent: `${e.action === "move-to-workspace" ? "Moved to workspace" : "Moved to personal"}: ${o.name} (id=${o.id})`,
      structuredContent: {
        project: o,
        success: !0
      }
    };
  }
}, Is = ["project", "section"], Mi = {
  type: s.enum(Is).describe(
    'The type of entity to reorder. "project" reorders sibling projects within the same parent (and can move projects to a new parent). "section" reorders sections within the same project.'
  ),
  items: s.array(
    s.object({
      id: s.string().min(1).describe("The ID of the entity."),
      order: s.number().int().min(0).optional().describe(
        "The new position/order value for the entity. Lower values appear first."
      ),
      parentId: s.string().min(1).optional().describe(
        'Move a project to be a sub-project of this parent project ID, or use "root" to move to the top level. Only valid when type is "project".'
      )
    })
  ).min(1).describe(
    "The items to reorder or move. Each item must have at least order or parentId. Items with parentId will be moved first, then items with order will be reordered. All items being reordered should be siblings for predictable results."
  )
}, Fi = {
  type: s.enum(Is).describe("The type of entity that was reordered/moved."),
  movedCount: s.number().describe("The number of entities moved to a new parent."),
  reorderedCount: s.number().describe("The number of entities reordered."),
  affectedIds: s.array(s.string()).describe("The IDs of all affected entities."),
  success: s.boolean().describe("Whether the operation was successful.")
}, Hi = {
  name: g.REORDER_OBJECTS,
  description: 'Reorder sibling projects or sections, and optionally move projects to a new parent. For projects: set order to reorder siblings, and/or set parentId to move under a new parent (use "root" for top level). For sections: set order to reorder within a project.',
  parameters: Mi,
  outputSchema: Fi,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { type: r, items: o } = e, n = /* @__PURE__ */ new Set();
    for (const p of o) {
      if (n.has(p.id))
        throw new Error(`Duplicate item id=${p.id}. Each item must appear only once.`);
      if (n.add(p.id), p.order === void 0 && p.parentId === void 0)
        throw new Error(
          `Item id=${p.id} must have at least one of "order" or "parentId".`
        );
      if (p.parentId !== void 0 && r !== "project")
        throw new Error(
          `parentId is only supported when type is "project", but type is "${r}".`
        );
    }
    const a = [], i = o.filter((p) => p.parentId !== void 0);
    for (const p of i) {
      const m = p.parentId;
      a.push(
        V("project_move", {
          id: p.id,
          parentId: m === "root" ? null : m
        })
      );
    }
    const c = o.filter(
      (p) => p.order !== void 0
    );
    c.length > 0 && (r === "project" ? a.push(
      V("project_reorder", {
        projects: c.map((p) => ({
          id: p.id,
          childOrder: p.order
        }))
      })
    ) : a.push(
      V("section_reorder", {
        sections: c.map((p) => ({
          id: p.id,
          sectionOrder: p.order
        }))
      })
    ));
    try {
      await t.sync({ commands: a });
    } catch (p) {
      const m = p instanceof Error ? p.message : String(p);
      throw new Error(`Reorder failed: ${m}`);
    }
    const d = r === "project" ? "projects" : "sections", l = o.map((p) => p.id), u = [];
    return i.length > 0 && u.push(`moved ${i.length}`), c.length > 0 && u.push(`reordered ${c.length}`), {
      textContent: `${u.join(" and ")} ${d}: ${l.map((p) => `id=${p}`).join(", ")}`,
      structuredContent: {
        type: r,
        movedCount: i.length,
        reorderedCount: c.length,
        affectedIds: l,
        success: !0
      }
    };
  }
}, Wi = s.object({
  id: s.string().min(1).describe("The ID of the task to reschedule."),
  date: s.string().min(1).describe(
    "The new date for the task. Use YYYY-MM-DD for date-only, or YYYY-MM-DDTHH:MM:SS for datetime. If date-only is provided and the task already has a specific time, the existing time is preserved."
  )
}), Bi = {
  tasks: s.array(Wi).min(1).describe("The tasks to reschedule with their new dates.")
}, zi = {
  tasks: s.array(F).describe("The rescheduled tasks."),
  totalCount: s.number().describe("The total number of tasks rescheduled."),
  rescheduledTaskIds: s.array(s.string()).describe("The IDs of the rescheduled tasks.")
}, Ki = {
  name: g.RESCHEDULE_TASKS,
  description: "Reschedule tasks to new dates while preserving recurring schedules. Unlike update-tasks (which replaces the entire due string and can wipe recurrence), this tool changes only the date, keeping recurrence patterns intact. Use this when moving recurring tasks to a different date without altering their repeat pattern.",
  parameters: Bi,
  outputSchema: zi,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: r } = e, n = (await Promise.all(
      r.map(async (d) => {
        const l = await t.getTask(d.id);
        return { input: d, task: l };
      })
    )).map(({ input: d, task: l }) => {
      if (!l.due)
        throw new Error(
          `Task "${l.content}" (${l.id}) has no due date. Rescheduling requires an existing due date.`
        );
      const u = qi(d.date, l.due);
      return V("item_update", {
        id: d.id,
        due: {
          date: u,
          string: l.due.string,
          isRecurring: l.due.isRecurring,
          timezone: l.due.timezone ?? void 0,
          lang: l.due.lang ?? void 0
        }
      });
    });
    try {
      await t.sync({ commands: n });
    } catch (d) {
      const l = d instanceof Error ? d.message : String(d);
      throw new Error(`Reschedule failed: ${l}`);
    }
    const a = await Promise.all(r.map((d) => t.getTask(d.id))), i = a.map(P);
    return {
      textContent: ot("Rescheduled", i, {
        showDetails: i.length <= 5
      }),
      structuredContent: {
        tasks: i,
        totalCount: i.length,
        rescheduledTaskIds: a.map((d) => d.id)
      }
    };
  }
};
function qi(e, t) {
  if (/^\d{4}-\d{2}-\d{2}$/.test(e) && t.datetime) {
    const o = t.datetime.substring(10);
    return e + o;
  }
  return e;
}
const Yi = {
  query: s.string().min(1).describe("The search query string to find tasks and projects.")
}, Gi = {
  results: s.array(
    s.object({
      id: s.string().describe("The ID of the result."),
      title: s.string().describe("The title of the result."),
      url: s.string().describe("The URL of the result.")
    })
  ).describe("The search results."),
  totalCount: s.number().describe("Total number of results found.")
}, Vi = {
  name: g.SEARCH,
  description: "Search across tasks and projects in Todoist. Returns a list of relevant results with IDs, titles, and URLs.",
  parameters: Yi,
  outputSchema: Gi,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { query: r } = e, [o, n] = await Promise.all([
      tt({
        client: t,
        query: `search: ${r}`,
        limit: C.TASKS_MAX,
        cursor: void 0
      }),
      rs(t, r)
    ]), a = [];
    for (const i of o.tasks)
      a.push({
        id: `task:${i.id}`,
        title: i.content,
        url: Mt(i.id)
      });
    for (const i of n)
      a.push({
        id: `project:${i.id}`,
        title: i.name,
        url: Ft(i.id)
      });
    return {
      textContent: JSON.stringify({ results: a }),
      structuredContent: { results: a, totalCount: a.length }
    };
  }
}, Ji = {
  ids: s.array(s.string().min(1)).min(1).describe("The IDs of the tasks to uncomplete.")
}, Xi = {
  uncompleted: s.array(s.string()).describe("The IDs of successfully uncompleted tasks."),
  failures: s.array(ee).describe("Failed task uncompletion with error details."),
  totalRequested: s.number().describe("The total number of tasks requested to uncomplete."),
  successCount: s.number().describe("The number of successfully uncompleted tasks."),
  failureCount: s.number().describe("The number of failed task uncompletions.")
}, Zi = {
  name: g.UNCOMPLETE_TASKS,
  description: "Uncomplete (reopen) one or more completed tasks by their IDs.",
  parameters: Ji,
  outputSchema: Xi,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const r = [], o = [];
    for (const a of e.ids)
      try {
        await t.reopenTask(a), r.push(a);
      } catch (i) {
        const c = i instanceof Error ? i.message : "Unknown error";
        o.push({
          item: a,
          error: c
        });
      }
    return {
      textContent: Qi({
        uncompleted: r,
        failures: o,
        args: e
      }),
      structuredContent: {
        uncompleted: r,
        failures: o,
        totalRequested: e.ids.length,
        successCount: r.length,
        failureCount: o.length
      }
    };
  }
};
function Qi({
  uncompleted: e,
  failures: t,
  args: r
}) {
  return nt({
    action: "Uncompleted tasks",
    success: e.length,
    total: r.ids.length,
    successItems: e,
    successLabel: "Reopened",
    failures: t
  });
}
const ec = s.object({
  id: s.string().min(1).describe("The ID of the comment to update."),
  content: s.string().min(1).describe("The new content for the comment.")
}), tc = {
  comments: s.array(ec).min(1).describe("The comments to update.")
}, sc = {
  comments: s.array(ye).describe("The updated comments."),
  totalCount: s.number().describe("The total number of comments updated."),
  updatedCommentIds: s.array(s.string()).describe("The IDs of the updated comments."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of comments updated.")
  }).describe("Summary of operations performed.")
}, rc = {
  name: g.UPDATE_COMMENTS,
  description: "Update multiple existing comments with new content.",
  parameters: tc,
  outputSchema: sc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { comments: r } = e, o = r.map(async (c) => await t.updateComment(c.id, { content: c.content })), a = (await Promise.all(o)).map(ge);
    return {
      textContent: oc({
        comments: a
      }),
      structuredContent: {
        comments: a,
        totalCount: a.length,
        updatedCommentIds: a.map((c) => c.id),
        appliedOperations: {
          updateCount: a.length
        }
      }
    };
  }
};
function oc({ comments: e }) {
  const t = e.filter((a) => a.taskId).length, r = e.filter((a) => a.projectId).length, o = [];
  if (t > 0) {
    const a = t > 1 ? "comments" : "comment";
    o.push(`${t} task ${a}`);
  }
  if (r > 0) {
    const a = r > 1 ? "comments" : "comment";
    o.push(`${r} project ${a}`);
  }
  return o.length > 0 ? `Updated ${o.join(" and ")}` : "No comments updated";
}
const nc = s.object({
  id: s.string().min(1).describe("The ID of the filter to update."),
  name: s.string().min(1).optional().describe("The new name of the filter."),
  query: s.string().min(1).optional().describe(
    'The new filter query string. Examples: "today & p1", "#Work & overdue", "@email & today".'
  ),
  color: le,
  isFavorite: s.boolean().optional().describe("Whether to mark the filter as a favorite.")
}), ac = {
  filters: s.array(nc).min(1).describe("The filters to update.")
}, ic = {
  filters: s.array(rt).describe("The updated filters."),
  totalCount: s.number().describe("The total number of filters updated."),
  updatedFilterIds: s.array(s.string()).describe("The IDs of the updated filters."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of filters actually updated."),
    skippedCount: s.number().describe("The number of filters skipped (no changes).")
  }).describe("Summary of operations performed.")
}, cc = {
  name: g.UPDATE_FILTERS,
  description: "Update one or more existing personal filters with new values.",
  parameters: ac,
  outputSchema: ic,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { filters: r } = e, o = [], a = r.map((m) => {
      const h = lc(m);
      return h !== null ? { kind: "skipped", reason: h } : (o.push(m), { kind: "updated", filter: m });
    }).filter((m) => m.kind === "skipped").length;
    if (o.length === 0)
      return {
        textContent: `Updated 0 filters (${a} skipped - no changes)`,
        structuredContent: {
          filters: [],
          totalCount: 0,
          updatedFilterIds: [],
          appliedOperations: { updateCount: 0, skippedCount: a }
        }
      };
    const i = o.map((m) => {
      const { id: h, color: y, ...f } = m;
      return V("filter_update", {
        id: h,
        ...f,
        ...y !== void 0 ? { color: y } : {}
      });
    });
    await t.sync({ commands: i });
    const d = ((await t.sync({ resourceTypes: ["filters"], syncToken: "*" })).filters ?? []).filter((m) => !m.isDeleted), l = new Set(o.map((m) => m.id)), u = d.filter((m) => l.has(m.id)).map((m) => ({
      id: m.id,
      name: m.name,
      query: m.query,
      color: ue.parse(m.color),
      isFavorite: m.isFavorite,
      itemOrder: m.itemOrder
    }));
    return {
      textContent: dc({ filters: u, skippedCount: a }),
      structuredContent: {
        filters: u,
        totalCount: u.length,
        updatedFilterIds: u.map((m) => m.id),
        appliedOperations: {
          updateCount: u.length,
          skippedCount: a
        }
      }
    };
  }
};
function dc({
  filters: e,
  skippedCount: t
}) {
  const r = e.length;
  let o = `Updated ${r} filter${r === 1 ? "" : "s"}`;
  if (t > 0 && (o += ` (${t} skipped - no changes)`), r > 0) {
    const n = e.map((a) => `• ${a.name} (id=${a.id})`).join(`
`);
    o += `:
${n}`;
  }
  return o;
}
function lc({ id: e, ...t }) {
  return Object.values(t).every((o) => o === void 0) ? "no-fields" : null;
}
const uc = s.object({
  labelType: s.literal("personal").describe("Update a personal label, identified by its ID."),
  id: s.string().min(1).describe("The ID of the personal label to update."),
  name: s.string().min(1).max(128).optional().describe("The new name of the label."),
  color: le,
  order: s.number().int().optional().describe("The new position of the label in the label list."),
  isFavorite: s.boolean().optional().describe("Whether to mark the label as a favorite.")
}), pc = s.object({
  labelType: s.literal("shared").describe(
    "Rename a shared label across all tasks. Shared labels are identified by name; only renaming is supported (no color, order, or favorite)."
  ),
  name: s.string().min(1).describe("The current name of the shared label."),
  newName: s.string().min(1).describe("The new name for the shared label.")
}), mc = s.discriminatedUnion("labelType", [
  uc,
  pc
]), hc = {
  labels: s.array(mc).min(1).describe(
    'The labels to update. Use labelType="personal" with an ID to update a personal label, or labelType="shared" with name+newName to rename a shared label.'
  )
}, fc = s.object({
  name: s.string().describe("The previous name of the shared label."),
  newName: s.string().describe("The new name of the shared label.")
}), bc = {
  updatedLabels: s.array(pe).describe("The updated personal labels."),
  renamedSharedLabels: s.array(fc).describe("The shared labels that were renamed."),
  totalCount: s.number().describe("The total number of successful operations (personal + shared)."),
  appliedOperations: s.object({
    updateCount: s.number().describe("Number of personal labels updated."),
    renameCount: s.number().describe("Number of shared labels renamed."),
    skippedCount: s.number().describe("Number of operations skipped (no changes or shared label not found).")
  }).describe("Summary of operations performed.")
}, gc = {
  name: g.UPDATE_LABELS,
  description: "Update one or more existing labels. Personal labels (identified by ID) can have their name, color, order, and favorite flag updated. Shared labels (identified by name) can only be renamed.",
  parameters: hc,
  outputSchema: bc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { labels: r } = e, o = await Promise.all(
      r.map(async (u) => {
        if (u.labelType === "personal") {
          const m = Tc(u);
          if (m !== null) return { kind: "skipped", reason: m };
          const { id: h, labelType: y, ...f } = u;
          return { kind: "updated", label: await t.updateLabel(h, f) };
        }
        return await t.renameSharedLabel({
          name: u.name,
          newName: u.newName
        }) ? { kind: "renamed", name: u.name, newName: u.newName } : { kind: "skipped", reason: "not-found" };
      })
    ), n = o.filter((u) => u.kind === "updated").map((u) => pe.parse(u.label)), a = o.filter(
      (u) => u.kind === "renamed"
    ).map(({ name: u, newName: p }) => ({ name: u, newName: p })), i = o.filter((u) => u.kind === "skipped").length, c = o.filter(
      (u) => u.kind === "skipped" && u.reason === "no-fields"
    ).length, d = o.filter(
      (u) => u.kind === "skipped" && u.reason === "not-found"
    ).length;
    return {
      textContent: yc({
        updatedLabels: n,
        renamedSharedLabels: a,
        skippedNoFields: c,
        skippedNotFound: d
      }),
      structuredContent: {
        updatedLabels: n,
        renamedSharedLabels: a,
        totalCount: n.length + a.length,
        appliedOperations: {
          updateCount: n.length,
          renameCount: a.length,
          skippedCount: i
        }
      }
    };
  }
};
function yc({
  updatedLabels: e,
  renamedSharedLabels: t,
  skippedNoFields: r,
  skippedNotFound: o
}) {
  const n = e.length, a = t.length, i = n + a, c = [];
  c.push(
    `Updated ${i} label${i === 1 ? "" : "s"} (${n} personal, ${a} shared)`
  );
  const d = [];
  if (r > 0 && d.push(`${r} skipped - no changes`), o > 0 && d.push(`${o} skipped - shared label not found`), d.length > 0 && (c[0] += ` (${d.join(", ")})`), n > 0) {
    c.push("Personal:");
    for (const l of e) c.push(`• ${l.name} (id=${l.id})`);
  }
  if (a > 0) {
    c.push("Shared:");
    for (const l of t) c.push(`• ${l.name} → ${l.newName}`);
  }
  return c.join(`
`);
}
function Tc({
  id: e,
  labelType: t,
  ...r
}) {
  return Object.values(r).every((n) => n === void 0) ? "no-fields" : null;
}
const kc = s.object({
  id: s.string().min(1).describe("The ID of the project to update."),
  name: s.string().min(1).optional().describe("The new name of the project."),
  isFavorite: s.boolean().optional().describe("Whether the project is a favorite."),
  viewStyle: s.enum(["list", "board", "calendar"]).optional().describe("The project view style."),
  description: s.preprocess(
    // `null` is the advertised clear value; the param schema stays a
    // plain string (Gemini forbids nullable schemas, not preprocessing),
    // so `null` is normalised to "" before the project wire clear.
    (e) => e === null ? "" : e,
    s.string().describe(
      "The description of the project (Markdown). Pass null (or an empty string) to clear it."
    )
  ).optional(),
  color: le
}), wc = {
  projects: s.array(kc).min(1).describe("The projects to update.")
}, vc = {
  projects: s.array(Q).describe("The updated projects."),
  totalCount: s.number().describe("The total number of projects updated."),
  updatedProjectIds: s.array(s.string()).describe("The IDs of the updated projects."),
  failures: s.array(ee).describe(
    "Projects that could not be updated, with the reason for each. A failure here does not affect the other projects in the batch — do not retry the whole batch; address or drop the failed items."
  ),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of projects actually updated."),
    skippedCount: s.number().describe("The number of projects skipped (no changes)."),
    failureCount: s.number().describe("The number of projects that failed to update.")
  }).describe("Summary of operations performed.")
}, Ic = {
  name: g.UPDATE_PROJECTS,
  description: "Update multiple existing projects with new values.",
  parameters: wc,
  outputSchema: vc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { projects: r } = e, o = await Promise.allSettled(
      r.map(async (l) => {
        const u = jc(l);
        if (u !== null) return { kind: "skipped", reason: u };
        const { id: p, ...m } = l;
        return { kind: "updated", project: await O(() => t.updateProject(p, m)) };
      })
    ), n = [], a = [];
    let i = 0, c = 0;
    for (const [l, u] of o.entries()) {
      if (u.status === "rejected") {
        a.push({
          item: r[l]?.id ?? `Project ${l + 1}`,
          error: z(u.reason)
        });
        continue;
      }
      u.value.kind === "updated" ? n.push(Z(u.value.project)) : u.value.reason === "no-fields" ? i++ : c++;
    }
    return {
      textContent: Sc({
        projects: n,
        skippedNoFields: i,
        skippedNoValidValues: c,
        failures: a
      }),
      structuredContent: {
        projects: n,
        totalCount: n.length,
        updatedProjectIds: n.map((l) => l.id),
        failures: a,
        appliedOperations: {
          updateCount: n.length,
          skippedCount: i + c,
          failureCount: a.length
        }
      }
    };
  }
};
function Sc({
  projects: e,
  skippedNoFields: t,
  skippedNoValidValues: r,
  failures: o
}) {
  const n = e.length, a = e.map((d) => `• ${d.name} (id=${d.id})`).join(`
`);
  let i = `Updated ${n} project${n === 1 ? "" : "s"}`;
  const c = [];
  return t > 0 && c.push(`${t} skipped - no changes`), r > 0 && c.push(`${r} skipped - no valid field values`), c.length > 0 && (i += ` (${c.join(", ")})`), n > 0 && (i += `:
${a}`), at(i, o);
}
function jc({ id: e, ...t }) {
  const r = Object.values(t);
  return r.length === 0 ? "no-fields" : r.every((o) => o === void 0) ? "no-valid-values" : null;
}
const Cc = s.object({
  type: s.literal("relative"),
  id: s.string().min(1).describe("The ID of the relative reminder to update."),
  minuteOffset: s.number().int().min(0).optional().describe("New minute offset before task due time."),
  service: je.optional().describe('New delivery method: "email" or "push".'),
  isUrgent: Ce
}), $c = s.object({
  type: s.literal("absolute"),
  id: s.string().min(1).describe("The ID of the absolute reminder to update."),
  due: cs.optional().describe("New due date/time for the reminder."),
  service: je.optional().describe('New delivery method: "email" or "push".'),
  isUrgent: Ce
}), Dc = s.object({
  type: s.literal("location"),
  id: s.string().min(1).describe("The ID of the location reminder to update."),
  name: s.string().optional().describe("New location name."),
  locLat: s.string().optional().describe("New latitude."),
  locLong: s.string().optional().describe("New longitude."),
  locTrigger: ds.optional().describe(
    'New trigger condition: "on_enter" or "on_leave".'
  ),
  radius: s.number().int().optional().describe("New radius in meters.")
}), xc = s.discriminatedUnion("type", [
  Cc,
  $c,
  Dc
]), Ac = {
  reminders: s.array(xc).min(1).max(we).describe(
    `Array of reminders to update (max ${we}). Each must include the reminder type and ID. Only include fields that need to change.`
  )
}, Ec = {
  reminders: s.array(st).describe("The updated reminders."),
  totalCount: s.number().describe("Total reminders updated."),
  updatedReminderIds: s.array(s.string()).describe("IDs of updated reminders.")
}, Oc = {
  name: g.UPDATE_REMINDERS,
  description: 'Update existing reminders. Each reminder must specify its type ("relative", "absolute", or "location") and ID. Only include fields that need to change.',
  parameters: Ac,
  outputSchema: Ec,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !0 },
  async execute(e, t) {
    const { reminders: r } = e, o = r.map(async (d) => {
      switch (d.type) {
        case "relative": {
          const { id: l, type: u, ...p } = d;
          return await t.updateReminder(l, {
            reminderType: "relative",
            ...p
          });
        }
        case "absolute": {
          const { id: l, type: u, ...p } = d;
          return await t.updateReminder(l, {
            reminderType: "absolute",
            ...p
          });
        }
        case "location": {
          const { id: l, type: u, ...p } = d;
          return await t.updateLocationReminder(l, p);
        }
      }
    }), a = (await Promise.all(o)).map(be), i = a.length === 1 ? "reminder" : "reminders";
    return {
      textContent: `Updated ${a.length} ${i}`,
      structuredContent: {
        reminders: a,
        totalCount: a.length,
        updatedReminderIds: a.map((d) => d.id)
      }
    };
  }
}, Pc = s.object({
  id: s.string().min(1).describe("The ID of the section to update."),
  name: s.string().min(1).optional().describe("The new name of the section."),
  description: s.preprocess(
    // `null` is the advertised clear value; the param schema stays a
    // plain string (Gemini forbids nullable schemas, not preprocessing),
    // so `null` is normalised to "" and then mapped to the section
    // wire clear (`null`) at execute.
    (e) => e === null ? "" : e,
    s.string().describe(
      "The description of the section (Markdown). Pass null (or an empty string) to clear it."
    )
  ).optional()
}).refine((e) => e.name !== void 0 || e.description !== void 0, {
  message: 'Provide at least one of "name" or "description" to update.'
}), _c = {
  sections: s.array(Pc).min(1).describe("The sections to update.")
}, Rc = {
  sections: s.array(X).describe("The updated sections."),
  totalCount: s.number().describe("The total number of sections updated."),
  updatedSectionIds: s.array(s.string()).describe("The IDs of the updated sections.")
}, Uc = {
  name: g.UPDATE_SECTIONS,
  description: "Update multiple existing sections with new values.",
  parameters: _c,
  outputSchema: Rc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute({ sections: e }, t) {
    const r = await Promise.all(
      e.map(({ id: n, name: a, description: i }) => {
        const c = {
          ...a !== void 0 ? { name: a } : {},
          ...i !== void 0 ? { description: i === "" ? null : i } : {}
        };
        return t.updateSection(n, c);
      })
    );
    return {
      textContent: Lc({
        sections: r
      }),
      structuredContent: {
        sections: r.map(K),
        totalCount: r.length,
        updatedSectionIds: r.map((n) => n.id)
      }
    };
  }
};
function Lc({ sections: e }) {
  const t = e.length, r = e.map((n) => `• ${n.name} (id=${n.id}, projectId=${n.projectId})`).join(`
`);
  return `Updated ${t} section${t === 1 ? "" : "s"}:
${r}`;
}
function Ss(e, t) {
  return t.projectId !== void 0 ? e.projectId === t.projectId && !e.sectionId && !e.parentId : t.sectionId !== void 0 ? e.sectionId === t.sectionId && !e.parentId : e.parentId === t.parentId;
}
function Nc(e, t) {
  return t.projectId !== void 0 ? e.projectId === t.projectId : t.sectionId !== void 0 ? e.sectionId === t.sectionId : e.parentId === t.parentId;
}
function Mc(e) {
  return e.projectId !== void 0 ? `project:${e.projectId}` : e.sectionId !== void 0 ? `section:${e.sectionId}` : `parent:${e.parentId}`;
}
function Fc(e) {
  const t = [];
  return e.projectId && t.push({ projectId: e.projectId }), e.sectionId && t.push({ sectionId: e.sectionId }), e.parentId && t.push({ parentId: e.parentId }), t;
}
function Hc({
  taskId: e,
  request: t,
  current: r
}) {
  const o = Fc(t);
  if (o.length === 0)
    return { redundantMoveSkipped: !1 };
  let n = o, a = !1;
  if (r) {
    const i = o.length === 1 ? (c) => Ss(r, c) : (c) => Nc(r, c);
    n = o.filter((c) => !i(c)), a = n.length < o.length;
  }
  if (n.length === 0)
    return { redundantMoveSkipped: a };
  if (n.length > 1)
    throw new Error(
      `Task ${e}: Only one of projectId, sectionId, or parentId can be specified at a time. The Todoist API requires exactly one destination for move operations.`
    );
  return { move: n[0], redundantMoveSkipped: a };
}
const Wc = s.object({
  id: s.string().min(1).describe("The ID of the task to update."),
  content: s.string().optional().describe(
    'The new task name/title. Should be concise and actionable (e.g., "Review PR #123", "Call dentist"). For longer content, use the description field instead. Supports Markdown.'
  ),
  description: s.string().optional().describe(
    "New additional details, notes, or context for the task. Use this for longer content rather than putting it in the task name. Supports Markdown."
  ),
  projectId: s.string().optional().describe(
    'Move the task to this project ID, or the text "inbox" for the inbox. Omit unless the project should change: moving a task to a project also lifts it out of its section and out from under its parent.'
  ),
  sectionId: s.string().optional().describe(
    "Move the task to this section ID. Omit unless the section should change: moving a task to a section also lifts it out from under its parent."
  ),
  parentId: s.string().optional().describe("Make the task a subtask of this task ID. Omit unless the parent should change."),
  order: s.number().optional().describe("The new order of the task within its parent/section."),
  priority: Ze.optional().describe(Xe),
  dueString: s.preprocess(
    // Keep accepting legacy null while exposing a Gemini-compatible string schema.
    (e) => e === null ? "remove" : e,
    s.string().describe(
      'The new due date for the task in natural language (e.g., "tomorrow at 5pm"). Use "remove" to clear the due date.'
    )
  ).optional(),
  deadlineDate: s.preprocess(
    // Keep accepting legacy null while exposing a Gemini-compatible string schema.
    (e) => e === null ? "remove" : e,
    s.string().describe(
      'The new deadline date for the task in ISO 8601 format (YYYY-MM-DD, e.g., "2025-12-31"). Deadlines are immovable constraints shown with a different indicator than due dates. Use "remove" to clear the deadline.'
    )
  ).optional(),
  duration: s.string().optional().describe(
    'The duration of the task. Use format: "2h" (hours), "90m" (minutes), "2h30m" (combined), or "1.5h" (decimal hours). Max 24h.'
  ),
  responsibleUser: s.preprocess(
    // Keep accepting legacy null while exposing a Gemini-compatible string schema.
    (e) => e === null ? "unassign" : e,
    s.string()
  ).optional().describe(
    'Change task assignment. Use "unassign" to remove assignment. Can be "me" (assigns to current user), a user ID, name, or email. User must be a project collaborator.'
  ),
  labels: s.array(s.string()).optional().describe("The new labels for the task. Replaces all existing labels."),
  isUncompletable: s.boolean().optional().describe(
    "Whether this task should be uncompletable (organizational header). Tasks with isUncompletable: true appear as organizational headers and cannot be completed."
  )
}), Bc = (e) => !!(e.projectId || e.sectionId || e.parentId), js = (e) => Object.keys(e).length > 0, zc = ["remove", "no date"], Kc = ["remove", "no date", "no deadline"], qc = "no date", Ye = Kt.TASKS_PER_OPERATION, Ot = {
  tasks: s.array(Wc).min(1).max(Ye).describe(`The tasks to update (max ${Ye}).`)
}, Yc = {
  tasks: s.array(F).describe("The updated tasks."),
  totalCount: s.number().describe("The total number of tasks updated."),
  updatedTaskIds: s.array(s.string()).describe("The IDs of the updated tasks."),
  failures: s.array(ee).describe(
    "Tasks that could not be updated, with the reason for each. A failure here does not affect the other tasks in the batch."
  ),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of tasks actually updated."),
    skippedCount: s.number().describe("The number of tasks skipped (no changes)."),
    failureCount: s.number().describe("The number of tasks that failed to update."),
    redundantMovesSkipped: s.number().describe(
      "Moves not performed because the task was already in the requested destination. A non-zero count means container fields were sent that did not need to be."
    )
  }).describe("Summary of operations performed.")
}, Gc = {
  name: g.UPDATE_TASKS,
  description: "Update existing tasks including content, dates, priorities, and assignments. Send only the fields that change.",
  parameters: Ot,
  outputSchema: Yc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: r } = s.object(Ot).parse(e), o = r.map(() => ({ kind: "skipped" })), n = (T, b) => {
      o[T] = { kind: "failed", error: b };
    }, a = (T, b) => {
      n(T, z(b));
    }, i = r.map((T, b) => ({ task: T, index: b })).filter(({ task: T }) => rd(T)), c = await Jc(
      i.map(({ task: T }) => T),
      t
    ), d = await Promise.allSettled(
      i.map(({ task: T, index: b }) => ed({ task: T, index: b, client: t, inbox: c }))
    ), l = [];
    for (const [T, b] of d.entries()) {
      const S = i[T]?.index ?? T;
      b.status === "fulfilled" ? l.push(b.value) : a(S, b.reason);
    }
    const u = await Cs({
      client: t,
      ids: [
        ...new Set(
          l.filter((T) => Bc(T.moveRequest)).map((T) => T.id)
        )
      ]
    }), p = [];
    for (const T of l)
      try {
        const b = Hc({
          taskId: T.id,
          request: T.moveRequest,
          current: u.get(T.id)
        });
        T.redundantMoveSkipped = b.redundantMoveSkipped, b.move && p.push({ item: T, move: b.move });
      } catch (b) {
        a(T.index, b), T.failed = !0;
      }
    const m = /* @__PURE__ */ new Map(), h = [], y = (T) => {
      const b = T.filter((S) => !S.failed);
      b.length !== 0 && h.push(
        Qc({
          client: t,
          items: b,
          onUpdated: (S, $) => {
            o[S.index] = { kind: "updated", task: $ };
          },
          onFailed: (S, $) => {
            const D = m.get(S.index);
            if (D && "task" in D) {
              o[S.index] = {
                kind: "partial",
                task: D.task,
                error: `Move applied; field update failed: ${z($)}`
              };
              return;
            }
            a(S.index, $);
          }
        })
      );
    }, f = new Set(p.map(({ item: T }) => T.index));
    y(l.filter((T) => !f.has(T.index))), await Xc({
      client: t,
      moving: p,
      results: m,
      onResolved: (T) => {
        const b = [];
        for (const { item: S } of T) {
          const $ = m.get(S.index);
          if ($ && "error" in $) {
            n(S.index, $.error), S.failed = !0;
            continue;
          }
          b.push(S);
        }
        y(b);
      }
    }), await Promise.all(h);
    for (const T of l) {
      if (T.failed || js(T.updateArgs))
        continue;
      const b = m.get(T.index);
      b && "task" in b && (o[T.index] = { kind: "updated", task: b.task });
    }
    const { updatedTasks: v, failures: j, skippedCount: A } = Vc({ tasks: r, outcomes: o }), k = l.filter((T) => T.redundantMoveSkipped).length, I = v.map(P);
    return {
      textContent: sd({
        tasks: I,
        failures: j,
        skippedCount: A,
        redundantMovesSkipped: k
      }),
      structuredContent: {
        tasks: I,
        totalCount: I.length,
        updatedTaskIds: v.map((T) => T.id),
        failures: j,
        appliedOperations: {
          updateCount: I.length,
          skippedCount: A,
          failureCount: j.length,
          redundantMovesSkipped: k
        }
      }
    };
  }
};
function Vc({ tasks: e, outcomes: t }) {
  const r = [], o = [];
  let n = 0;
  for (const [a, i] of t.entries()) {
    const c = e[a]?.id ?? `Task ${a + 1}`;
    if (i.kind === "skipped") {
      n++;
      continue;
    }
    if (i.kind === "failed") {
      o.push({ item: c, error: i.error });
      continue;
    }
    r.push(i.task), i.kind === "partial" && o.push({ item: c, error: i.error, code: "PARTIAL_MOVE_APPLIED" });
  }
  return { updatedTasks: r, failures: o, skippedCount: n };
}
async function Jc(e, t) {
  if (!e.some((r) => N(r.projectId)))
    return {};
  try {
    return { projectId: (await O(() => t.getUser())).inboxProjectId };
  } catch (r) {
    return { error: r };
  }
}
async function Cs({
  client: e,
  ids: t
}) {
  const r = /* @__PURE__ */ new Map();
  if (t.length === 0)
    return r;
  try {
    const o = new Set(t), n = {
      ids: t.join(","),
      limit: Ye
    }, { results: a } = await O(() => e.getTasks(n));
    for (const i of a)
      o.has(i.id) && r.set(i.id, i);
  } catch {
  }
  return r;
}
async function Xc({
  client: e,
  moving: t,
  results: r,
  onResolved: o
}) {
  if (t.length === 0)
    return;
  const n = /* @__PURE__ */ new Map();
  for (const c of t) {
    const d = Mc(c.move), l = n.get(d);
    l ? l.push(c) : n.set(d, [c]);
  }
  const a = lr(e), i = [];
  for (const c of n.values()) {
    const d = [...new Set(c.map(({ item: p }) => p.id))], l = c[0]?.move, u = c.length === 1 ? c[0] : void 0;
    try {
      if (u) {
        const f = await a(
          () => O(() => e.moveTask(u.item.id, l))
        );
        r.set(u.item.index, { task: f }), o(c);
        continue;
      }
      const p = await a(
        () => O(() => e.moveTasks(d, l))
      ), m = new Map(p.map((f) => [f.id, f])), h = [], y = [];
      for (const f of c) {
        const v = m.get(f.item.id);
        v ? (r.set(f.item.index, { task: v }), y.push(f)) : h.push(f);
      }
      o(y), h.length > 0 && i.push({ group: h, destination: l });
    } catch (p) {
      if (u) {
        r.set(u.item.index, { error: z(p) }), o(c);
        continue;
      }
      i.push({ group: c, destination: l, error: p });
    }
  }
  if (i.length > 0) {
    await Zc({ client: e, unresolved: i, results: r });
    for (const { group: c } of i)
      o(c);
  }
}
async function Zc({
  client: e,
  unresolved: t,
  results: r
}) {
  const o = [...new Set(t.flatMap(({ group: a }) => a.map(({ item: i }) => i.id)))], n = await Cs({ client: e, ids: o });
  for (const { group: a, destination: i, error: c } of t) {
    const d = c === void 0 ? "Task not returned by the move" : z(c);
    for (const { item: l } of a) {
      const u = n.get(l.id);
      if (u && Ss(u, i)) {
        r.set(l.index, { task: u });
        continue;
      }
      r.set(l.index, { error: d });
    }
  }
}
async function Qc({
  client: e,
  items: t,
  onUpdated: r,
  onFailed: o
}) {
  const n = ur(e);
  await Promise.all(
    t.filter((a) => js(a.updateArgs)).map(async (a) => {
      try {
        const i = await n(
          () => O(() => e.updateTask(a.id, a.updateArgs))
        );
        r(a, i);
      } catch (i) {
        o(a, i);
      }
    })
  );
}
async function ed({
  task: e,
  index: t,
  client: r,
  inbox: o
}) {
  const {
    id: n,
    projectId: a,
    sectionId: i,
    parentId: c,
    dueString: d,
    duration: l,
    responsibleUser: u,
    priority: p,
    labels: m,
    deadlineDate: h,
    ...y
  } = e;
  if (N(a) && o.error !== void 0)
    throw o.error;
  const f = N(a) ? o.projectId : a;
  let v = {
    ...y,
    ...m !== void 0 && { labels: m }
  };
  p && (v.priority = Zt(p));
  const j = Pt(
    d,
    zc,
    qc
  );
  j !== void 0 && (v = { ...v, dueString: j });
  const A = Pt(h, Kc, null);
  if (A !== void 0 && (v = { ...v, deadlineDate: A }), l)
    try {
      const { minutes: k } = Xt(l);
      v = {
        ...v,
        duration: k,
        durationUnit: "minute"
      };
    } catch (k) {
      throw k instanceof _ ? new Error(`Task ${n}: ${k.message}`) : k;
    }
  return u !== void 0 && (v = {
    ...v,
    assigneeId: await td(r, n, u)
  }), {
    index: t,
    id: n,
    updateArgs: v,
    moveRequest: {
      projectId: f,
      sectionId: i,
      parentId: c
    },
    redundantMoveSkipped: !1,
    failed: !1
  };
}
async function td(e, t, r) {
  if (r === null || r === "unassign")
    return null;
  const o = await it.validateTaskUpdateAssignment(
    e,
    t,
    r
  );
  if (!o.isValid) {
    const n = o.error?.message || "Assignment validation failed", a = o.error?.suggestions?.join(". ") || "";
    throw new Error(`Task ${t}: ${n}${a ? `. ${a}` : ""}`);
  }
  return o.resolvedUser?.userId;
}
function sd({
  tasks: e,
  failures: t,
  skippedCount: r,
  redundantMovesSkipped: o
}) {
  const n = [];
  r > 0 && n.push(`${r} skipped - no changes`), o > 0 && n.push(`${o} already in requested destination`), t.length > 0 && n.push(`${t.length} failed`);
  const a = n.length > 0 ? ` (${n.join(", ")})` : "", i = ot("Updated", e, {
    context: a,
    showDetails: e.length <= 5
  });
  if (t.length === 0)
    return i;
  const c = t.slice(0, fe.MAX_FAILURES_SHOWN), d = t.length - c.length, l = c.map((p) => `    ${p.item}: ${p.error}`).join(`
`), u = d > 0 ? `
    +${d} more` : "";
  return `${i}
Failed (${t.length}) - address or drop these items:
${l}${u}`;
}
function rd({ id: e, ...t }) {
  return Object.keys(t).length > 0;
}
function Pt(e, t, r) {
  if (e === void 0)
    return e;
  if (e === null)
    return r;
  const o = e.trim().toLowerCase();
  return t.includes(o) ? r : e;
}
const od = {}, nd = {
  type: s.literal("user_info").describe("The type of the response."),
  userId: s.string().describe("The user ID."),
  fullName: s.string().describe("The full name of the user."),
  timezone: s.string().describe("The timezone of the user."),
  currentLocalTime: s.string().describe("The current local time of the user."),
  startDay: s.number().describe("The start day of the week (1 = Monday, 7 = Sunday)."),
  startDayName: s.string().describe("The name of the start day."),
  weekStartDate: s.string().describe("The start date of the current week (YYYY-MM-DD)."),
  weekEndDate: s.string().describe("The end date of the current week (YYYY-MM-DD)."),
  currentWeekNumber: s.number().describe("The current week number of the year."),
  completedToday: s.number().describe("The number of tasks completed today."),
  dailyGoal: s.number().describe("The daily goal for task completions."),
  weeklyGoal: s.number().describe("The weekly goal for task completions."),
  email: s.string().describe("The email address of the user."),
  plan: s.enum(["Todoist Free", "Todoist Pro", "Todoist Business"]).describe("The user plan.")
};
function ad(e) {
  return e.businessAccountId ? "Todoist Business" : e.isPremium ? "Todoist Pro" : "Todoist Free";
}
function id(e, t) {
  const o = ((e.getDay() || 7) - t + 7) % 7, n = new Date(e);
  return n.setDate(e.getDate() - o), n;
}
function cd(e) {
  const t = new Date(e);
  return t.setDate(e.getDate() + 6), t;
}
function dd(e) {
  const t = new Date(e.getFullYear(), 0, 1), r = (e.getTime() - t.getTime()) / 864e5;
  return Math.ceil((r + t.getDay() + 1) / 7);
}
function ld(e) {
  return ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"][e === 7 ? 0 : e] ?? "Unknown";
}
function ud(e) {
  try {
    return new Intl.DateTimeFormat("en-US", { timeZone: e }), !0;
  } catch {
    return !1;
  }
}
function $s(e) {
  return ud(e) ? e : "UTC";
}
function pd(e, t) {
  const r = $s(t);
  return e.toLocaleString("en-US", {
    timeZone: r,
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: !1
  });
}
async function md(e) {
  const t = await e.getUser(), r = t.tzInfo?.timezone ?? "UTC", o = $s(r), n = /* @__PURE__ */ new Date(), a = pd(n, o), i = t.startDay ?? 1, c = ld(i), d = ad(t), l = new Date(n.toLocaleString("en-US", { timeZone: o })), u = id(l, i), p = cd(u), m = dd(l), y = [
    "# User Information",
    "",
    `**User ID:** ${t.id}`,
    `**Full Name:** ${t.fullName}`,
    `**Email:** ${t.email}`,
    `**Timezone:** ${o}`,
    `**Current Local Time:** ${a}`,
    "",
    "## Week Settings",
    `**Week Start Day:** ${c} (${i})`,
    `**Current Week:** Week ${m}`,
    `**Week Start Date:** ${ie(u)}`,
    `**Week End Date:** ${ie(p)}`,
    "",
    "## Daily Progress",
    `**Completed Today:** ${t.completedToday}`,
    `**Daily Goal:** ${t.dailyGoal}`,
    `**Weekly Goal:** ${t.weeklyGoal}`,
    "",
    "## Account Info",
    `**Plan:** ${d}`
  ].join(`
`), f = {
    type: "user_info",
    userId: t.id,
    fullName: t.fullName,
    timezone: o,
    currentLocalTime: a,
    startDay: i,
    startDayName: c,
    weekStartDate: ie(u),
    weekEndDate: ie(p),
    currentWeekNumber: m,
    completedToday: t.completedToday,
    dailyGoal: t.dailyGoal,
    weeklyGoal: t.weeklyGoal,
    email: t.email,
    plan: d
  };
  return { textContent: y, structuredContent: f };
}
const hd = {
  name: g.USER_INFO,
  description: "Get comprehensive user information including user ID, full name, email, timezone with current local time, week start day preferences, current week dates, daily/weekly goal progress, and user plan (Free/Pro/Business).",
  parameters: od,
  outputSchema: nd,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const r = await md(t);
    return {
      textContent: r.textContent,
      structuredContent: r.structuredContent
    };
  }
}, _t = 10 * 1024 * 1024, fd = /* @__PURE__ */ new Set([
  "image/png",
  "image/jpeg",
  "image/gif",
  "image/webp",
  "image/svg+xml"
]), bd = /* @__PURE__ */ new Set([
  "text/plain",
  "text/csv",
  "text/html",
  "text/markdown",
  "application/json",
  "application/xml",
  "text/xml"
]), gd = {
  ".png": "image/png",
  ".jpg": "image/jpeg",
  ".jpeg": "image/jpeg",
  ".gif": "image/gif",
  ".webp": "image/webp",
  ".svg": "image/svg+xml",
  ".txt": "text/plain",
  ".csv": "text/csv",
  ".html": "text/html",
  ".md": "text/markdown",
  ".json": "application/json",
  ".xml": "application/xml",
  ".pdf": "application/pdf"
};
function yd(e) {
  return fd.has(e) ? "image" : bd.has(e) || e.startsWith("text/") ? "text" : "binary";
}
function Td(e) {
  return (e.split(";")[0] ?? e).trim().toLowerCase();
}
function kd(e) {
  try {
    const t = new URL(e).pathname, r = t.lastIndexOf(".");
    if (r === -1) return;
    const o = t.slice(r).toLowerCase();
    return gd[o];
  } catch {
    return;
  }
}
function wd(e) {
  try {
    const t = new URL(e).pathname, r = t.lastIndexOf("/");
    return r === -1 ? void 0 : t.slice(r + 1) || void 0;
  } catch {
    return;
  }
}
const vd = {
  fileUrl: s.url().describe(
    "The URL of the attachment file to view. Get this from the fileUrl field in a comment's fileAttachment."
  )
}, Id = {
  name: g.VIEW_ATTACHMENT,
  description: "View a file attachment from a Todoist comment. Pass the fileUrl from a comment's fileAttachment field. Supports images (returned inline), text files (returned as text), and binary files like PDFs (returned as embedded resources).",
  parameters: vd,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute({ fileUrl: e }, t) {
    const r = await t.viewAttachment(e), o = r.headers["content-length"], n = o ? Number.parseInt(o, 10) : void 0, a = wd(e), i = r.headers["content-type"], c = i ? Td(i) : void 0, d = c && c !== "application/octet-stream" ? c : kd(e) ?? c ?? "application/octet-stream";
    if (n && n > _t)
      return {
        textContent: `Attachment "${a ?? e}" is too large to display inline (${(n / 1024 / 1024).toFixed(1)}MB, limit is 10MB). File type: ${d}`
      };
    const l = Buffer.from(await r.arrayBuffer()), u = l.byteLength;
    if (u > _t)
      return {
        textContent: `Attachment "${a ?? e}" is too large to display inline (${(u / 1024 / 1024).toFixed(1)}MB, limit is 10MB). File type: ${d}`
      };
    const p = yd(d), m = [];
    return p === "image" ? m.push({
      type: "image",
      data: l.toString("base64"),
      mimeType: d
    }) : p === "text" ? m.push({
      type: "text",
      text: l.toString("utf-8")
    }) : m.push({
      type: "resource",
      resource: {
        uri: e,
        mimeType: d,
        blob: l.toString("base64")
      }
    }), {
      textContent: `Attachment: ${a ?? "unknown"} (${d}, ${(u / 1024).toFixed(1)}KB)`,
      contentItems: m
    };
  }
}, Sd = {
  ...Pa,
  _meta: {
    ui: {
      resourceUri: Le
    }
  }
}, jd = {
  // Task management tools
  addTasks: pn,
  completeTasks: wn,
  uncompleteTasks: Zi,
  updateTasks: Gc,
  rescheduleTasks: Ki,
  findTasks: Fa,
  findTasksByDate: Sd,
  findCompletedTasks: aa,
  // Project management tools
  addProjects: Yo,
  updateProjects: Ic,
  findProjects: ka,
  projectManagement: Ri,
  projectMove: Ni,
  // Section management tools
  addSections: an,
  updateSections: Uc,
  findSections: xa,
  // Comment management tools
  addComments: Io,
  findComments: Xn,
  updateComments: rc,
  // Reminder management tools
  addReminders: tn,
  findReminders: Sa,
  updateReminders: Oc,
  // Attachment tools
  viewAttachment: Id,
  // Label management tools
  addLabels: Uo,
  updateLabels: gc,
  findLabels: la,
  // Filter management tools
  findFilters: $o,
  addFilters: Oo,
  updateFilters: cc,
  // Activity and audit tools
  findActivity: zn,
  getProductivityStats: Qa,
  // Health and insights tools
  getProjectHealth: ui,
  getProjectActivityStats: oi,
  analyzeProjectHealth: yn,
  getWorkspaceInsights: fi,
  // General tools
  getOverview: Ja,
  deleteObject: jn,
  fetchObject: Ln,
  reorderObjects: Hi,
  userInfo: hd,
  // Assignment and collaboration tools
  findProjectCollaborators: ha,
  manageAssignments: Oi,
  // Template tools
  exportProjectTemplate: Dn,
  importProjectTemplate: ki,
  // Workspace tools
  listWorkspaces: Ci,
  // OpenAI MCP tools
  search: Vi,
  fetch: Fn
}, Cd = Object.values(jd), $d = `
## Todoist Task and Project Management Tools

You have access to comprehensive Todoist management tools for personal productivity and team collaboration. Use these tools to help users manage tasks, projects, sections, comments, and assignments effectively.

### Core Capabilities:
- Create, update, complete, and search tasks with rich metadata (priorities, due dates, durations, assignments)
- Manage projects and sections with flexible organization
- Handle comments and collaboration features
- Bulk assignment operations for team workflows
- Get overviews and insights about workload and progress

### Choosing between tools

What each tool does and how to fill its parameters is in the tool's own description and input schema. This section covers only what those cannot: which tool to reach for, and how tools relate to each other.

**Dates**

- To move a task to a different date use **reschedule-tasks**, never **update-tasks**. update-tasks replaces the whole due string, which destroys recurrence on recurring tasks.
- Never send a task's existing projectId, sectionId or parentId back to **update-tasks** — those fields are treated as a move.
- All dates respect the user's timezone.

**Finding things**

- For "what did I complete?", use **find-activity** with objectType="task" and eventType="completed". It reports completion events, including each occurrence of a recurring task. **find-completed-tasks** lists completed tasks, which is not the same question.
- To resolve a person's name or email to a user ID, or to answer "who is X?", use **find-project-collaborators** with just a searchTerm. It covers every shared project you can access plus yourself, so an empty result means they collaborate on none of them — not that they do not exist.
- Before assigning work to someone, call **find-project-collaborators** again with the target projectId. Resolving an ID only proves they collaborate on *some* project you can see; assignment fails unless they collaborate on that one.
- To find out whether a task hides subtasks, use **fetch-object** with includeChildren rather than a speculative **find-tasks** call.
- Filter tasks by label **name**. Label IDs are only for **delete-object** and **update-labels**. Shared labels can be renamed but not recoloured, reordered or favourited.

**Deleting and archiving**

- **delete-object** removes every object type; there is no per-type delete tool. Reminders use type "reminder", location reminders "location_reminder".
- A workspace project must be archived with **project-management** before it can be deleted. A personal project can be deleted directly.

**Templates**

- Templates cannot be listed through this server, so only use an ID or URL the user gave you. To start a new project from one, call **add-projects** first and import into it. Imports write immediately and cannot be undone.

**Project health**

- Health data may be stale — check the isStale flag, and call **analyze-project-health** to refresh it before reading again.

**Batches**

- Prefer a batch tool over one call per item.
- Where a batch tool returns per-item \`failures\` alongside successes, one failure does not undo the rest: never retry the whole batch, re-send only the items whose failure reason is fixable. **reschedule-tasks** is the exception — it has no per-item failures and throws if any task fails, so retry it as a whole.

Use the **productivity-analysis** prompt for a combined productivity review — it pulls together user-info, get-productivity-stats and find-completed-tasks.

Always provide clear, actionable task titles and descriptions. Use the overview tools to give users context about their workload and project status.
`;
function Hd({
  todoistApiKey: e,
  baseUrl: t,
  features: r = []
}) {
  const o = new xs(
    { name: "todoist-mcp-server", version: Je },
    {
      capabilities: {
        tools: { listChanged: !0 },
        prompts: { listChanged: !0 }
      },
      instructions: $d
    }
  ), n = jr(e, { baseUrl: t });
  Gs(o);
  const a = { server: o, client: n, features: r };
  for (const i of Cd)
    Kr({ tool: i, ...a });
  return o.registerPrompt(
    he.name,
    {
      title: he.title,
      description: he.description,
      argsSchema: he.argsSchema
    },
    he.callback
  ), o;
}
export {
  Er as $,
  Ja as A,
  Qa as B,
  oi as C,
  ui as D,
  fi as E,
  Fd as F,
  ki as G,
  Ci as H,
  Oi as I,
  Ri as J,
  Ni as K,
  Cd as L,
  Hi as M,
  Ki as N,
  Vi as O,
  Zi as P,
  rc as Q,
  cc as R,
  gc as S,
  Ic as T,
  Oc as U,
  Uc as V,
  Gc as W,
  hd as X,
  Id as Y,
  jr as Z,
  br as _,
  Io as a,
  Oo as b,
  Uo as c,
  Yo as d,
  tn as e,
  an as f,
  Hd as g,
  pn as h,
  yn as i,
  wn as j,
  jn as k,
  Dn as l,
  Fn as m,
  Ln as n,
  zn as o,
  Xn as p,
  aa as q,
  $o as r,
  la as s,
  jd as t,
  ha as u,
  ka as v,
  Sa as w,
  xa as x,
  Fa as y,
  Pa as z
};
