import type { DisableEmailArgs, GetOrCreateEmailArgs, GetOrCreateEmailResponse } from '../types/emails/index.js';
import { BaseClient } from './base-client.js';
/**
 * Internal sub-client handling email-forwarding endpoints.
 *
 * Instantiated by `TodoistApi`; every public email-forwarding method on
 * `TodoistApi` delegates here. See `todoist-api.ts` for user-facing
 * JSDoc.
 */
export declare class EmailClient extends BaseClient {
    getOrCreateEmailForwarding(args: GetOrCreateEmailArgs, requestId?: string): Promise<GetOrCreateEmailResponse>;
    disableEmailForwarding(args: DisableEmailArgs, requestId?: string): Promise<boolean>;
}
