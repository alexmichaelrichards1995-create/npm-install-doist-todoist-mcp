import type { GetIdMappingsArgs, GetMovedIdsArgs, IdMapping, MovedId } from '../types/id-mappings/index.js';
import { BaseClient } from './base-client.js';
/**
 * Internal sub-client handling ID-mapping endpoints.
 *
 * Instantiated by `TodoistApi`; every public id-mapping method on
 * `TodoistApi` delegates here. See `todoist-api.ts` for user-facing
 * JSDoc.
 */
export declare class IdMappingClient extends BaseClient {
    getIdMappings(args: GetIdMappingsArgs): Promise<IdMapping[]>;
    getMovedIds(args: GetMovedIdsArgs): Promise<MovedId[]>;
}
